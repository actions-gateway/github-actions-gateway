# Project Status

Single source of truth for progress and priorities across the full project. `docs/plan/` holds the implementation detail; this file holds the ordering and the overview. Pick the next task from the top of the [Queue](#queue).

## Conventions

**Status:** 🔲 ready · 🚫 blocked  
**Size:** S = one session · M = 2–3 sessions · L = multi-session, needs a phased plan doc in `docs/plan/`  
**Labels:** `milestone` `security` `tests` `speed` `docs` `infra` `feature` `bug` `flake` `retro` `1.0-gate` (blocks the [Release 1.0](plan/release-1.0.md) tag) `1.3-gate` (blocks the [Release 1.3](plan/release-1.3.md) tag) `2.0-gate` (blocks the [`v2.0.0`](plan/v2-ga.md) tag)  
**New IDs:** `make queue-id` ([why there is no counter here](development/queue-id-allocation.md))

Maintained per [`docs/development/maintaining-backlog.md`](development/maintaining-backlog.md): done rows are deleted (git is the archive), the open PR is the in-flight signal, new items enter at the priority they deserve, parked items live in [Deferred](#deferred), and every edit is an isolated `docs(status):` commit gated by `scripts/docs/lint-backlog.sh`.

---

## Progress

Plan-level view. ✅ = no open Queue row remains (intentionally-deferred residuals live in [Deferred](#deferred) and don't count against completion). ⚠️ = ≥1 open Queue row remains. See [maintaining-backlog.md](development/maintaining-backlog.md#-means-an-open-queue-row-remains--deferred-residuals-dont-count).

| Item | Labels | Status |
|---|---|---|
| [M1: Wire-protocol probe](plan/milestone-1.md) | `milestone` | ✅ |
| [M1: Unit-test coverage](plan/milestone-1-tests.md) | `milestone` `tests` | ✅ |
| [M2: AGC controller](plan/milestone-2.md) | `milestone` | ✅ |
| [M3: Worker pod](plan/milestone-3.md) | `milestone` | ✅ |
| [M4: GMC + proxy](plan/milestone-4.md) | `milestone` | ✅ |
| [M5: Hardening](plan/milestone-5.md) | `milestone` `security` | ✅ |
| [Release 1.0](plan/release-1.0.md) | `milestone` | ✅ |
| [Security hardening](plan/security.md) | `security` | ✅ |
| [Security audit 2 (2026-06)](plan/security-audit-2026-06.md) | `security` | ✅ |
| [Worker egress proxy](plan/worker-egress-proxy.md) | `security` `infra` | ✅ |
| [Docs](plan/docs.md) | `docs` | ✅ |
| [Six-layer docs audit](plan/docs-six-layer-audit.md) | `docs` | ✅ |
| [Make UX](plan/make.md) | `infra` | ✅ |
| [Docker image speed](plan/docker-image-speed.md) | `speed` | ✅ |
| [e2e test speed](plan/e2e-tests-speed.md) | `speed` `tests` | ✅ |
| [e2e CI speed round 2](plan/e2e-ci-speed-round-2.md) | `speed` `tests` `infra` | ✅ |
| [v2 API decomposition](plan/v2-api.md) | `infra` | ✅ |
| [Per-module coverage ≥75%](plan/coverage-to-75-per-module.md) | `tests` | ✅ |
| [GKE dogfood](plan/gke-dogfood.md) | `infra` `docs` | ✅ |
| <a id="Q248"></a>[Dogfood runner right-sizing](plan/dogfood-runner-rightsizing.md) | `infra` | ✅ |
| [Release 1.3](plan/release-1.3.md) | `milestone` | ✅ |
| [v2 GA graduation](plan/v2-ga.md) | `milestone` `infra` | ✅ |
| [v1 sunset → v2-only](plan/v1-classic-sunset-review.md) | `infra` | ✅ |
| [Worker right-sizing profiles](plan/runner-sizing-profiles.md) | `infra` | ✅ |
| [Capacity-aware job intake](plan/capacity-aware-intake.md) | `infra` | ✅ |

---

## Queue

Specific actionable items in priority order. Pick from the top; skip 🚫 items until their blocker clears. Intentionally parked items live in [Deferred](#deferred) below, out of the priority ordering.

| ID | Item | Labels | St | Sz | Notes |
|---|---|---|---|---|---|
| <a id="Q559"></a>Q559 | [`TestV2_RunnerSet_CapacityGate_FixedClusterSkipsAcquire` flaked on a docs-only PR](../cmd/agc/internal/controller/integration/v2_runnerset_capacity_gate_test.go) | `flake` `tests` | 🔲 | S | First sighting on #1059 (run 30675968647, 2026-08-01): 34.9s timeout at v2_runnerset_capacity_gate_test.go:242 — a closed capacity gate never rejected the delivery with reason=capacity. Passed on rerun untouched; the PR has no Go. |
| <a id="Q549"></a>Q549 | [`E2E_AGC_ScaleSetDrainedWorkerClaimAndRerunLandUnderChartRBAC` flaked on main](../cmd/gmc/test/e2e/worker_scaleset_recovery_test.go) | `flake` `tests` | 🔲 | S | Failed once on `main` (run 30658951388, 2026-07-31, hosted): 90s timeout at worker_scaleset_recovery_test.go:247. Passed on the next two main commits untouched — flake, not a Q528 regression. Diagnose the timed-out wait; flake cost compounds. |
| <a id="Q575"></a>Q575 | [A worker pod whose `job-payload` secret is absent stalls until something else clears it](../cmd/agc/internal/scalesetlistener/listener.go) | `bug` | 🔲 | S | Live on the rc.3 gate (2026-08-01): 8 worker pods sat Pending ~10 min mounting a `job-ss-<jobID>` secret that did not exist, then recovered unaided. Find what deletes it, or what creates the pod first. |
| <a id="Q577"></a>Q577 | [`stop.sh` leaves the pool up when its drain cannot converge](plan/gke-dogfood.md#reading-a-timed-out-drain) | `infra` `bug` | 🚫 | S | Blocked by [Q575](#Q575). Not a namespace-scope bug — the drain is cluster-wide because the resize evicts every tenant's AGC. Q553's wedge is closed; re-verify on rc.4 whether Q575 alone still stalls it. |
| <a id="Q583"></a>Q583 | [An AGC restart replays the whole queue and provisions workers for jobs long gone](../cmd/agc/internal/scalesetlistener/listener.go) | `infra` `bug` | 🔲 | M | Q553's guard dies with the process; a fresh listener polls from cursor 0 because the tier never delete-acks (`DeleteMessage` wire shape unproven, a P2-surfaced P4 unknown). Unmeasured live — confirm the queue retains them before building. |
| <a id="Q166"></a>Q166 | [v2 API M4: cross-namespace EgressProxy sharing](plan/v2-api.md) | `infra` `security` `feature` | 🔲 | M | Demand fired 2026-08-01: `sharing.allowedNamespaces` is served v2beta1 API with no enforcement (#1079 sweep), so the beta contract would harden a dormant field. Next: M4 consent check, CA distribution, dual-side NetworkPolicy. |
| <a id="Q408"></a>Q408 | [Untrusted-PR egress posture for Kata workers](plan/q408-untrusted-pr-egress.md) | `security` `infra` | 🔲 | L | Demand fired 2026-07-31; plan written. Athens pattern (Q244) applied to images: per-upstream pull-through mirrors, then swap `e2e-open-egress` for a mirror-scoped NP. Next: Phase 0 egress inventory. |
| <a id="Q536"></a>Q536 | [A GHES appliance behind a private CA is not trusted by the AGC](plan/archive/q506-ghes-endpoint-audit.md#6-legitimately-githubcom) | `bug` `infra` | 🔲 | M | `BuildProxyTrustPool` builds system roots + the proxy CA, so a private-CA appliance fails the TLS handshake. Named as out of scope by Q506's audit, "needs its own item if the project commits to GHES" — Q506 shipping Option A is that commitment. |
| <a id="Q547"></a>Q547 | [Deleting an ActionsGateway orphans its in-flight worker pods](plan/gke-dogfood.md#f3-e2e-operations--on-demand) | `bug` `infra` | 🔲 | M | The AGC is the sole reaper; GMC deletes it on gateway delete and leaves worker pods behind — do-not-disrupt annotations then pin billable nodes forever (2026-07-31 dogfood). Cascade-reap tenant workers on gateway deletion, or add pod ownerRefs/TTL. |
| <a id="Q582"></a>Q582 | [The v1 and v2 proxy pools collide throughout migration coexistence](operations/migration-v1-to-v2.md#step-3--coexistence-validation-and-rollback) | `bug` `infra` | 🔲 | M | Both stamp `app: actions-gateway-proxy`, so post-`gag-migrate` each pool's pods match the other's PDB, both HPAs wedge on `AmbiguousSelector` (neither autoscales), and hostname anti-affinity makes coexistence cost v1+v2 nodes. Found via Q570. |
| <a id="Q501"></a>Q501 | [A cancelled run's worker pod keeps running to completion](plan/q501-cancel-relay.md) | `bug` `infra` | 🔲 | M | Actuator shipped — an abandoned job's worker is now deleted. Trigger open: nothing tells the AGC a run was cancelled. Two measurements decide it, both named in the plan: does `renewjob` report the loss, and is ScaleSet affected at all. |
| <a id="Q539"></a>Q539 | [Validate Kata + Dragonfly as the mirror backend](plan/q408-untrusted-pr-egress.md#6-follow-on-validations-q539-q540) | `security` `infra` | 🚫 | M | Blocked by [Q408](#Q408) — validate the reference posture first. Dragonfly passes the mirror contract only via seed-peer Service, upstream allowlist, refused non-GET, workers never peers. |
| <a id="Q540"></a>Q540 | [Milestone: Kata + Dragonfly (node layer) + pull-through cache (guest layer)](plan/q408-untrusted-pr-egress.md#6-follow-on-validations-q539-q540) | `security` `infra` | 🚫 | M | Blocked by [Q539](#Q539). Validate the composed stack: node-level P2P for pod images, in-guest mirror for job pulls; confirm the P2P mesh stays unreachable from worker pods. |
| <a id="Q319"></a>Q319 | [Export v2 RunnerSet worker-capacity conditions as gauges](operations/observability-metrics.md) | `infra` `feature` | 🔲 | S | v1 sunset shipped, so every operator is on v2, yet the three `actions_gateway_worker_*` gauges list `RunnerGroupList` only. Q303's RunnerSet conditions have no collector. Add per-RunnerSet gauges. |
| <a id="Q298"></a>Q298 | [Infra PriorityClass allowlist has no dynamic augmentation (Q188 parity)](operations/security-operations.md#infra-pods-the-separate-allowed-infra-priority-classes-allowlist) | `infra` `security` `feature` | 🔲 | S | `--allowed-infra-priority-classes` is flag-only, so growing it needs a GMC restart. The worker allowlist takes an additive, fail-safe watched `PriorityClassAllowlist` CR (Q188, moved off a ConfigMap by Q492). |
| <a id="Q545"></a>Q545 | [`tidy-check` skips a PR that adds an import without touching module files](../.github/workflows/unit-test.yml) | `bug` `infra` `retro` | 🔲 | S | The `modules` filter watches `**/go.mod`/`go.sum`/`go.work` only, so a new import that untidies them never triggers the gate. #1034 merged untidy that way; the next PR to touch a go.mod paid for it. Add `**/*.go`, or gate on the tidy run itself. |
| <a id="Q579"></a>Q579 | [Nothing lints `.github/workflows/**`, and three docs claim `actionlint` does](../.github/workflows/unit-test.yml) | `infra` `docs` | 🔲 | S | Three docs credit `actionlint`: `release.md:540` with keeping SHA-pinned `uses:` clean, `Makefile:446` and `testing.md:183` with inline `run:` blocks. No workflow runs it — the `lint` job is golangci-lint, so a workflow-only PR ships unlinted. |
| <a id="Q563"></a>Q563 | [Nothing gates a published page that lands in no nav section](development/website.md#what-belongs-in-nav) | `docs` `infra` `retro` | 🔲 | S | MkDocs reports an unlisted page at INFO, invisible under a green build — that is how Q562's three operator pages hid. `not_in_nav` declares every deliberate omission, so the list should be empty; fail `docs-preview.sh build` when it is not. |
| <a id="Q569"></a>Q569 | [Split Deferred + Flake watch off `STATUS.md`](plan/docs-page-boundaries.md#phase-2-splitting-the-parked-register-q569) | `docs` `infra` | 🔲 | M | Phase 2 of the page-boundary split: the Queue is read top-down to pick work; the parked rows under it are only grepped on a trigger. Needs `check-roadmap.sh` given a second path, rule 8 spanning two files, `#QNNN` links retargeted. |
| <a id="Q520"></a>Q520 | [AGC swallows an unreadable proxy CA cert as "no proxy configured"](../cmd/agc/main.go) | `bug` `security` | 🔲 | S | `configureProxyTrust` treats any CA read error as no-TLS-proxy; sibling `buildMetricsOptions` errors on non-`IsNotExist`. A permission-denied CA mount silently disables proxy trust — match the sibling. |
| <a id="Q401"></a>Q401 | [File upstream k8s issue: delete-path probe results never reach `Ready`](operations/node-shutdown-budgets.md) | `docs` | 🔲 | S | Q388 measured: on ordinary delete the probe fails throughout termination but `Ready` never flips; removal comes from `deletionTimestamp`. k8s#124648 covers only eviction. Head of the upstream-report batch — file all four in one session. |
| <a id="Q402"></a>Q402 | [GKE DPv2 NodeLocal DNSCache NP repro — file against Google's docs](operations/troubleshooting.md) | `docs` | 🔲 | S | Google's NodeLocal DNSCache page names DPv2 as exempt from extra NP rules; Q229 measured the opposite (node-local-dns peer required). Capture a minimal repro w/ cluster version and report it — a default-on footgun on Autopilot. |
| <a id="Q426"></a>Q426 | [File a `claude-skills` issue: backlog skill teaches the removed `Next ID` counter](development/queue-id-allocation.md) | `docs` `infra` | 🔲 | S | Report it against [karlkfi/claude-skills](https://github.com/karlkfi/claude-skills), where the skill lives. CLAUDE.md overrides it here so sessions stay correct, but the skill and its vendored lint drift until upstream updates. |
| <a id="Q531"></a>Q531 | [File upstream: karpenter kwok chart panics on its own defaults](plan/capacity-aware-intake.md#9i-the-karpenter-arm-of-the-drift-gate-and-what-it-measured-q479) | `docs` | 🔲 | S | At v1.14.0 the chart renders `featureGates.staticCapacity`/`.capacityBuffer` into `FEATURE_GATES` but values.yaml omits both keys; the empty string panics the controller at startup. Q479's harness sets them explicitly as a workaround. |
| <a id="Q546"></a>Q546 | [Write down: verify a new spec with its whole family, not in isolation](development/testing.md#path-gated-workflows-verify-the-heavy-gates-actually-ran) | `docs` `tests` `retro` | 🔲 | S | A focused `--focus` run of a new e2e spec hid a regression it caused in its sibling — they interact through shared AGC config, invisible to either alone. CI caught it. Same shape as the path-gated-gates rule, and belongs beside it. |
| <a id="Q554"></a>Q554 | [Ship a curated runner template library](plan/runner-template-library.md) | `infra` `docs` `feature` | 🔲 | M | No new CRD — promote the CI-validated dogfood kata-dind and privileged-dind templates into a shipped kustomize base the e2e overlays patch, plus a plain baseline entry. Only CI-exercised templates ship. |
| <a id="Q555"></a>Q555 | [Opt-in auto-retry for flaky jobs](design/appendix-g-future-enhancements.md#g17-opt-in-auto-retry-for-flaky-jobs-beyond-disruptions) | `infra` `tests` `feature` | 🔲 | M | Extend the disruption re-run machinery to tenant-opted flaky-job retry: detection first (re-run-then-pass metrics per RunnerSet), then `spec.flakyRetry` with its own budget. Prior art and costs in G.17; next step is deciding the CRD shape. |
| <a id="Q564"></a>Q564 | [Proxy-side audit logging](design/appendix-g-future-enhancements.md#g3-proxy-side-audit-logging) | `security` `feature` | 🔲 | M | A structured line per accepted CONNECT (tenant, host:port, bytes each way, duration), off by default. Today the proxy emits counters only, so per-tenant egress is reconstructable just from cluster flow logs. Decide the opt-in field against v2. |
| <a id="Q565"></a>Q565 | [Proxy-enforced per-tenant rate limiting](design/appendix-g-future-enhancements.md#g2-proxy-enforced-per-tenant-rate-limiting) | `security` `feature` | 🔲 | M | Token bucket at the proxy so one looping tenant can be slowed before it hits GitHub's ceiling — today the only feedback is a 429 and AGC backoff. In-process state per pod; global limits would need a shared backend, out of scope. |
| <a id="Q566"></a>Q566 | [TLS between AGC/workers and the proxy](design/appendix-g-future-enhancements.md#g4-tls-between-agcworkers-and-the-proxy) | `security` `feature` | 🔲 | M | The CONNECT target host:port is cleartext on the in-cluster hop, observable to eBPF taps and shared-CNI visibility; the tunneled payload stays TLS to GitHub. Mount a cert-manager cert and move AGC/workers to an `https://` proxy URL. |
| <a id="Q567"></a>Q567 | [Per-tenant-group dedicated proxy pool](design/appendix-g-future-enhancements.md#g5-per-runnergroup-dedicated-proxy-pool) | `infra` `feature` | 🔲 | L | One pool per gateway today, so a bandwidth-heavy group can saturate a co-tenant's. Provision a dedicated Deployment/Service/HPA per opted-in group and make `HTTP_PROXY` per-group. Largest of the four — needs a plan doc before code. |
| <a id="Q568"></a>Q568 | [Grafana dashboards — the last open piece of the alerting doc](operations/observability-alerting.md) | `docs` | 🔲 | S | The other three §3.2 deliverables shipped (21 alert rules, SLO recording rules, runbook links). Panel JSON can be written against documented metric names; only validating that it renders needs a live Grafana, which dogfood can supply. |
| <a id="Q448"></a>Q448 | [Tighten the e2e NodeShare envelope against a measured allocatable](plan/runner-sizing-profiles.md#the-live-path-and-why-the-rc-gate-could-not-have-found-this) | `infra` | 🔲 | S | The declared 1500m÷1 envelope is a deliberate lower bound, so actuation can only reduce a worker's ask. Measure a live e2e node's allocatable, subtract the dind sidecar and Kata's 250m overhead, then raise it. |
| <a id="Q396"></a>Q396 | [Time eviction→GitHub-cancel and assert the rerun fires](plan/eviction-oversubscription-validation.md#experiment-1-mid-job-eviction-latency-both-tiers-q396) | `tests` `docs` | 🔲 | S | Classic tier measured 2026-07-29: 9m36s eviction→conclusion, unconfounded; its refused re-run shipped fixed (Q503 — verify live on the next run). Remaining: the scale-set half, where neither latency nor the refusal is measured. |
| <a id="Q544"></a>Q544 | [Confirm the Q495 run-identity annotation on a real worker at live-GitHub](plan/q459-drained-worker-recovery.md#status) | `tests` | 🔲 | S | The 2026-07-29 runs predate the fix (images from 719e67f1): every worker matched by the snapshot fallback, none by the annotation — the unfixed build's behaviour. Classic drain recovery rests on it; check `run-id` on the next credentialed run. |
| <a id="Q522"></a>Q522 | [Consolidate GMC PKI/cert generation helpers](../cmd/gmc/internal/controller/shared_pki.go) | `infra` | 🔲 | M | `generateEgressProxyCert` copies `generateProxyCert`; `generateMetricsCertsV2` copies `generateMetricsCerts`; both re-implement `randSerial`/`encodeCertPEM` from `shared_pki.go`; the renewal reason ladder repeats at 5 sites. |
| <a id="Q523"></a>Q523 | [GMC controller structural dedup batch](../cmd/gmc/internal/controller) | `infra` | 🔲 | M | `applyNamespacePSA` ×2, `tracingEnv` ×2, egress-stale eval ×2, roleRef-recreate appliers ×3, pod-scheduling copies ×2, `agc_vpa` re-inlines `applyManagedChild` (return the op result to share). |
| <a id="Q524"></a>Q524 | [Merge scaleset `svcCall`/`RawServiceCall` request builders](../scaleset/client.go) | `infra` | 🔲 | S | Both build admin-JWT service requests end-to-end (URL join, api-version, headers, Do, ReadAll, observe) and differ only in return shape and status mapping. Extract one `serviceRequest` used by both. |
| <a id="Q538"></a>Q538 | [Broker session `ownerName` is not kind-scoped](../broker/brokertest/server.go) | `tests` `infra` | 🔲 | S | Both kinds own a session as `<CR name>-<index>`; the Q466 kind split never reached it, so `ActiveSessionsForOwner` cannot separate a same-named RunnerGroup/RunnerSet and its doc still says owner = RunnerGroup. Found by Q535's test. |
| <a id="Q525"></a>Q525 | [Test-helper dedup batch flagged by the restraint review](../cmd/agc/internal/provisioner/provisioner_test.go) | `tests` | 🔲 | M | agc: goleak ignore-list ×3, seeded-pods ×5, podwaiter startWait ×14, RG fixture preamble ×14, prefix pod-waits ×4. gmc: captureSink→logtest, webhook table-tests, e2e dispatch preamble ×3, waitForPodGone ×4. |
| <a id="Q526"></a>Q526 | [Review dead v1 ActionsGateway API surface](../cmd/gmc/api/v1alpha1/actionsgateway_types.go) | `infra` `docs` | 🔲 | S | `ActiveSessions` is never written or read; `SecretReference.Namespace` is "reserved" but the webhook rejects non-empty. Served-API compat: decide deprecate/document vs drop at v2. |
| <a id="Q573"></a>Q573 | [Detect served API fields nothing reads](../api) | `infra` `tests` `retro` | 🔲 | S | `sharing.allowedNamespaces` shipped served-but-unenforced in v2beta1; only a manual docs sweep caught it (Q166 schedules the impl). Gate on spec fields with no reference outside generated/conversion code; complements [Q526](#Q526). |
| <a id="Q541"></a>Q541 | [Test helper claims to isolate a watch trigger and does not](../cmd/gmc/internal/controller/integration/suite_integration_test.go) | `tests` `retro` | 🔲 | S | `startEgressProxyReconcilerNoResync` says it proves a reconcile came from a watch, but a not-ready pool short-requeues every 15 s too — a Q506 test passed with both `Watches` deleted. State both sources; Q326 drives the pool Ready for this reason. |
| <a id="Q581"></a>Q581 | [The dogfood teardown and setup scripts have no test coverage](../scripts/dogfood/e2e-stop.sh) | `tests` `infra` | 🔲 | S | `e2e-start.sh` now covered (Q578); `e2e-stop.sh`, `e2e-setup.sh`, `setup.sh`, `delete.sh` still have none. Teardown inverts the risk: a drain read as converged strands billable nodes. |
| <a id="Q543"></a>Q543 | [Re-check concurrent work — `origin/main` and open PRs — before opening or merging](../CONTRIBUTING.md) | `docs` `infra` `retro` | 🔲 | S | Mandated at session start only; Q506 cost a late rebase. Concurrent PRs merge individually-green and jointly-red (Q560/Q558). One opened mid-session can change what yours should say — Q577 vs #1093. |
| <a id="Q532"></a>Q532 | [Enforce `BUILD_TAGS` ↔ `.golangci.yml` build-tags sync](development/testing.md#the-build-tag-gate) | `tests` `infra` `retro` | 🔲 | S | testing.md claims the vet-tags coverage assertion catches a tag missing from lint; it only checks go-vet-tags.sh's own list — `autoscaler` was absent from `.golangci.yml` from Q474 until Q479. Assert the two lists match under `make scripts-test`. |
| <a id="Q574"></a>Q574 | [One gate for every path list the workflows write twice](development/testing.md#a-path-list-written-twice-the-trigger-and-the-filter) | `infra` `retro` | 🔲 | M | Three shapes, three bespoke checks: shared paths across two `filters:` (Q533), `on.push.paths` vs its filter (gated), `pull_request.paths` vs `push.paths` (ungated). One registry replaces all three. |
| <a id="Q584"></a>Q584 | [Replace `check-path-filters.sh`'s hand-rolled YAML parsing with a real parser](development/go-workspaces.md#first-party-go-tooling-stays-outside-the-workspace) | `infra` | 🔲 | M | Measured 2026-08-01: the awk parsers accept only `filters: \|` exactly, so a valid reformat (`\|-`) emits 12 errors naming patterns already present. False red, not the false green first claimed — assertion 2 is registry-driven and fails closed. |
| <a id="Q534"></a>Q534 | [Rule: an upstream-behavior claim cites a measurement, in docs and in code comments](development/documentation-standards.md) | `docs` `retro` | 🔲 | S | Refuted twice: "Karpenter sets eventTime" (Q479) and "re-assigned or timed out server-side" (Q270's comment, Q551) both shipped from source inspection. One rule, both homes — a claim cites a measured version+date or the gate pinning it. |

---

## Deferred

Intentionally parked items. These carry **no priority position** and are **not** picked from the top of the Queue — each waits on an explicit trigger before it returns to active work. When an item's trigger fires, move its row back into the Queue at the position it then deserves.

Each trigger is tagged by source: **Demand:** an outside operator/user ask · **Event:** an observable outside-our-control condition · **Decision:** our own call (we're the blocker; grep `**Decision:**` for what we could move on unilaterally).

| ID | Item | Labels | Sz | Trigger to revive |
|---|---|---|---|---|
| <a id="Q530"></a>Q530 | [Isolate concurrent live-GitHub runs instead of forbidding them](plan/q511-live-github-run-isolation.md#the-decision-forbid-concurrency) | `tests` `infra` | L | **Decision:** the singleton rule (enforced by Q511's preflight) is costing more than parallel live runs are worth. Needs `actions-gateway/gateway-test`'s fixture workflows to take `runs-on` as an input — names alone do not isolate routing. |
| <a id="Q397"></a>Q397 | [`validate-cluster.sh` metrics-server retry — confirm on a from-zero bootstrap](plan/gke-dogfood.md#recreate-is-proven-end-to-end-q380) | `infra` | S | **Event:** the next dogfood recreate bootstraps from zero. The bounded retry ships and is unit-tested against faked probes; confirm the preflight prints `[WAIT]` then `PASS` inside the addon's ~2 min window, not WARN. |
| <a id="Q418"></a>Q418 | [Fast-release evicted jobs instead of waiting out the lock](plan/scaleset-eviction-recovery.md#phase-3-fast-release-if-phase-1-justifies-it) | `speed` `infra` | M | **Event:** [Q396](#Q396)'s benchmark attributes the eviction→conclusion delay to something the AGC can short-circuit. Probe deregistration first; `cmd/probe` has the scale-set harness. |
| <a id="Q424"></a>Q424 | [Utilization delta with priority tiers off vs on](plan/eviction-oversubscription-validation.md#experiment-5-utilization-delta-q424-deferred) | `tests` `docs` | L | **Event:** dogfood carries a workload steady enough for a fixed-window comparison to mean anything. Measures occupancy both ways: the packing-density number the write-up is missing. |
| <a id="Q416"></a>Q416 | [Classic listener: session heal leaks then self-collides](plan/runner-sizing-profiles.md#the-20-sample-paths-reached-2026-07-25-second-session) | `bug` `infra` | S | **Demand:** a Classic operator reports listener slots going quiet, OR Classic removal slips. `healSession` DELETEs before refreshing the token → 401 → leak → `CreateSession` 409 → goroutine exits; only the baseline restarts. |
| <a id="Q372"></a>Q372 | [Re-run the structural debt audit](plan/structural-debt-audit-2026-07.md) | `infra` | M | **Event:** the next minor release is cut, OR non-test Go LOC grows ≥20% over the 41,011-line baseline (2026-07-20) — whichever first. Growth, not calendar time, tracks drift; linters catch ~2 of 10 findings, so the sweep stays necessary. |
| <a id="Q354"></a>Q354 | [Raise e2e/tested K8s floor 1.35 → 1.36](../.github/workflows/e2e-reusable.yml) | `infra` `tests` | S | **Event:** GKE regular channel reaches 1.36, OR we want a 1.36-only feature (explicit floor-raise), OR kind stops shipping 1.35 node images. Then bump KIND_NODE_IMAGE digest + refresh the "verified on k8s 1.35" operations-doc claims. |
| <a id="Q174"></a>Q174 | [v2 bring-your-own proxy TLS certificate](plan/v2-api.md#deferred-out-of-the-critical-path) | `infra` `security` `feature` | M | **Demand:** an operator with managed PKI/Vault wants to supply the proxy cert instead of GMC's self-signed default. Add certificateSecretRef on EgressProxy. Invariant: same-namespace TLS Secret, no cross-tenant reuse. Additive; design goal 6. |
| <a id="Q169"></a>Q169 | [AGC horizontal scaling / multi-replica HA](design/appendix-e-capacity-planning.md) | `infra` `feature` | L | **Event:** a single per-tenant AGC becomes a measured bottleneck or a SPOF concern (near the ~1000-session ceiling). Single-replica with an in-memory session registry by design; real HA needs distributed session state. |
| <a id="Q15"></a>Q15 | [gVisor RuntimeClass validation](plan/milestone-5.md) | `milestone` `security` `feature` | S | **Demand:** an operator wants lightweight (non-VM) syscall-filtering for compute-only, non-DinD CI jobs — likeliest on GKE, where gVisor is first-party. Kata (Q224) already covers the DinD case, the primary sandboxing motivation here. |
| <a id="Q11"></a>Q11 | [Ed25519 live probe — M-11b](plan/security.md) | `security` `tests` | S | **Decision:** the broker swaps RSA-OAEP session-key delivery for X25519 ECDH ([Q351](#Q351)), making Ed25519 the *secure* default. Until then it's a less-secure opt-in (loses the AES session-key layer); RSA-3072 stays the default. |
| <a id="Q361"></a>Q361 | [CI latency round 2: lint + coverage module loops](plan/archive/unit-tests-speed.md#2026-07-20-re-baseline-q17-revival) | `speed` `infra` | M | **Decision:** CI latency is again the bottleneck (self-set threshold). Critical path is lint (~230 s per-module golangci-lint loop); coverage ~140 s. Levers in the linked re-baseline. |
| <a id="Q351"></a>Q351 | [X25519 ECDH session-key exchange](design/appendix-g-future-enhancements.md#g6-x25519-ecdh-session-key-exchange) | `security` | M | **Decision:** we swap RSA-OAEP session-key delivery for X25519 ECDH (Appendix G.6) — makes Ed25519 the *secure* default and unblocks [Q11](#Q11). Until then RSA-3072/RSA-OAEP stays the secure default. |
| <a id="Q70"></a>Q70 | Flip worker-image trivy leg to blocking | `security` `infra` | S | **Event:** upstream `actions-runner` base scans clean. The worker leg of [security-scan.yml](../.github/workflows/security-scan.yml) is report-only (~36 upstream HIGH/CRITICAL CVEs); when a bump clears them, set its `exit-code` to `1`. |
| <a id="Q198"></a>Q198 | [Quantified benchmark / case study](index.md) | `docs` | M | **Decision:** a paid scale run is funded (or Q181 real-cluster data exists) so real-GitHub-at-scale numbers can back a published case study. Needs ~$10–30 ephemeral cluster + real GitHub. Split from Q193 (free demo stays active). |
| <a id="Q203"></a>Q203 | [Enable Plausible analytics on the docs site](development/website.md) | `docs` | S | **Decision:** a maintainer decides to collect site traffic and provisions a Plausible site. Client wiring shipped (Q195) — set `extra.analytics.plausible_domain` in `mkdocs.yml` and redeploy; analytics sends nothing until then. |
| <a id="Q214"></a>Q214 | [SPIFFE/SPIRE workload-identity signer](plan/v2beta1.md#workload-identity-a-different-config-vault-first) | `security` `infra` `feature` | M | **Demand:** An operator wants keyless / SPIRE-based App-JWT signing. Slots behind the existing Q197 `githubapp.Signer` interface as another `signer.provider`, exactly like the deferred cloud KMS providers — additive, post-beta. |
| <a id="Q215"></a>Q215 | [Worker cache backend (actions/cache + Docker layer cache)](plan/ecosystem-integration-landscape.md#j-registry-build-cache--images-runner-workload-plane) | `infra` `feature` | L | **Demand:** a concrete ARC-parity ask for build/dependency caching. Workers are storage-less today. Add an optional PVC/object-store cache for `actions/cache` + Docker layers. Needs a plan doc + security review of cross-job cache isolation. |
| <a id="Q216"></a>Q216 | [First-class GPU runner support (GPU Operator/NFD)](design/appendix-e-capacity-planning.md) | `infra` `feature` | M | **Demand:** a concrete GPU runner workload/ask. priorityTiers already nominally carry GPU labels; first-class support adds nodeSelector/tolerations/RuntimeClass conventions + GPU Operator / NFD awareness (and Volcano gang-scheduling for multi-GPU). |
| <a id="Q217"></a>Q217 | [OLM / OperatorHub bundle](operations/install.md) | `infra` `docs` `feature` | M | **Demand:** OpenShift/OperatorHub adoption demand. Helm-only is the deliberate install stance; an OLM bundle/catalog entry waits for a concrete OperatorHub ask. Additive packaging, no core code change. |
| <a id="Q407"></a>Q407 | [Capacity gate: `ProvisioningRequest` probe](design/appendix-g-future-enhancements.md#g16-provisioningrequest-pre-acquire-capacity-probe-check-capacity) | `infra` `feature` | L | **Event:** a GPU/spot operator finds ~1 claim/window ([§9h](plan/capacity-aware-intake.md#9h-what-the-dogfood-re-run-measured-for-the-latch-q513)) too costly, OR CA runs with provisioning requests enabled, OR `check-capacity` support broadens. |
| <a id="Q268"></a>Q268 | [Warm worker pool (`minIdleWorkers`)](design/appendix-g-future-enhancements.md#g12-warm-worker-pool-minidleworkers) | `infra` `feature` | M | **Demand:** a CPU-CI team hits pod-schedule latency after pre-pull (Q211) + cache volumes (Q215) are exhausted. Opt-in per-RG idle-pod pool. Does NOT address Q224 starvation — see the [lever spike](plan/q224-fanout-dispatch-lever-spike.md). |
| <a id="Q272"></a>Q272 | [Scale-set upstream maturity watch](plan/v1-classic-sunset-review.md) | `infra` | S | **Event:** `actions/scaleset` reaches GA/v1.0 or the auto-assign contract (`actions/scaleset#107`) is documented. Not a graduation blocker (sunset review §6.1); it lifts the Public-Preview caveat and triggers the U6 vendor-vs-own client revisit. |
| <a id="Q452"></a>Q452 | [Decide whether GA `v2` defines the deprecated FQDN aliases](plan/v2-ga.md#phase-2--the-graduation-hop) | `docs` `infra` | S | **Event:** [v2-ga.md](plan/v2-ga.md) Phase 2 (the graduation hop) begins. Omit the aliases from `v2` — the hub conversion then needs a lossless carrier, since it rides them verbatim today — or carry them deprecated into a frozen GA. |
| <a id="Q413"></a>Q413 | [v2 GA graduation (`v2beta1` → `v2`)](plan/v2-ga.md) | `milestone` `infra` | L | **Event:** `v1.3.0` ships, starting the Phase 1 soak: no incompatible `v2beta1` shape change over two minors, all five kinds carrying real traffic, conversion round-trips on real objects. |
| <a id="Q273"></a>Q273 | [Complete v1 removal (full v2-only)](plan/q273-v2-front-door.md) | `docs` `infra` | M | **Event:** [v2-ga.md](plan/v2-ga.md) Phase 3 begins — the same gate as [Q264](#Q264), which it ships beside. Front door, banners, and `gag-migrate` are done; the residual `v1alpha1` + `v2alpha1` removal lands in the `v2.0.0` bundle. |
| <a id="Q264"></a>Q264 | [Remove deprecated classic acquisition machinery](plan/q264-scale-set-protocol.md) | `infra` | L | **Event:** [v2-ga.md](plan/v2-ga.md) Phase 3 begins. `v2beta1` is ScaleSet-only, so classic serves only v1alpha1/v2alpha1 objects and comes out in the same `v2.0.0` bundle, not separately. Strips `acquisitionProtocol`/`maxListeners`. |
| <a id="Q275"></a>Q275 | [Reconcile AGC capacity/density docs with the ScaleSet default](design/appendix-a-capacity-slos.md) | `docs` | S | **Decision:** classic removal proceeds on the deprecation-window schedule ([Q264](#Q264)) — reconcile alongside it. appendix-a's ≤1,000-session ceiling and README Tier 2's "thousands per AGC" are classic framing; keep the density evidence. |
| <a id="Q310"></a>Q310 | Operator diagnostic aggregator (`gag status` / kubectl plugin) | `infra` `feature` | L | **Demand:** operators ask for gateway diagnostics beyond raw kubectl + the runbook. Add a `gag status <gateway>` / kubectl plugin aggregating session, pool, and runner state per gateway. |
| <a id="Q344"></a>Q344 | First-class `scaleset` list/prune for orphan scale sets | `infra` `feature` | S | **Event:** orphan scale sets recur or an operator wants a prune path. Q334 fixed runner-*record* orphans, not stale *scale sets*; a throwaway deleted `gag-scaleset3`. Add a `scaleset` prune command. |

### Flake watch

Flakes whose mitigation has shipped and that have **not recurred since**, plus rare first sightings not yet worth fixing. The trigger to revive is the flake recurring on `main` after its fix. On recurrence, [flakes-first](development/maintaining-backlog.md#flake-fixes-go-first) pulls the row back to the **top of the Queue** — now escalated, since the first mitigation didn't hold. Kept here (not closed) so a second occurrence is recognised as a recurrence rather than a fresh find. A row that clears its [soak bar](development/maintaining-backlog.md#retiring-a-flake-watch-row) retires to the cold [retired-flake ledger](development/flake-watch-retired.md) — still greppable, at no live-table cost.

| ID | Item | Labels | Sz | Trigger to revive |
|---|---|---|---|---|
| <a id="Q570"></a>Q570 | [`E2E_Migration_MigratedTenantReconcilesIntoAWorkingControlPlane` times out when the v1 proxy pool takes both workers](../cmd/gmc/test/e2e/migration_v1_to_v2_test.go) | `flake` `tests` | S | **Event:** the migrated proxy-pool wait times out on `main` after the Q570 fix (the tenant pins its inline pool with `WithProxyReplicas(1, 1)`, so its HPA cannot take the worker the migrated pool needs). → top of Queue, escalated. |
| <a id="Q548"></a>Q548 | [Released-upgrade check dies on tar EPIPE reading the chart's values.yaml](../scripts/e2e/chart-released-upgrade-check.sh) | `flake` `infra` | S | **Event:** `tar: Cannot write: Broken pipe` fails `chart-released-upgrade-check` on `main` after the Q548 fix (the awk consumer reads the whole stream instead of exiting at first match). → top of Queue, escalated. |
| <a id="Q516"></a>Q516 | [Stale sibling-worktree lint cache fails `make check` cross-session](../scripts/go/go-lint.sh) | `infra` `flake` | S | **Event:** local lint reports findings citing a deleted `.claude/worktrees/*` path after the Q516 fix (go-lint.sh scopes `GOLANGCI_LINT_CACHE` per worktree under `tmp/`; CI keeps the default). → top of Queue, escalated. |
| <a id="Q498"></a>Q498 | [Provisioner eviction/rerun tests flake under parallel load](../cmd/agc/internal/provisioner/provisioner_test.go) | `flake` `tests` | S | **Event:** an eviction/rerun test fails again on `main` (unreproduced post-Q503: 320 runs under saturating load; #1010 made the recovery tests block on done channels). → top of Queue, escalated. |
| <a id="Q490"></a>Q490 | [`TestAGC_Q260_FanoutCompletionReconciles` cancels a job every delivery completed](development/testing.md#synchronize-on-the-signal-you-assert-on) | `tests` `flake` | S | **Event:** it fails `cancelled` vs `completed` on `main` after the Q490 fix (the stub publishes `CompleteJobCalls` only after committing the accounting, and the test waits on `DeliveryResults`). → top of Queue, escalated. |
| <a id="Q471"></a>Q471 | [`validate-cluster-test` flakes under parallel `make check` load](../scripts/e2e/validate-cluster-test.sh) | `flake` `tests` | S | **Event:** a `metrics`/`retry` assertion fails on `main` after the Q471 fix (the retry paths now read a fake clock the fake `sleep` advances, so no real elapsed time is measured and the suite never sleeps). → top of Queue, escalated. |
| <a id="Q300"></a>Q300 | [Systemic kindnet e2e leg flakiness (cross-spec)](plan/q300-gmc-kindnet-e2e-flake.md) | `tests` `flake` `infra` | M | **Event:** kindnet `e2e / e2e` red on `main` again. 07-18 escalation was misattributed (Q349, pre-fix). Dump now captures nfqueue/memory counters + full-window kindnet logs — attribute per the plan's triage table before any code change. |

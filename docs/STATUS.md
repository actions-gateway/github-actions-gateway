# Project Status

Single source of truth for progress and priorities across the full project. `docs/plan/` holds the implementation detail; this file holds the ordering and the overview. Pick the next task from the top of the [Queue](#queue).

## Conventions

**Status:** 🔲 ready · 🚫 blocked  
**Size:** S = one session · M = 2–3 sessions · L = multi-session, needs a phased plan doc in `docs/plan/`  
**Labels:** `milestone` `security` `tests` `speed` `docs` `ci` `dogfood` `debt` `feature` `bug` `flake` `retro` `1.0-gate` (blocks the [Release 1.0](plan/release-1.0.md) tag) `1.3-gate` (blocks the [Release 1.3](plan/release-1.3.md) tag) `1.4-gate` (blocks the [Release 1.4](plan/release-1.4.md) tag) `1.5-gate` (blocks the [Release 1.5](plan/release-1.5.md) tag) `2.0-gate` (blocks the [`v2.0.0`](plan/v2-ga.md) tag)  
**New IDs:** `make queue-id TITLE="…"`: it searches for near-duplicates, then claims ([why there is no counter here](development/queue-id-allocation.md))

Maintained per [`docs/development/maintaining-backlog.md`](development/maintaining-backlog.md): done rows are deleted (git is the archive), the open PR is the in-flight signal, new items enter at the priority they deserve, parked items live in [Deferred](#deferred), and every edit is an isolated `docs(status):` commit gated by `scripts/docs/lint-backlog.sh`.

---

## Progress

Plan-level view. ✅ = no open Queue row remains (intentionally-deferred residuals live in [Deferred](#deferred) and don't count against completion). ⚠️ = ≥1 open Queue row remains. See [maintaining-backlog.md](development/maintaining-backlog.md#-means-an-open-queue-row-remains--deferred-residuals-dont-count).

| Item | Labels | Status |
|---|---|---|
| [M1: Wire-protocol probe](plan/milestone-1.md) | `milestone` | ✅ |
| [M1: Unit-test coverage](plan/milestone-1-tests.md) | `milestone` `tests` | ✅ |
| [M2: AGC controller](plan/milestone-2.md) | `milestone` | ✅ |
| [M3: Worker pod](plan/milestone-3.md) | `milestone` | ✅ |
| [M4: GMC + proxy](plan/milestone-4.md) | `milestone` | ✅ |
| [M5: Hardening](plan/milestone-5.md) | `milestone` `security` | ✅ |
| [Release 1.0](plan/release-1.0.md) | `milestone` | ✅ |
| [Security hardening](plan/security.md) | `security` | ✅ |
| [Security audit 2 (2026-06)](plan/security-audit-2026-06.md) | `security` | ✅ |
| [Worker egress proxy](plan/worker-egress-proxy.md) | `security` | ✅ |
| [Docs](plan/docs.md) | `docs` | ✅ |
| [Six-layer docs audit](plan/docs-six-layer-audit.md) | `docs` | ✅ |
| [Make UX](plan/make.md) | `ci` | ✅ |
| [Docker image speed](plan/docker-image-speed.md) | `speed` | ✅ |
| [e2e test speed](plan/e2e-tests-speed.md) | `speed` `tests` | ✅ |
| [e2e CI speed round 2](plan/e2e-ci-speed-round-2.md) | `speed` `tests` `ci` | ✅ |
| [Merge queue](plan/merge-queue.md) | `speed` `ci` | ✅ |
| [v2 API decomposition](plan/v2-api.md) | `feature` | ✅ |
| [Per-module coverage ≥75%](plan/coverage-to-75-per-module.md) | `tests` | ✅ |
| [GKE dogfood](plan/gke-dogfood.md) | `dogfood` `docs` | ✅ |
| [Test progress visibility](plan/test-progress-visibility.md) | `tests` `ci` `dogfood` | ✅ |
| <a id="Q248"></a>[Dogfood runner right-sizing](plan/dogfood-runner-rightsizing.md) | `dogfood` | ✅ |
| [Release 1.3](plan/release-1.3.md) | `milestone` | ✅ |
| [v2 GA graduation](plan/v2-ga.md) | `milestone` | ✅ |
| [v1 sunset → v2-only](plan/v1-classic-sunset-review.md) | `debt` | ✅ |
| [Worker right-sizing profiles](plan/runner-sizing-profiles.md) | `feature` | ✅ |
| [Capacity-aware job intake](plan/capacity-aware-intake.md) | `feature` | ✅ |
| [Markdown gates on a real parser](plan/markdown-gates-parser.md) | `milestone` `ci` `debt` | ✅ |
| [Release 1.4](plan/release-1.4.md) | `milestone` | ⚠️ |

---

## Queue

Specific actionable items in priority order. Pick from the top; skip 🚫 items until their blocker clears. Intentionally parked items live in [Deferred](#deferred) below, out of the priority ordering.

| ID | Item | Labels | St | Sz | Notes |
|---|---|---|---|---|---|
| <a id="Q752"></a>Q752 | [`record-launch-test`'s 10s launch bound fails under the `make check` fan-out, passes standalone](../scripts/agent/record-launch-test.sh) | `flake` `tests` | 🔲 | S | Seen 3/6 `make check` runs 2026-08-08 under Q733, 0/4 under `make scripts-test` alone, 0/5 standalone: it needs full-check contention. The tree case waits 100×`sleep 0.1` for a `.launch` record. Q642/Q690's shape: bounds startup, not the subject. |
| <a id="Q747"></a>Q747 | [e2e cross-tenant NetworkPolicy probe intermittently connects instead of being blocked](../cmd/gmc/test/e2e/isolation_test.go) | `flake` `tests` `security` | 🔲 | S | Failed on 9480f29b, passed unchanged on 7a531dff (#1344). The curl pod ended Succeeded with HTTP_CODE=400, so it reached the nsB proxy rather than timing out, and the spec's own gate pod had already observed enforcement live. Mechanism unverified. |
| <a id="Q703"></a>Q703 | [Gates die mid-run under `run-parallel`, pass standalone](../scripts/ci/run-parallel.sh) | `flake` `ci` | 🔲 | S | Trigger fired 2026-08-08 twice: `claude-go-throttle-hook-test` (exit 1, empty hook output) and `record-launch-test` (died after 6 assertions), both passing standalone. The second carries Q733's errexit prologue, so that fix does not close it. |
| <a id="Q741"></a>Q741 | [GMC envtest suite exceeds its 5m timeout](development/testing.md) | `flake` `tests` | 🔲 | S | Measured 2026-08-08 on `origin/main` 70ec7925, alone: 324.5s vs `-timeout 5m`, panic, CredRotation red. Runs: 246s green, 315s, 326s. Whichever test holds the wall takes the blame, so the name moves. Split the package or raise the budget. |
| <a id="Q706"></a>Q706 | [`alloc-queue-id-test` bounds fleet startup on the wall clock](development/testing.md#two-clocks-in-one-assertion) | `tests` `retro` | 🔲 | S | Read 2026-08-05: `alloc-queue-id-test.sh:166` waits ~1000×`sleep 0.01` for the fleet, then fails `fleet startup`; it bounds the scheduler, not the subject. Q642 measured p99 469ms and 0.45% still starting at 10s. The last such site. |
| <a id="Q751"></a>Q751 | [Nothing declares the bash 4.4+ floor `inherit_errexit` makes load-bearing](development/bash-style.md#set--e-stops-at-a-command-substitution) | `debt` `tests` | 🔲 | S | Measured 2026-08-08 under Q733: 170 `scripts/` files now need `inherit_errexit` (bash 4.4+), stock macOS ships 3.2 at `/bin/bash`, and `check-tools.sh` checks no bash version. The 3 hooks fail open, the rest die confusingly. New required-tier prereq. |
| <a id="Q749"></a>Q749 | [`STATUS_GATES` claims to be complete and omits two gates a STATUS.md change can fail](../Makefile) | `ci` `debt` `retro` | 🔲 | S | Comment calls it the complete set a STATUS.md-only change can fail, but STATUS.md has an em-dash ceiling (39, at 32) and page-density scans `docs/*.md`; neither is listed. `gate-lists-check` checks only the subset direction, so nothing catches it. |
| <a id="Q742"></a>Q742 | [A whole-file em-dash ceiling lets two green PRs merge to a red `main`](development/documentation-standards.md#enforcing-the-em-dash-rule) | `ci` `bug` | 🔲 | S | Measured 2026-08-08 twice: testing.md 593 to 595 (#1340, #1342), caught; then CONTRIBUTING.md 48 to 50 (#1348, #1349), which broke `main`. Each PR sits within its ceiling on its own base, so per-PR CI cannot see the merge result. Ratchet on the diff. |
| <a id="Q738"></a>Q738 | [Local load harnesses cannot reach a 2-core runner's contention](development/testing.md#a-throwaway-load-harness-is-a-measuring-instrument-so-calibrate-it) | `tests` `retro` | 🔲 | S | Measured across this family: Q596 spent 2418 runs and Q703 240, neither reproducing a trigger seen only on CI. An 18-core Mac is the wrong shape. A `--cpus=2` container around `make scripts-test` would make the negative mean something. |
| <a id="Q743"></a>Q743 | [`doc-links.yml` has no `merge_group`, so five docs gates skip queue validation](../.github/workflows/doc-links.yml) | `ci` `retro` | 🔲 | S | Measured 2026-08-08: 9 of 25 workflows declare it; not this one, so its 5 gates never see the candidate merge. Q742 ratchets the em-dash ceiling; this covers the other 4 gates and any later ratchet. Needs Q574's paths-filter `changes` job. |
| <a id="Q750"></a>Q750 | [The broken-main exception duplicates one fix across every in-flight session](../CONTRIBUTING.md#re-check-concurrent-work-before-opening) | `ci` `debt` | 🔲 | S | Decided 2026-08-08: a main fix never rides in an unrelated PR. Search for a standalone fix; wait and rebase if one is open, own it if none is. The claim is a draft PR, which the Q668 hook reads. Measured: 4 sessions fixed one overrun; 2 collided. |
| <a id="Q754"></a>Q754 | [D.9 and alternatives.md call account-per-tenant ForgeMT's model, which its own docs do not](design/appendix-d-alternatives-considered.md#d9-forgemt-and-account-per-tenant-runner-platforms) | `docs` | 🔲 | S | Its boundary is IAM/OIDC role scope, labels, and GitHub App scope, with the account layout left to the operator and an EC2 lane for VM isolation. The disagreement is real; re-derive both trade-off bullets from it, and re-date the star count. |
| <a id="Q760"></a>Q760 | [The GPU plan calls AMD the same shape of problem as TPUs and Gaudi; the cost model disagrees](plan/gpu-and-accelerated-ci.md) | `docs` | 🔲 | S | AMD has no gVisor support and no Kata path (Kata's kernel build rejects the vendor), so isolated AMD CI means VM-per-job via VFIO: a different product shape, not a bigger one. Appendix F sells its memory-per-dollar without that caveat. |
| <a id="Q755"></a>Q755 | [D.3 reads as though ARC cannot express scheduling priority at all, and its template can](design/appendix-d-alternatives-considered.md#d3-actions-runner-controller-arc) | `docs` | 🔲 | S | `AutoscalingRunnerSetSpec.Template` is a full PodTemplateSpec, so `priorityClassName` is settable per scale set. The real gaps are sharper: ordering yes, reserved floor no (needs per-index variation), shared ceiling no. |
| <a id="Q756"></a>Q756 | [Appendix B's overhead figures are unscoped, and the CI-relevant cost is a configuration question](design/appendix-b-worker-isolation.md#b2-operational-cost) | `docs` | 🔲 | S | Kata's `+1-3s` is a QEMU number (Firecracker boots in ~125ms); the gVisor memory and startup figures are uncited. Add prose, not a table row: no rigorous current head-to-head exists, and devmapper vs virtio-fs swings more than the runtime does. |
| <a id="Q758"></a>Q758 | [The AWS nested-virt framing predates EC2 shipping it on virtual instances](design/appendix-b-worker-isolation.md#b2-operational-cost) | `docs` | 🔲 | S | AWS launched it on virtual instances 2026-02-16 (C7i/C8i, M7i/M8i, R7i/R8i, X8i, I7i and flex variants), so "requires bare metal" is stale in B.2 and in kata-dind-workloads.md. No P or G family is on the list, so the GPU argument is unaffected. |
| <a id="Q757"></a>Q757 | [D.5 attributes a cluster-admin install requirement to Kueue that Kueue does not document](design/appendix-d-alternatives-considered.md#d5-kueue-and-kubernetes-job-queue--quota-managers) | `docs` | 🔲 | S | The conclusion holds; the sourcing does not. Claim the checkable thing: the chart ships CRDs, ClusterRoles and bindings, admission webhook configurations, and APIServices, so installing it needs cluster-scoped permissions. |
| <a id="Q759"></a>Q759 | [D.8's capacity-signal gating principle reaches no surface outside Appendix D](design/appendix-d-alternatives-considered.md#d8-gating-intake-on-capacity-which-signals-are-safe-to-gate-on) | `docs` | 🔲 | S | Safe to gate on only if no other actor waits on that signal to make capacity appear: it generalises to any admission controller above an autoscaler. Evaluate promoting to why-gag.md or the under-claims list; keep the table. |
| <a id="Q408"></a>Q408 | [Untrusted-PR egress posture for Kata workers](plan/q408-untrusted-pr-egress.md) | `security` | 🔲 | L | Phase 1 (self-hosted lane drops `get.helm.sh` + the Actions-cache data plane; hosted lane keeps caches) awaits its validation run: green Kata e2e whose log names no non-GitHub non-registry host. Then Phase 2 mirror manifests. |
| <a id="Q595"></a>Q595 | [The cancel runbook's manual pod delete may re-run the job GitHub cancelled](operations/troubleshooting.md#cancelling-a-run-does-not-stop-its-worker-pod) | `bug` `docs` | 🔲 | S | A hand-deleted worker matches `externallyDeletedBeforeTerminal` (source-read, unmeasured), so Q502 calls `rerun-failed-jobs` for a run an operator deliberately stopped. Measure whether GitHub honours it for a `cancelled` conclusion. |
| <a id="Q631"></a>Q631 | [The release gate starves itself: routed CI and the e2e leg draw on one global CPU budget](plan/gke-dogfood.md#part-f--e2e-on-gke-kata-containers) | `dogfood` `debt` | 🔲 | S | Deploy routes CI to GAG, scaling `workers` to 8 (32 vCPU) out of the same `CPUS_ALL_REGIONS` the e2e pool then needs. Cost two rc.5 runs at the old limit of 32. Raised to 64, so 52 worst case now fits — but nothing caps the competition. |
| <a id="Q682"></a>Q682 | [Measure sibling `skipped` completion semantics live (Q260 fan-out)](plan/q645-abandoned-completion.md#the-remedy) | `tests` | 🔲 | S | Inv. H covered only the winner's own delivery. The default-on sibling fan-out sends `skipped` mid-run; whether it concludes anything is unmeasured (brokertest assumes not). Needs a two-delivery probe arm. |
| <a id="Q637"></a>Q637 | [The release gate asserts nothing about the capacity gate or the namespace-quota rung](../scripts/dogfood/validate-release.sh) | `dogfood` `tests` | 🔲 | M | `validate-release.sh` has zero references to `capacityGate`, `nodeAutoscaling` or quota; its legs are deploy, e2e, sizing, CRD smoke. It builds an autoscaling cluster with headroom, so the constrained path is never driven on real infra. |
| <a id="Q707"></a>Q707 | [Ownership lease for local singleton tiers (e2e/kind slots)](development/testing.md#stopping-a-run-name-the-target-never-the-program) | `debt` `tests` | 🔲 | M | Measured: of 38 local shell-kill targets only 2 carried a worktree anchor; the rest match every session (Q690 killed the `make check` verifying it). e2e/kind slots are singletons with no ownership record, so reclaim guesses. Copy `lib/lease.sh`. |
| <a id="Q401"></a>Q401 | [File upstream k8s issue: delete-path probe results never reach `Ready`](operations/node-shutdown-budgets.md) | `docs` | 🔲 | S | Q388 measured: on ordinary delete the probe fails throughout termination but `Ready` never flips; removal comes from `deletionTimestamp`. k8s#124648 covers only eviction. Head of the upstream-report batch; three of four remain (Q426 filed). |
| <a id="Q402"></a>Q402 | [GKE DPv2 NodeLocal DNSCache NP repro — file against Google's docs](operations/troubleshooting.md) | `docs` | 🔲 | S | Google's NodeLocal DNSCache page names DPv2 as exempt from extra NP rules; Q229 measured the opposite (node-local-dns peer required). Capture a minimal repro w/ cluster version and report it — a default-on footgun on Autopilot. |
| <a id="Q531"></a>Q531 | [File upstream: karpenter kwok chart panics on its own defaults](plan/capacity-aware-intake.md#9i-the-karpenter-arm-of-the-drift-gate-and-what-it-measured-q479) | `docs` | 🔲 | S | At v1.14.0 the chart renders `featureGates.staticCapacity`/`.capacityBuffer` into `FEATURE_GATES` but values.yaml omits both keys; the empty string panics the controller at startup. Q479's harness sets them explicitly as a workaround. |
| <a id="Q539"></a>Q539 | [Validate Kata + Dragonfly as the mirror backend](plan/q408-untrusted-pr-egress.md#6-follow-on-validations-q539-q540) | `security` | 🚫 | M | Blocked by [Q408](#Q408): validate the posture first. Dragonfly passes the mirror contract only via seed-peer Service, upstream allowlist, refused non-GET, workers never peers. Grade those [per content class](plan/caching-and-worker-storage.md). |
| <a id="Q540"></a>Q540 | [Milestone: Kata + Dragonfly (node layer) + pull-through cache (guest layer)](plan/q408-untrusted-pr-egress.md#6-follow-on-validations-q539-q540) | `security` | 🚫 | M | Blocked by [Q539](#Q539). Validate the composed stack: node-level P2P for pod images, in-guest mirror for job pulls; confirm the P2P mesh stays unreachable from worker pods. One deployment for both classes? [scope](plan/caching-and-worker-storage.md) |
| <a id="Q695"></a>Q695 | [No alert covers a capacity gate throttling intake](../deploy/monitoring/prometheusrule.yaml) | `docs` `debt` | 🔲 | S | Measured 2026-08-05: none of the 20 alerts reference `worker_capacity_declined`, `jobs_admission_rejected_total`, or `scaleset_capacity_withheld`, so a gated set throttling intake is invisible. Q658 settled that `AwaitingProbe` alone must not alert. |
| <a id="Q704"></a>Q704 | [Preview series below 1/WAIT render as permanent flat zeros](../deploy/monitoring/preview/exporter.py) | `docs` `debt` `retro` | 🔲 | S | Measured 2026-08-05: at WAIT=660, `int(rate*elapsed)` truncates to 0 for 5 rates under 0.002, so 6 series never move. "Quota retries (1h)" is empty in the shipped shot. Same class as Q651. |
| <a id="Q701"></a>Q701 | [No doc records how to hold the scale-set poll loop out of its own poll](development/testing.md#synchronize-on-the-signal-you-assert-on) | `tests` `docs` `retro` | 🔲 | S | Two techniques, and picking wrong costs a session: `parkInPoll` (Q603) fails when the event under test wakes parked polls, as `CompleteAssignedJob` does, so Q689 blocked in Provision instead. Only the archived Q603 plan names either. |
| <a id="Q702"></a>Q702 | [A flake row whose investigation measured a production defect does not link it](development/maintaining-backlog.md#flake-fixes-go-first) | `docs` `retro` | 🔲 | S | Q685 and Q689 were one defect from two sides, and Q689's only measurement sat inside Q685's `testing.md` paragraph. Nothing connected them; a dispatcher had to ask. Rule: cross-link the pair, and say which row owns the measurement. |
| <a id="Q739"></a>Q739 | [Nothing makes `record-launch.sh` fire, so the handle it writes is optional](development/testing.md#the-launch-record) | `debt` `retro` | 🔲 | S | Q709 shipped the record but not the trigger: nothing reaches for `record-launch.sh`, so a run launched without it has no handle after a compaction. Candidate seam: foreground-guard `hint`, unverified. |
| <a id="Q694"></a>Q694 | [`pr-requeue-eligible`'s timeline and GraphQL reads are untested behind the `gh` stub](../scripts/agent/pr-requeue-eligible-test.sh) | `tests` `retro` | 🔲 | S | The checker gates an unattended merge, but the stub returns post-`--jq` text, so a malformed `--jq` in `human_enqueued` or `in_queue` fails as a live crash, not a red suite. Only `gh pr view`'s read has run against a real PR. |
| <a id="Q693"></a>Q693 | [`find-duplicate-rows` cannot see a limitation already documented in `docs/` prose](development/maintaining-backlog.md#search-before-you-file) | `docs` `debt` `retro` | 🔲 | S | Searches STATUS.md's three tables only, so "known and accepted" is invisible: Q692's session nearly filed the merge-driver gap this page already states. Grep the page a row links, or say so in the rule. Distinct from [Q686](#Q686), doc-link anchors. |
| <a id="Q686"></a>Q686 | [`check-doc-links` is blind to a section named in prose](development/documentation-standards.md#conventions) | `docs` `ci` `retro` | 🔲 | S | `[file.md](file.md) § Heading` renders like the anchored form but nothing checks the section name: verbatim when written, silently wrong after a rename. 11 in the tree vs 1,051 anchored. |
| <a id="Q740"></a>Q740 | [Ship a signed build-capable runner image for the DinD templates](operations/runner-template-library.md) | `feature` `docs` | 🔲 | M | Q554 ships `kata-dind`/`privileged-dind` with an `example.invalid` `workerImage`: the runner needs a docker CLI and no image we publish has one. Ship actions-runner + CLI + buildx, signed, SBOM, digest-pinned, and drop the placeholder. |
| <a id="Q737"></a>Q737 | [Align the shipped DinD templates on one docker:NN-dind version](../deploy/templates/README.md) | `docs` `debt` | 🔲 | S | Q554 shipped `privileged-dind` on `docker:27-dind`, `kata-dind` on `docker:28-dind`: what CI exercises. As operator-facing artifacts the split reads as an accident. Bumping 27 changes what the dogfood e2e runs, so it needs a green e2e leg. |
| <a id="Q688"></a>Q688 | [15 test scripts have no `scripts/README.md` mention, blocking a coverage gate](../scripts/README.md) | `docs` `debt` | 🔲 | S | Measured 2026-08-04 under Q660: 15 `*-test.sh` across agent/, ci/, docs/, fetch/, go/ appear nowhere in the README. The convention names a test in its subject's row, applied unevenly. |
| <a id="Q698"></a>Q698 | [The delete-path inventory misses a `Delete` whose context arg is named unconventionally](../cmd/agc/internal/provisioner/deletion_inventory_test.go) | `tests` `debt` `retro` | 🔲 | S | `isContextArg` keys on a first arg named `ctx`-ish or a `context.*` call, so `Client.Delete(c, pod)` is skipped silently: fail-open inside a fail-closed gate. Widen to any 2+ arg `.Delete(`, letting the inventory classify the noise. |
| <a id="Q569"></a>Q569 | [Split Deferred + Flake watch off `STATUS.md`](plan/docs-page-boundaries.md#phase-2-splitting-the-parked-register-q569) | `docs` | 🔲 | M | Phase 2 of the page-boundary split: the Queue is read top-down to pick work; the parked rows under it are only grepped on a trigger. Needs `check-roadmap.sh` given a second path, rule 8 spanning two files, `#QNNN` links retargeted. |
| <a id="Q712"></a>Q712 | [Pin each scale set to a named GitHub runner group](plan/release-1.5.md#q712--the-runner-group-binding-is-declared-and-never-wired) | `security` `feature` `1.5-gate` | 🔲 | M | Declared at `scalesetlistener/listener.go:372`, resolved at `:690`, never set at the sole production call site, so every scale set lands in the default runner group — GitHub's authorization point for which repos may target a tenant's runners. |
| <a id="Q716"></a>Q716 | [Assert a worker pod cannot reach the cloud metadata server](design/05-security.md) | `security` `tests` | 🔲 | S | Three docs advise denying `169.254.169.254/32` and Q226 measured HTTP 200 from inside a Kata guest, but no test names the address. Kata bounds the kernel, not the pod network. Add a policy-enforcing-CNI negative assertion. |
| <a id="Q715"></a>Q715 | [Detect the runner version a tenant's worker image actually ships](operations/tenant-onboarding.md) | `bug` `security` | 🔲 | M | `agent.version` reports the global `GITHUB_RUNNER_VERSION` whatever `spec.workerImage` is, and `RunnerVersionTooOld` fires classic-only, so GitHub's enforced-minimum bump (GHEC 2026-09-25) lands on the shipped tier with no signal. |
| <a id="Q714"></a>Q714 | [Gate intake on workers that schedule but never start](plan/capacity-aware-intake.md) | `bug` `feature` | 🔲 | M | `podUnschedulable()` keys on `PodScheduled=False`, so an `ImagePullBackOff` worker trips no rung: it binds, never starts, and each claim spends a JIT record and holds a lock to `pendingPodDeadline`. Measured; the 1.4 DinD placeholder hits it. |
| <a id="Q726"></a>Q726 | [Multi-label runner sets for `runs-on` array targeting](operations/migration-from-arc.md) | `feature` | 🔲 | M | ARC added multi-label scale sets in 0.14.0 (2026-03-19); a `v2beta1` RunnerSet is CEL-rejected above one `runnerLabel`. A workflow using `runs-on: [linux, gpu]` needs an edit per target to migrate, reversing the zero-edit claim. |
| <a id="Q727"></a>Q727 | [A non-privileged path for `container:` and `services:` steps](operations/in-runner-image-builds.md) | `feature` | 🔲 | L | ARC's `containerMode: kubernetes` runs them as separate pods with a provisioned volume. GAG runs one worker pod per job, so the path is DinD, unprivileged only under Kata. Needs a design: broker step pods, or document Kata DinD as the answer. |
| <a id="Q719"></a>Q719 | [Validate RWX persistent storage for worker jobs, and put it in the reference architecture](operations/README.md) | `docs` `tests` | 🔲 | M | Workers are storage-less and nothing validates a `ReadWriteMany` volume mounted into one. RWX is how jobs share files, and what ARC's `containerMode: kubernetes` needs, so it is also a migration blocker. Validate, then document the stance. |
| <a id="Q717"></a>Q717 | [Fold the scale-up token bucket into the advertised capacity](plan/release-1.5.md#candidates-not-yet-accepted) | `debt` `feature` | 🔲 | M | Waited on at `provisioner.go:532`/`:793`, after the claim, with the job holding its GitHub lock (the CRD godoc says so). A fourth `min()` rung in `AdvertiseCapacity` would never assign the burst in the first place. |
| <a id="Q722"></a>Q722 | [Re-measure per-session AGC memory against the scale-set listener](design/appendix-a-capacity-slos.md) | `tests` `docs` | 🔲 | S | The ~12 KiB figure on four marketing surfaces comes from `mem_test.go`, which imports classic `agentpool`/`listener`, never `scalesetlistener`. The pod-count claim is unaffected, and the sunset review suggests re-measuring strengthens it. |
| <a id="Q721"></a>Q721 | [Surface advertised capacity and its withheld breakdown in `RunnerSet` status](operations/troubleshooting.md) | `feature` `docs` | 🔲 | S | `adv.Withheld` reaches Prometheus only, so a tenant without metrics access cannot see why intake is throttled. Add `advertisedCapacity` and a per-reason withheld list to `RunnerSetStatus`. Distinct from Q695's alert gap. |
| <a id="Q724"></a>Q724 | [Budget-owner cost and utilization dashboard](operations/personas.md#budget-owner) | `feature` `docs` | 🚫 | M | Two dashboards serve two of the five documented personas. `cost-attribution.md` maps namespaces to OpenCost/Kubecost for the budget owner, with no dashboard on top. Blocked by [Q713](#Q713): utilization panels need the missing series. |
| <a id="Q725"></a>Q725 | [Security and compliance dashboard for the audit persona](operations/personas.md#security--compliance) | `feature` `security` | 🚫 | M | The Security persona needs evidence produced unprompted: per-tenant egress attribution, admission rejections, abuse signals. Blocked by [Q564](#Q564): the proxy emits counters, not a record per connection. |
| <a id="Q720"></a>Q720 | [Publish the queue depth GitHub reports on every poll](design/03-api-contracts.md) | `feature` `docs` | 🔲 | S | Every poll carries `totalAvailableJobs`; the listener stores the struct and `Status()` exports only assigned plus running. Export it as a gauge so an operator can answer why a job sits queued, the top demand signal. |
| <a id="Q713"></a>Q713 | [Emit job duration and pod-creation latency on the scale-set tier](plan/release-1.5.md#q713--the-shipped-tier-emits-no-duration-or-latency-series) | `bug` `debt` `1.5-gate` | 🔲 | M | `waitForCompletion` and the pod waiter run only in `provision()` (`provisioner.go:557`/`:583`); `ProvisionScaleSetWorker` registers neither, so the shipped tier emits both series empty: two SLOs, a critical alert and four recording rules read blank. |
| <a id="Q728"></a>Q728 | [Let pin-bearing docs cite a third-party version](development/documentation-standards.md) | `docs` `debt` | 🔲 | S | `check-release-pins.sh` reads every bare `X.Y.Z` in its five pin files as a GAG pin, so `README.md` cannot name the ARC version a measurement used. The [pre-flight](operations/release.md#1-pre-flight) asks for that re-measurement every release. |
| <a id="Q555"></a>Q555 | [Opt-in auto-retry for flaky jobs](design/appendix-g-future-enhancements.md#g17-opt-in-auto-retry-for-flaky-jobs-beyond-disruptions) | `tests` `feature` | 🔲 | M | Extend the disruption re-run machinery to tenant-opted flaky-job retry: detection first, then `spec.flakyRetry` with its own budget. Detection needs a real job outcome, which only the unread exit code carries; G.17 has that, the channels, the costs. |
| <a id="Q564"></a>Q564 | [Proxy-side audit logging](design/appendix-g-future-enhancements.md#g3-proxy-side-audit-logging) | `security` `feature` | 🔲 | M | A structured line per accepted CONNECT (tenant, host:port, bytes each way, duration), off by default. Today the proxy emits counters only, so per-tenant egress is reconstructable just from cluster flow logs. Decide the opt-in field against v2. |
| <a id="Q565"></a>Q565 | [Proxy-enforced per-tenant rate limiting](design/appendix-g-future-enhancements.md#g2-proxy-enforced-per-tenant-rate-limiting) | `security` `feature` | 🔲 | M | Token bucket at the proxy so one looping tenant can be slowed before it hits GitHub's ceiling — today the only feedback is a 429 and AGC backoff. In-process state per pod; global limits would need a shared backend, out of scope. |
| <a id="Q566"></a>Q566 | [TLS between AGC/workers and the proxy](design/appendix-g-future-enhancements.md#g4-tls-between-agcworkers-and-the-proxy) | `security` `feature` | 🔲 | M | The CONNECT target host:port is cleartext on the in-cluster hop, observable to eBPF taps and shared-CNI visibility; the tunneled payload stays TLS to GitHub. Mount a cert-manager cert and move AGC/workers to an `https://` proxy URL. |
| <a id="Q567"></a>Q567 | [Per-tenant-group dedicated proxy pool](design/appendix-g-future-enhancements.md#g5-per-runnergroup-dedicated-proxy-pool) | `feature` | 🔲 | L | One pool per gateway today, so a bandwidth-heavy group can saturate a co-tenant's. Provision a dedicated Deployment/Service/HPA per opted-in group and make `HTTP_PROXY` per-group. Largest of the four — needs a plan doc before code. |
| <a id="Q568"></a>Q568 | [Grafana dashboards — the last open piece of the alerting doc](operations/observability-alerting.md) | `docs` | 🔲 | S | The other three §3.2 deliverables shipped (21 alert rules, SLO recording rules, runbook links). Panel JSON can be written against documented metric names; only validating that it renders needs a live Grafana, which dogfood can supply. |
| <a id="Q657"></a>Q657 | [Repeat the eviction measurement now that the spec proves which worker it disrupted](plan/eviction-oversubscription-validation.md#the-17s-outlier-is-unattributable-q657-2026-08-03) | `tests` | 🔲 | S | Three clean runs say lock TTL (9m36–9m45s); the lone 17s run predates the `runner_name` identity check, so "not reliably ungraceful" is unsupported, not refuted. Repeat with the check to see whether a fast conclusion recurs. |
| <a id="Q662"></a>Q662 | [foreground-guard registers the fast e2e targets as 30-minute commands](../.claude/foreground-guard.json) | `debt` | 🔲 | S | Every `e2e-*` target is registered at 30 min; only `make e2e`/`e2e-up` are slow (`e2e-github-cleanup` takes seconds). `foreground-guard-patterns-test.sh` asserts the fast ones as must-match, so narrowing means flipping them. Runtimes unmeasured. |
| <a id="Q522"></a>Q522 | [Consolidate GMC PKI/cert generation helpers](../cmd/gmc/internal/controller/shared_pki.go) | `debt` | 🔲 | M | `generateEgressProxyCert` copies `generateProxyCert`; `generateMetricsCertsV2` copies `generateMetricsCerts`; both re-implement `randSerial`/`encodeCertPEM` from `shared_pki.go`; the renewal reason ladder repeats at 5 sites. |
| <a id="Q523"></a>Q523 | [GMC controller structural dedup batch](../cmd/gmc/internal/controller) | `debt` | 🔲 | M | `applyNamespacePSA` ×2, `tracingEnv` ×2, egress-stale eval ×2, roleRef-recreate appliers ×3, pod-scheduling copies ×2, `agc_vpa` re-inlines `applyManagedChild` (return the op result to share). |
| <a id="Q677"></a>Q677 | [`CreateSession` sends a kind-unscoped runner name](../cmd/agc/internal/listener/session.go) | `bug` | 🔲 | S | It derives `<CR name>-<index>` for both kinds, but Q466 kind-scoped the *registered* name to `rs-<set>-<index>`, so a RunnerSet's `agent.name`/`ownerName` name no registered runner. Broker reaction unmeasured; it validates `agent.id`. From Q538. |
| <a id="Q525"></a>Q525 | [Test-helper dedup batch flagged by the restraint review](../cmd/agc/internal/provisioner/provisioner_test.go) | `tests` | 🔲 | M | agc: goleak ignore-list ×3, seeded-pods ×5, podwaiter startWait ×14, RG fixture preamble ×14, prefix pod-waits ×4. gmc: captureSink→logtest, webhook table-tests, e2e dispatch preamble ×3, waitForPodGone ×4. |
| <a id="Q573"></a>Q573 | [Detect served API fields nothing reads](../api) | `ci` `tests` `retro` | 🔲 | S | Measured 2026-08-08: only 2 of 106 served v2beta1 fields were unread (`Sharing`, `allowedNamespaces`), both Q166's, and Q166 has landed, so the gate baselines empty. Q526's unwritten *status* field: spec-only scope misses it. |
| <a id="Q541"></a>Q541 | [Test helper claims to isolate a watch trigger and does not](../cmd/gmc/internal/controller/integration/suite_integration_test.go) | `tests` `retro` | 🔲 | S | `startEgressProxyReconcilerNoResync` says it proves a reconcile came from a watch, but a not-ready pool short-requeues every 15 s too — a Q506 test passed with both `Watches` deleted. State both sources; Q326 drives the pool Ready for this reason. |
| <a id="Q586"></a>Q586 | [The dogfood bootstrap scripts have no test coverage](../scripts/dogfood/setup.sh) | `tests` `dogfood` | 🔲 | M | Q581 covered the teardown half (`e2e-stop.sh`, `delete.sh`) and split this out: bootstrap aborts loudly instead of stranding nodes, so it ranks lower. `setup.sh` and `e2e-setup.sh` need a `*_LIB_ONLY` seam first; keep the keychain read stubbed. |
| <a id="Q532"></a>Q532 | [Enforce `BUILD_TAGS` ↔ `.golangci.yml` build-tags sync](development/testing.md#the-build-tag-gate) | `tests` `ci` `retro` | 🔲 | S | testing.md claims the vet-tags coverage assertion catches a tag missing from lint; it only checks go-vet-tags.sh's own list — `autoscaler` was absent from `.golangci.yml` from Q474 until Q479. Assert the two lists match under `make scripts-test`. |
| <a id="Q574"></a>Q574 | [One gate for every path list the workflows write twice](development/testing.md#a-path-list-written-twice-the-trigger-and-the-filter) | `ci` `retro` | 🔲 | M | Three shapes, three bespoke checks: shared paths across two `filters:` (Q533), `on.push.paths` vs its filter (gated), `pull_request.paths` vs `push.paths` (ungated). One registry replaces all three, over Q584's `devtools/ci/pathfilters` parser. |
| <a id="Q672"></a>Q672 | [The proxy-scaling runbook's `ResourceQuota` strings are unmeasured](operations/troubleshooting.md#proxy-pool-not-scaling) | `docs` `retro` | 🔲 | S | Q591 measured the `<unknown>` causes; the quota one needs its own repro. `FailedCreate ... exceeded quota`, `ReplicaFailure` with `exceeded quota`, and the "~30s" scrape cycle are unmeasured. |
| <a id="Q736"></a>Q736 | [No zero-match guard on the test-integration `RUN=` filter](../cmd/gmc/Makefile) | `ci` `tests` | 🔲 | S | Measured 2026-08-08: `go test -run <nonsense>` exits 0 printing `[no tests to run]`, so a mistyped `RUN=` reads as a pass. `make test`/`make e2e` now fail on zero match (Q680/Q679); the two module targets need a shared wrapper to do the same. |
| <a id="Q344"></a>Q344 | [First-class `scaleset` list/prune for orphan scale sets](../scaleset/client.go) | `feature` | 🔲 | S | Measured 2026-08-06: 9 orphans across 2 repos, 5 unreferenced. Pruning needs a hand-rolled registration-token → RemoteAuth → DELETE chain; `Client` has no list-all. Q334 fixed runner-*record* orphans. |
| <a id="Q650"></a>Q650 | [Em-dash density in the design and operations docs](development/documentation-standards.md) | `docs` | 🔲 | L | Measured 2026-08-04, code/headings/link text excluded: 9,993 in 584,490 words, 17.1 per 1,000 vs 3; 231 of 249 files over. `make em-dash-report` ranks them; the [gate](development/documentation-standards.md#enforcing-the-em-dash-rule) ratchets each. |
| <a id="Q710"></a>Q710 | [Overlong paragraphs in the design and operations docs](plan/q710-overlong-paragraphs.md) | `docs` | 🔲 | L | Phase 1 done: three prose ≥140w left in chapters 01–08, deliberately (corpus 154 → 127, parser-measured; the row's 90/24 came from a blank-line scan). Next: 02-architecture's 25 long bullets, then appendix-h, then operations. |
| <a id="Q711"></a>Q711 | [Gate the single-line card bullet invariant as a Go markdown devtool](development/website.md#card-bullets-fit-on-one-line) | `ci` `docs` `retro` | 🔲 | S | Measured 2026-08-06 at 1440px: 50 chars (3-col, 347px bullet), 77 (2-col, 559px), per [website.md](development/website.md#card-bullets-fit-on-one-line). Parsing density puts it in Go over goldmark, not shell. Both pages comply on main; starts green. |
| <a id="Q734"></a>Q734 | [Slate's body text sits below the APCA body floor, capping every muted token under it](development/website.md#interface-rules-the-custom-layer-must-satisfy) | `docs` | 🔲 | M | Measured 2026-08-08: slate body is Lc 66.9 against a 75 floor, so Q731's `--gag-muted` could only reach 55.6 without out-contrasting body copy. Fixing it means overriding Material's palette, repainting every dark-mode page. |
| <a id="Q744"></a>Q744 | [A cumulative per-file ceiling admits a jointly-red merge from two green PRs](development/testing.md#the-status-you-report-is-a-claim-too) | `ci` `retro` | 🔲 | S | Measured 2026-08-08 on `testing.md` (ceiling 594): #1340 added net +1 em-dash and #1342 +2, each green alone, together red on `main` for ~20 min. Q560/Q562's class on a new gate. First: does the merge queue run `em-dash` on the candidate at all? |
| <a id="Q745"></a>Q745 | [piped-gate lets a backgrounded gate report a trailing command's exit status](../.claude/piped-gate-guard.json) | `debt` `retro` | 🔲 | S | `make check > log; echo "EXIT=$?"; grep …` backgrounded reports `grep`'s 0, so a red gate reads green. It already denies a backgrounded call ending in `echo`/`\|\|`/`&`; this shape ends in a filter. Needs both-direction assertions. |
| <a id="Q687"></a>Q687 | [Document the predicate for a genuine human prompt in a transcript](development/README.md) | `docs` `retro` | 🔲 | S | A real prompt is `origin.kind == "human"` minus four classes that also carry it: hook denial text, `<bash-input>`, `<create-pr-command>`, `<system-reminder>` prefixes. One use so far. |
| <a id="Q735"></a>Q735 | [Narrow the semver floor past test files in released package dirs](../devtools/release/semverfloor/narrow.go) | `debt` | 🔲 | S | Distinct from Q705 (comment-only): `go list -deps` reports directories, so a `_test.go` beside compiled source reads as shipped. Measured 2026-08-08: 2 of 68 raising commits in v1.2.0..v1.3.0 ship only `_test.go`, both `fix`, so no verdict flips. |

---

## Deferred

Intentionally parked items. These carry **no priority position** and are **not** picked from the top of the Queue — each waits on an explicit trigger before it returns to active work. When an item's trigger fires, move its row back into the Queue at the position it then deserves.

Each trigger is tagged by source: **Demand:** an outside operator/user ask · **Event:** an observable outside-our-control condition · **Decision:** our own call (we're the blocker; grep `**Decision:**` for what we could move on unilaterally).

| ID | Item | Labels | Sz | Trigger to revive |
|---|---|---|---|---|
| <a id="Q675"></a>Q675 | [Merge-queue Phase 3 re-measure: heavy-gate runs per branch, kickback rate, merge latency](plan/merge-queue.md#phase-3-re-measure-then-decide) | `ci` `speed` | S | **Event:** ~1 week of queue operation (on/after 2026-08-10). Re-run the plan's 2026-08-03 measurements, then decide among the recorded candidates: e2e merge-group-only, split status PRs + pr-deps-gate, per-item backlog files, or nothing. |
| <a id="Q501"></a>Q501 | [A cancelled run's worker pod keeps running to completion](plan/q501-cancel-relay.md) | `bug` | S | **Event:** the next live-GitHub run reports `Q501 candidate A outcome` — the cancel spec now takes that reading, and nothing is buildable until it lands. Phase 0 closed the item to classic-only: ScaleSet bounds a cancel at two graces. |
| <a id="Q530"></a>Q530 | [Isolate concurrent live-GitHub runs instead of forbidding them](plan/q511-live-github-run-isolation.md#the-decision-forbid-concurrency) | `tests` | L | **Decision:** the singleton rule (enforced by Q511's preflight) is costing more than parallel live runs are worth. Needs `actions-gateway/gateway-test`'s fixture workflows to take `runs-on` as an input — names alone do not isolate routing. |
| <a id="Q397"></a>Q397 | [`validate-cluster.sh` metrics-server retry — confirm on a from-zero bootstrap](plan/gke-dogfood.md#recreate-is-proven-end-to-end-q380) | `dogfood` | S | **Event:** the next dogfood recreate bootstraps from zero. The bounded retry ships and is unit-tested against faked probes; confirm the preflight prints `[WAIT]` then `PASS` inside the addon's ~2 min window, not WARN. |
| <a id="Q448"></a>Q448 | [Tighten the e2e NodeShare envelope against a measured allocatable](plan/runner-sizing-profiles.md#the-live-path-and-why-the-rc-gate-could-not-have-found-this) | `tests` `dogfood` | S | **Event:** a dogfood turn-up stands an e2e node up — raising the envelope needs one live allocatable reading, less the dind sidecar and Kata's 250m. The 1500m÷1 lower bound sits under both static asks, so nothing is unschedulable meanwhile. |
| <a id="Q418"></a>Q418 | [Fast-release evicted jobs instead of waiting out the lock](plan/scaleset-eviction-recovery.md#phase-3-fast-release-if-phase-1-justifies-it) | `speed` | M | **Event:** a probe shows deregistering an evicted job's runner makes GitHub conclude before the lock TTL. Q396 measured 9m36-9m45s on both tiers, all of it GitHub's own detection. `cmd/probe` has the harness. |
| <a id="Q424"></a>Q424 | [Utilization delta with priority tiers off vs on](plan/eviction-oversubscription-validation.md#experiment-5-utilization-delta-q424-deferred) | `tests` `docs` | L | **Event:** dogfood carries a workload steady enough for a fixed-window comparison to mean anything. Measures occupancy both ways: the packing-density number the write-up is missing. |
| <a id="Q416"></a>Q416 | [Classic listener: session heal leaks then self-collides](plan/runner-sizing-profiles.md#the-20-sample-paths-reached-2026-07-25-second-session) | `bug` | S | **Demand:** a Classic operator reports listener slots going quiet, OR Classic removal slips. `healSession` DELETEs before refreshing the token → 401 → leak → `CreateSession` 409 → goroutine exits; only the baseline restarts. |
| <a id="Q372"></a>Q372 | [Re-run the structural debt audit](plan/structural-debt-audit-2026-07.md) | `debt` | M | **Event:** the next minor release is cut, OR non-test Go LOC grows ≥20% over the 41,011-line baseline (2026-07-20) — whichever first. Growth, not calendar time, tracks drift; linters catch ~2 of 10 findings, so the sweep stays necessary. |
| <a id="Q354"></a>Q354 | [Raise e2e/tested K8s floor 1.35 → 1.36](../.github/workflows/e2e-reusable.yml) | `ci` `tests` | S | **Event:** GKE regular channel reaches 1.36, OR we want a 1.36-only feature (explicit floor-raise), OR kind stops shipping 1.35 node images. Then bump KIND_NODE_IMAGE digest + refresh the "verified on k8s 1.35" operations-doc claims. |
| <a id="Q174"></a>Q174 | [v2 bring-your-own proxy TLS certificate](plan/v2-api.md#deferred-out-of-the-critical-path) | `security` `feature` | M | **Demand:** an operator with managed PKI/Vault wants to supply the proxy cert instead of GMC's self-signed default. Add certificateSecretRef on EgressProxy. Invariant: same-namespace TLS Secret, no cross-tenant reuse. Additive; design goal 6. |
| <a id="Q169"></a>Q169 | [AGC horizontal scaling / multi-replica HA](design/appendix-e-capacity-planning.md) | `feature` | L | **Event:** a single per-tenant AGC becomes a measured bottleneck or a SPOF concern (near the ~1000-session ceiling). Single-replica with an in-memory session registry by design; real HA needs distributed session state. |
| <a id="Q15"></a>Q15 | [gVisor RuntimeClass validation](plan/milestone-5.md) | `milestone` `security` `feature` | S | **Demand:** an operator wants lightweight (non-VM) syscall-filtering for compute-only, non-DinD CI jobs — likeliest on GKE, where gVisor is first-party. Kata (Q224) already covers the DinD case, the primary sandboxing motivation here. |
| <a id="Q11"></a>Q11 | [Ed25519 live probe — M-11b](plan/security.md) | `security` `tests` | S | **Decision:** the broker swaps RSA-OAEP session-key delivery for X25519 ECDH ([Q351](#Q351)), making Ed25519 the *secure* default. Until then it's a less-secure opt-in (loses the AES session-key layer); RSA-3072 stays the default. |
| <a id="Q361"></a>Q361 | [CI latency round 2: lint + coverage module loops](plan/archive/unit-tests-speed.md#2026-07-20-re-baseline-q17-revival) | `speed` `ci` | M | **Decision:** CI latency is again the bottleneck (self-set threshold). Critical path is lint (~230 s per-module golangci-lint loop); coverage ~140 s. Levers in the linked re-baseline. |
| <a id="Q351"></a>Q351 | [X25519 ECDH session-key exchange](design/appendix-g-future-enhancements.md#g6-x25519-ecdh-session-key-exchange) | `security` | M | **Decision:** we swap RSA-OAEP session-key delivery for X25519 ECDH (Appendix G.6) — makes Ed25519 the *secure* default and unblocks [Q11](#Q11). Until then RSA-3072/RSA-OAEP stays the secure default. |
| <a id="Q70"></a>Q70 | Flip worker-image trivy leg to blocking | `security` `ci` | S | **Event:** upstream `actions-runner` base scans clean. The worker leg of [security-scan.yml](../.github/workflows/security-scan.yml) is report-only (~36 upstream HIGH/CRITICAL CVEs); when a bump clears them, set its `exit-code` to `1`. |
| <a id="Q198"></a>Q198 | [Quantified benchmark / case study](index.md) | `docs` | M | **Decision:** a paid scale run is funded (or Q181 real-cluster data exists) so real-GitHub-at-scale numbers can back a published case study. Needs ~$10–30 ephemeral cluster + real GitHub. Split from Q193 (free demo stays active). |
| <a id="Q203"></a>Q203 | [Enable Plausible analytics on the docs site](development/website.md) | `docs` | S | **Decision:** a maintainer decides to collect site traffic and provisions a Plausible site. Client wiring shipped (Q195) — set `extra.analytics.plausible_domain` in `mkdocs.yml` and redeploy; analytics sends nothing until then. |
| <a id="Q214"></a>Q214 | [SPIFFE/SPIRE workload-identity signer](plan/v2beta1.md#workload-identity-a-different-config-vault-first) | `security` `feature` | M | **Demand:** An operator wants keyless / SPIRE-based App-JWT signing. Slots behind the existing Q197 `githubapp.Signer` interface as another `signer.provider`, exactly like the deferred cloud KMS providers — additive, post-beta. |
| <a id="Q215"></a>Q215 | [Worker cache backend (actions/cache + Docker layer cache)](plan/caching-and-worker-storage.md) | `feature` | L | **Demand:** an in-cluster cache ask, OR [Q408](#Q408) Phase 1 lands and removes the working one. `actions/cache` reaches its store today, so the gap is local; and the read-only mirror role cannot hold a read-write cache, which is the design question. |
| <a id="Q718"></a>Q718 | [Multi-node GPU jobs: gang scheduling and topology-aware placement](design/appendix-e-capacity-planning.md) | `feature` | L | **Demand:** a multi-node GPU CI workload. One job needs N co-scheduled pods in one NVLink/IB domain, which the capacity integer cannot express: gating a gang is a placement predicate, not a count. Needs a design doc; interacts with Kueue/Volcano. |
| <a id="Q216"></a>Q216 | [First-class GPU runner support (GPU Operator/NFD)](design/appendix-e-capacity-planning.md) | `feature` | M | **Demand:** a concrete GPU runner workload/ask. priorityTiers already nominally carry GPU labels; first-class support adds nodeSelector/tolerations/RuntimeClass conventions + GPU Operator / NFD awareness (and Volcano gang-scheduling for multi-GPU). |
| <a id="Q217"></a>Q217 | [OLM / OperatorHub bundle](operations/install.md) | `docs` `feature` | M | **Demand:** OpenShift/OperatorHub adoption demand. Helm-only is the deliberate install stance; an OLM bundle/catalog entry waits for a concrete OperatorHub ask. Additive packaging, no core code change. |
| <a id="Q407"></a>Q407 | [Capacity gate: `ProvisioningRequest` probe](design/appendix-g-future-enhancements.md#g16-provisioningrequest-pre-acquire-capacity-probe-check-capacity) | `feature` | L | **Event:** a GPU/spot operator finds ~1 claim/window ([§9h](plan/capacity-aware-intake.md#9h-what-the-dogfood-re-run-measured-for-the-latch-q513)) too costly, OR CA runs with provisioning requests enabled, OR `check-capacity` support broadens. |
| <a id="Q268"></a>Q268 | [Warm worker pool (`minIdleWorkers`)](design/appendix-g-future-enhancements.md#g12-warm-worker-pool-minidleworkers) | `feature` | M | **Demand:** a CPU-CI team hits pod-schedule latency after pre-pull (Q211) + cache volumes (Q215) are exhausted. Opt-in per-RG idle-pod pool. Does NOT address Q224 starvation — see the [lever spike](plan/q224-fanout-dispatch-lever-spike.md). |
| <a id="Q272"></a>Q272 | [Scale-set upstream maturity watch](plan/v1-classic-sunset-review.md) | `docs` | S | **Event:** `actions/scaleset` reaches GA/v1.0 or the auto-assign contract (`actions/scaleset#107`) is documented. Not a graduation blocker (sunset review §6.1); it lifts the Public-Preview caveat and triggers the U6 vendor-vs-own client revisit. |
| <a id="Q452"></a>Q452 | [Decide whether GA `v2` defines the deprecated FQDN aliases](plan/v2-ga.md#phase-2--the-graduation-hop) | `docs` | S | **Event:** [v2-ga.md](plan/v2-ga.md) Phase 2 (the graduation hop) begins. Omit the aliases from `v2` — the hub conversion then needs a lossless carrier, since it rides them verbatim today — or carry them deprecated into a frozen GA. |
| <a id="Q413"></a>Q413 | [v2 GA graduation (`v2beta1` → `v2`)](plan/v2-ga.md) | `milestone` | L | **Event:** `v1.3.0` ships, starting the Phase 1 soak: no incompatible `v2beta1` shape change over two minors, all five kinds carrying real traffic, conversion round-trips on real objects. |
| <a id="Q273"></a>Q273 | [Complete v1 removal (full v2-only)](plan/q273-v2-front-door.md) | `docs` | M | **Event:** [v2-ga.md](plan/v2-ga.md) Phase 3 begins — the same gate as [Q264](#Q264), which it ships beside. Front door, banners, and `gag-migrate` are done; the residual `v1alpha1` + `v2alpha1` removal lands in the `v2.0.0` bundle. |
| <a id="Q264"></a>Q264 | [Remove deprecated classic acquisition machinery](plan/q264-scale-set-protocol.md) | `debt` | L | **Event:** [v2-ga.md](plan/v2-ga.md) Phase 3 begins. `v2beta1` is ScaleSet-only, so classic serves only v1alpha1/v2alpha1 objects and comes out in the same `v2.0.0` bundle, not separately. Strips `acquisitionProtocol`/`maxListeners`. |
| <a id="Q275"></a>Q275 | [Reconcile AGC capacity/density docs with the ScaleSet default](design/appendix-a-capacity-slos.md) | `docs` | S | **Decision:** classic removal proceeds on the deprecation-window schedule ([Q264](#Q264)) — reconcile alongside it. appendix-a's ≤1,000-session ceiling and README Tier 2's "thousands per AGC" are classic framing; keep the density evidence. |
| <a id="Q310"></a>Q310 | Operator diagnostic aggregator (`gag status` / kubectl plugin) | `feature` | L | **Demand:** operators ask for gateway diagnostics beyond raw kubectl + the runbook. Add a `gag status <gateway>` / kubectl plugin aggregating session, pool, and runner state per gateway. |
| <a id="Q678"></a>Q678 | [Re-assess the script entry point once most gate cores are Go](../scripts/README.md) | `debt` `ci` | S | **Decision:** enough gate cores have moved to Go to judge the seam on more than one instance. Every devtools program is fronted by a `scripts/<group>/` script doing file selection plus the build; whether that still earns its keep is unmeasured. |

### Flake watch

Flakes whose mitigation has shipped and that have **not recurred since**, plus rare first sightings not yet worth fixing. The trigger to revive is the flake recurring on `main` after its fix. On recurrence, [flakes-first](development/maintaining-backlog.md#flake-fixes-go-first) pulls the row back to the **top of the Queue** — now escalated, since the first mitigation didn't hold. Kept here (not closed) so a second occurrence is recognised as a recurrence rather than a fresh find. A row that clears its [soak bar](development/maintaining-backlog.md#retiring-a-flake-watch-row) retires to the cold [retired-flake ledger](development/flake-watch-retired.md) — still greppable, at no live-table cost.

| ID | Item | Labels | Sz | Trigger to revive |
|---|---|---|---|---|
| <a id="Q690"></a>Q690 | [`release-sentinel-test`'s live-run case fails under `make check`, passes standalone](development/testing.md#two-clocks-in-one-assertion) | `flake` `tests` | S | **Event:** the live-run cases fail on `main` after the Q690 fix (they count the watcher's own polls against a stubbed `sleep`/`date`; no real seconds bound them). Reproduced: a wall clock outrunning the timer, not load. → top of Queue, escalated. |
| <a id="Q685"></a>Q685 | [`TestListener_RestartDoesNotReprovisionAConcludedJob` re-provisioned a concluded job](development/testing.md#synchronize-on-the-signal-you-assert-on) | `flake` `tests` `ci` | S | **Event:** it fails `Condition satisfied` on `main` after the Q685 fix. The production window it raced is closed by Q689 ([architecture](design/02-architecture.md)), so its `deleteAttempts > 0` wait no longer carries it. → top of Queue, escalated. |
| <a id="Q664"></a>Q664 | [`E2E_AGC_StuckPendingPodReaped` times out with the stuck-Pending workers never reaped](design/02-architecture.md) | `flake` `tests` `ci` | S | **Event:** it times out on `main` after the Q664 fix (the reconcilers cap work-queue retry at 30s, so a reconcile error cannot strand the reap deadline `RequeueAfter` carries). Not reproduced locally in 4 runs. → top of Queue, escalated. |
| <a id="Q648"></a>Q648 | [The runner→GitHub egress probe scores an HTTP 403 as reachable, so a refused runner reads as a real failure](development/testing.md#runnergithub-egress-attribution-q352) | `flake` `tests` `ci` | S | **Event:** a `real-github-egress` spec fails on `main` after the Q648 fix and the banner misattributes it — `BLOCKED` on a spec still red across re-runs, or `REACHABLE`/`INCONCLUSIVE` on one that re-runs green. → top of Queue, escalated. |
| <a id="Q642"></a>Q642 | [`progress-watch-test`'s `interval 0` case fails under `make check`, passes standalone](development/testing.md#the-clock-is-as-often-a-deadline-around-a-process-as-a-sleep-inside-one) | `flake` `tests` | S | **Event:** the `interval 0` case fails on `main` after the Q642 fix (it asserts on ticks a stubbed `sleep` records; no deadline bounds the watcher). Reproduced: 8/1760 launches alive at the old 10 s deadline. → top of Queue, escalated. |
| <a id="Q596"></a>Q596 | [`check-v2-api-sync-test`'s `tree-in-sync` fails under `make check`, passes standalone](development/testing.md#the-v2-api-sync-gate) | `flake` `ci` | S | **Event:** `tree-in-sync` fails on `main` after the Q596 fix. Trigger unreproduced (2418 runs under load); what shipped is evidence — a failed read reads as trouble, not drift, and the assertion prints the gate rc and output. → top of Queue. |
| <a id="Q604"></a>Q604 | [`stallJob` installs its runner-name conflict after the job is already pollable](../scaleset/scalesetstub/stub.go) | `flake` `tests` | S | **Event:** a `stallJob` caller fails `must be held for a re-offer` on `main` after the Q604 fix (`EnqueueStalledJob` registers the conflict in the same locked section that makes the job pollable). → top of Queue, escalated. |
| <a id="Q602"></a>Q602 | [`TestListener_AbandonedJobDoesNotSurviveARestart` stops the listener before the abandon's message delete is issued](development/testing.md#synchronize-on-the-signal-you-assert-on) | `flake` `tests` | S | **Event:** it fails `Condition satisfied` on `main` after the Q602 fix (the test now waits for `deleteAttempts > 0`, the delete the invariant rests on, not only the abandoned counter, which rises at in-memory settle). → top of Queue, escalated. |
| <a id="Q600"></a>Q600 | [`TestMultiplexer_DuplicateJobDeliveryProvisionsOnce` reads its peak-provisioner count before the winner reaches the handler](development/testing.md#synchronize-on-the-signal-you-assert-on) | `flake` `tests` | S | **Event:** it fails `expected 1, actual 0` on `main` after the Q600 fix (the test now waits on `handlerMax >= 1`, the counter the invariant reads, not only on the losers' dedup metric). → top of Queue, escalated. |
| <a id="Q549"></a>Q549 | [`E2E_AGC_ScaleSetDrainedWorkerClaimAndRerunLandUnderChartRBAC` times out waiting for the disrupted run's re-run](plan/q549-scaleset-rerun-flake.md) | `flake` `tests` | S | **Event:** the re-run wait fails on `main` post-fix. Two modes ([triage](plan/q549-scaleset-rerun-flake.md)): A, AGC replaced mid-window (pinned); B, undiagnosed; PR sightings 2026-08-01, 2026-08-04, soak from the later. → top of Queue, escalated. |
| <a id="Q570"></a>Q570 | [`E2E_Migration_MigratedTenantReconcilesIntoAWorkingControlPlane` times out when the v1 proxy pool takes both workers](../cmd/gmc/test/e2e/migration_v1_to_v2_test.go) | `flake` `tests` | S | **Event:** the migrated proxy-pool wait times out on `main` after the Q570 fix (the tenant pins its inline pool with `WithProxyReplicas(1, 1)`, so its HPA cannot take the worker the migrated pool needs). → top of Queue, escalated. |
| <a id="Q559"></a>Q559 | [`TestV2_RunnerSet_CapacityGate_FixedClusterSkipsAcquire` acquires the job it must refuse](development/testing.md#the-two-effects-can-be-one-object-read-through-two-clients) | `flake` `tests` | S | **Event:** it fails `reason=capacity` on `main` after the Q559 fix (the wait reads the condition through the reconciler's own cached client — the observer the admission rung reads — not the apiserver). → top of Queue, escalated. |
| <a id="Q548"></a>Q548 | [Released-upgrade check dies on tar EPIPE reading the chart's values.yaml](../scripts/e2e/chart-released-upgrade-check.sh) | `flake` `ci` | S | **Event:** `tar: Cannot write: Broken pipe` fails `chart-released-upgrade-check` on `main` after the Q548 fix (the awk consumer reads the whole stream instead of exiting at first match). → top of Queue, escalated. |
| <a id="Q516"></a>Q516 | [Stale sibling-worktree lint cache fails `make check` cross-session](../scripts/go/go-lint.sh) | `ci` `flake` | S | **Event:** local lint reports findings citing a deleted `.claude/worktrees/*` path after the Q516 fix (go-lint.sh scopes `GOLANGCI_LINT_CACHE` per worktree under `tmp/`; CI keeps the default). → top of Queue, escalated. |
| <a id="Q498"></a>Q498 | [Provisioner eviction/rerun tests flake under parallel load](../cmd/agc/internal/provisioner/provisioner_test.go) | `flake` `tests` | S | **Event:** an eviction/rerun test fails again on `main` (unreproduced post-Q503: 320 runs under saturating load; #1010 made the recovery tests block on done channels). → top of Queue, escalated. |
| <a id="Q490"></a>Q490 | [`TestAGC_Q260_FanoutCompletionReconciles` cancels a job every delivery completed](development/testing.md#synchronize-on-the-signal-you-assert-on) | `tests` `flake` | S | **Event:** it fails `cancelled` vs `completed` on `main` after the Q490 fix (the stub publishes `CompleteJobCalls` only after committing the accounting, and the test waits on `DeliveryResults`). → top of Queue, escalated. |
| <a id="Q471"></a>Q471 | [`validate-cluster-test` flakes under parallel `make check` load](../scripts/e2e/validate-cluster-test.sh) | `flake` `tests` | S | **Event:** a `metrics`/`retry` assertion fails on `main` after the Q471 fix (the retry paths now read a fake clock the fake `sleep` advances, so no real elapsed time is measured and the suite never sleeps). → top of Queue, escalated. |
| <a id="Q300"></a>Q300 | [Systemic kindnet e2e leg flakiness (cross-spec)](plan/q300-gmc-kindnet-e2e-flake.md) | `tests` `flake` | M | **Event:** kindnet `e2e / e2e` red on `main` again. 07-18 escalation was misattributed (Q349, pre-fix). Dump now captures nfqueue/memory counters + full-window kindnet logs — attribute per the plan's triage table before any code change. |

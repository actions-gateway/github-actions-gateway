---
id: Q789
rank: b39
labels:
    - flake
    - tests
status: deferred
size: S
target: ../../scripts/docs/alloc-queue-id-test.sh
---

# `alloc-queue-id-test`'s `gh` stub reads a ref-lock collision as broken

**Event:** the fleet fails `every concurrent allocator exits 0` on `main` after the fix (the stub maps `cannot lock ref` to the 422 the atomic API answers).
Reproduced 2/20 then 1/20 under a 1,748 fork/s storm. → top of Queue, escalated.

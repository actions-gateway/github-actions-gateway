---
id: Q828
rank: ar
labels:
    - bug
    - ci
    - retro
status: ready
size: S
target: ../../scripts/agent/pr-requeue-eligible.sh
---

# A verdict record written before the format change is refused as if it were corrupt

Measured 2026-08-12 on #1426: an `--assess` predating #1431 writes `ELIGIBLE main`, but `record()` now writes keyed `verdict` lines, so `--confirm` reads '' and refuses. Fails closed, but reads as corruption, not skew (Q805's class).

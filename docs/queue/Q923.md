---
id: Q923
rank: ao
labels:
    - ci
    - debt
status: ready
size: XS
target: ../../.golangci.yml
---

# The SA1019 `Subjects` exclusion matches nothing and is dead config

**The blocking defect this row was filed for is closed.** It asserted that `main` itself failed `nolintlint` and that every PR was blocked; measured 2026-08-21 on `adffc8ba4`, `unit-test.yml`, which holds the `lint` job, is green on `main` across the twelve PRs that landed that day.
The stale path the row named as the lead was corrected before then: the rule at `.golangci.yml:194` names `cmd/agc/internal/transport/trustpool_test.go`, which is the file's current name.
What survives is smaller and the opposite shape.
No `SA1019` directive remains anywhere in `cmd/agc/internal/transport/`, so the exclusion now excludes nothing, and its comment still explains a suppression that has no subject.
Delete it, or swap it for the directive it was written for, never both.
Measured 2026-08-24: restoring the two `//nolint:staticcheck` directives while the exclusion still stands fails `nolintlint` with 2 issues, because [exclusion rules are applied before the nolint filter](../development/testing.md#a-directive-is-unused-red-is-about-the-linters-silence-not-the-directive).
The same file carries a stale count in the `nolintlint` settings comment at `.golangci.yml:78`, "71 of 79 are justified gosec accepts"; measured 2026-08-24 it is 104 of 117 first-party sites.
Fix it in the same edit, since this row already opens that file.
[Q929](Q929.md) owns the separate question of the lint gate returning two verdicts on one tree.

---
id: Q717
rank: b0t
labels:
    - debt
    - feature
status: ready
size: M
target: ../plan/release-1.5.md#candidates-not-yet-accepted
---

# Fold the scale-up token bucket into the advertised capacity

Waited on at `provisioner.go:532`/`:793`, after the claim, with the job holding its GitHub lock (the CRD godoc says so). A fourth `min()` rung in `AdvertiseCapacity` would never assign the burst in the first place.

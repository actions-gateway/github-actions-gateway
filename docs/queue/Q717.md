---
id: Q717
rank: ajd
labels:
    - debt
    - feature
status: ready
size: M
target: ../plan/release-1.5.md#candidates-not-yet-accepted
---

# Fold the scale-up token bucket into the advertised capacity

`scaleUp.wait` runs after the claim, with the job holding its GitHub lock (the CRD godoc says so).
A fourth `min()` rung would never assign the burst, and `admission.go` needs it in `Admit` too or it ships to one tier.

---
id: Q908
rank: b1z
labels:
    - docs
    - debt
status: ready
size: S
target: ../../scripts/ci/gate-list.sh
---

# `# status-scope: none` is dead syntax that two gates still carry

`STATUS_GATES` retired with Q889 and `gate-list.sh` no longer mentions it, so the annotation gates nothing: `make gate-lists-check` passes with it removed (measured 2026-08-18).
Two Makefile targets still declare it and `scripts/README.md` still documents the rule that reads it, so the next gate author copies both.
Drop all three.

---
id: Q775
rank: b1l
labels:
    - ci
    - docs
    - retro
status: ready
size: S
target: ../development/parallel-dispatch.md
---

# A shipped Q-ID can be re-filed as an open row with no gate catching it

A dispatcher filed Q766 while its worker filed-and-deleted the same row, so landing both would have re-opened shipped work. `lint-backlog` rule 12 only checks the `refs/queue-ids` claim. Needs a parallel-dispatch note and a gate look.

---
id: Q775
rank: b1k
labels:
    - ci
    - docs
    - retro
status: ready
size: S
target: ../development/parallel-dispatch.md
---

# A shipped Q-ID can be re-filed as an open row with no gate catching it

A dispatcher filed Q766 while its worker filed-and-deleted the same row, so landing both would have re-opened shipped work. Splits: the rule-12 gate half is the `session-backlog` skill's upstream; the parallel-dispatch note half stays here.

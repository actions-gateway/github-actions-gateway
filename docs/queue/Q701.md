---
id: Q701
rank: b0b
labels:
    - tests
    - docs
    - retro
status: ready
size: S
target: ../development/testing.md#synchronize-on-the-signal-you-assert-on
---

# No doc records how to hold the scale-set poll loop out of its own poll

Two techniques, and picking wrong costs a session: `parkInPoll` (Q603) fails when the event under test wakes parked polls, as `CompleteAssignedJob` does, so Q689 blocked in Provision instead.
Only the archived Q603 plan names either.

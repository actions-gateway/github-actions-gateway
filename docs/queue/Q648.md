---
id: Q648
rank: b3d
labels:
    - flake
    - tests
    - ci
status: deferred
size: S
target: ../development/testing.md#runnergithub-egress-attribution-q352
---

# The runner→GitHub egress probe scores an HTTP 403 as reachable

**Event:** a `real-github-egress` spec fails on `main` after the Q648 fix and the banner misattributes it — `BLOCKED` on a spec still red across re-runs, or `REACHABLE`/`INCONCLUSIVE` on one that re-runs green. → top of Queue, escalated.

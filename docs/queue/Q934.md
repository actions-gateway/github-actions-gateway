---
id: Q934
rank: b3x
labels:
    - debt
    - retro
status: deferred
size: S
target: ../development/parallel-dispatch.md
---

# Worker contention sweep false-positives on unrelated histories

**Event:** `karlkfi/claude-skills` fixes the sweep snippet, after which this repo's playbook can drop its own caveat.
`git diff --quiet origin/main...$b` exits non-zero on a branch with no merge base, so the `session-worker` contention sweep reads `origin/gh-pages` as touching the file.
Measured 2026-08-18 and again 2026-08-21, where it was the only reproducible false positive in the loop.
The fix is upstream, not here: filed 2026-08-21 as gap 2 of `karlkfi/claude-skills` issue 288, alongside a second defect in the same six lines, that a reader extending the loop to several paths writes `for path in`, which destroys `PATH` under zsh.

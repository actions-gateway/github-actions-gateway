---
id: Q1116
rank: Zy
labels:
    - flake
    - ci
status: ready
size: S
target: ../../scripts/docs/check-queue-rules.sh
---

# check-queue-rules rule 9 did not fire on the dogfood runner

**Event:** `check-queue-rules-test` fails with `FAIL rule 9: an anchored last item still obliges the flip (rc=0 want=1)`.

**Measured 2026-09-16** while working Q1105, from [run 34827435782](https://github.com/actions-gateway/github-actions-gateway/actions/runs/34827435782) (`workflow_dispatch` on `main`, 2026-09-14 09:20Z, `target_gag=true`).
The `scripts-test` job failed on the self-hosted `gag-ci-scaleset` runner after 314s; the step concluded `failure`, not a timeout `cancelled`, and every other step in the job succeeded.
The assertion wanted the checker to exit 1 and it exited 0, so this is the checker declining to fire on input built to make it fire, which is a fail-open in a store gate rather than a noisy assertion.

The line beside it reads `check-queue-rules: ok (1 item(s), 2 at the merge base, rules 8, 9, 11, 13 and 14)`, so the checker ran and reached a verdict over a *one-item* view.
That is the thread to pull: whether the fixture's second item was absent when rule 9 was evaluated, which would make the rule vacuous rather than wrong.
**Unverified** — the mechanism above is read off one log and has not been reproduced.

**It is the only failure in the window.** Across the last nine `unit-test.yml` runs carrying this job (2026-09-14 to 2026-09-15), `scripts-test` was otherwise green at 153-210s, all on `ubuntu-latest`.
So the one failure is also the one run on the scaleset lane, and runner and outcome are perfectly confounded in this sample: it cannot say whether the trigger is that runner or chance.
Reaching the lane again needs a `workflow_dispatch` with `target_gag`, which is what a second reading costs.

Not [Q822](Q822.md): that row's trigger names `git-merge-script-index-test` and `git-merge-plan-index-test` and a `merge-*-index: the build failed with:` line, and this is a different suite reaching a different verdict.
Not [Q947](Q947.md), which is `status-lint`'s path filter omitting this checker's wrapper rather than the checker's own behaviour.

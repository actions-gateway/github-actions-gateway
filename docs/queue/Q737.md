---
id: Q737
rank: b0j
labels:
    - docs
    - debt
status: ready
size: S
target: ../../deploy/templates/README.md
---

# Align the shipped DinD templates on one docker:NN-dind version

Q554 shipped `privileged-dind` on `docker:27-dind`, `kata-dind` on `docker:28-dind`: what CI exercises.
As operator-facing artifacts the split reads as an accident.
Bumping 27 changes what the dogfood e2e runs, so it needs a green e2e leg.

---
id: Q672
rank: b1d
labels:
    - docs
    - retro
status: ready
size: S
target: ../operations/troubleshooting.md#proxy-pool-not-scaling
---

# The proxy-scaling runbook's `ResourceQuota` strings are unmeasured

Q591 measured the `<unknown>` causes; the quota one needs its own repro. `FailedCreate ... exceeded quota`, `ReplicaFailure` with `exceeded quota`, and the "~30s" scrape cycle are unmeasured.

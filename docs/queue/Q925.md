---
id: Q925
rank: aw9
labels:
    - debt
    - ci
status: ready
size: S
target: ../../devtools/docs/reasontiers/main.go
---

# Enumerate the GMC's Event reasons for the release notes

`reasontiers` scans `cmd/agc` only, so `api-surface-since.sh` reports half the Event reasons an operator meets (Q780).
Run on `cmd/gmc` it exits 2 at `found 0 condition reasons`: GMC reasons use the `gmcv2alpha1` alias `reasonPkgs` omits.

---
id: Q867
rank: a2
labels:
    - ci
    - docs
status: ready
size: S
target: ../../devtools/docs/metrictiers/values.go
---

# Refute a single-tier series claim with the label-value origins the derivation already has

Measured 2026-08-14: marking `message_poll_errors_total` Classic only passes, because `checkContradiction` reads emission sites and the scale-set tier reaches it through an adapter. Q851's value origins name `scalesetlistener` and would refute it.

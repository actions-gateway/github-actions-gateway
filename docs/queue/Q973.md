---
id: Q973
rank: ajh
labels:
    - tests
    - debt
status: ready
size: S
---

# Gate that every admission rung exists in both acquisition tiers

The two-tier rung parity Q443 exists to enforce is held by a prose comment in `admission.go` ("a rung added here must be added to Admit, and vice versa") and nothing else; Q717 hand-mirrored a fourth rung under it.
`check-metric-tiers.sh` records the same parity walk going stale four times (Q683, Q691, Q713, Q844) before a gate replaced the obligation.
A test walking the `AdmitReason*` constants and asserting each is reachable in both `Admit` and `AdvertiseCapacity` is the same shape.
Raised in review of #1702.

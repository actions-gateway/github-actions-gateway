---
id: Q471
rank: b3r
labels:
    - flake
    - tests
status: deferred
size: S
target: ../../scripts/e2e/validate-cluster-test.sh
---

# `validate-cluster-test` flakes under parallel `make check` load

**Event:** a `metrics`/`retry` assertion fails on `main` after the Q471 fix (the retry paths now read a fake clock the fake `sleep` advances, so no real elapsed time is measured and the suite never sleeps). → top of Queue, escalated.

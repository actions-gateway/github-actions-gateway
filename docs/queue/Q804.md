---
id: Q804
rank: b1q
labels:
    - tests
    - debt
status: ready
size: S
target: ../development/testing.md#test-doubles-must-long-poll
---

# `brokertest` answers `GET /message` 202 at once instead of long-polling

The one double that does not hold the poll, against this repo's own rule.
Q788's client floor caps the spin at 10 req/s, so what remains is the timing-window distortion the rule is about; `scalesettest` is the shape to copy (wake on queue change).

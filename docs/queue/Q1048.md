---
id: Q1048
rank: ajlf
labels:
    - tests
    - dogfood
target: ../../scripts/dogfood/e2e-mirror-clients.sh
status: ready
size: S
---

# Nothing schedules the mirror client census in a dogfood window

The reading Q1026 was written to take before its own narrowing has still never been taken, and no checklist asks for it.

**Measured 2026-09-03.** No workflow invokes `scripts/dogfood/e2e-mirror-clients.sh` and no runbook step asks for it.
Outside its own unit test and the `mk/gate-lists.mk` entry that runs that test, every mention of it in the tree is prose: `scripts/README.md`, `deploy/registry-mirror/README.md`, and backlog rows.
Its three siblings are all scheduled: `docs/operations/kata-dind-workloads.md` names `e2e-mirror-validate.sh`, `e2e-mirror-hits.sh` and `mirror-timing.sh` as window checks, and the census is the one omission.

**What it would have said.** The script resolves every address that reached a mirror to a pod or a node and reports whether each carries `actions-gateway/component=workload`, the label the shared topology's ingress peer now ANDs into its `from` element.
That is a prediction of who the narrowing cuts off, and it is valid under the isolated topology the dogfood cluster actually runs, because it is a census of clients rather than a grading of a policy.
It is also the reading that reaches a Kata worker's real source address on GKE Dataplane V2, which no kind lane can stand in for: `E2E_Mirror_SharedTenantsNP` ([`cmd/gmc/test/e2e/mirror_np_test.go`](../../cmd/gmc/test/e2e/mirror_np_test.go)) grades the peer expression on a Calico kind cluster with a curl pod, and names this census as its residual.

**Why it is small and not urgent.** Every client in this repository is a container of an AGC-provisioned worker pod, and `pod.go` sets the label unconditionally, so the source read says the census should pass.
That is precisely why it is worth taking rather than assuming: the script's own header says a source read has checked the clients somebody thought of.
The cost of never taking it is borne by an adopter running the shared topology, once per adoption, against a failure that is total for the client it misses.

**What it needs.** A line in `kata-dind-workloads.md`'s window checklist beside its three siblings, and a step in the Kata leg's sequence that runs it while the workers are still up: the script exits 2 rather than passing when an address no longer resolves, so it cannot be run after `e2e-stop.sh`.
**Q1038 answered its half on 2026-09-12 and the answer is `e2e-reusable.yml`.** `mirror-timing.sh` runs there as a step (``.github/workflows/e2e-reusable.yml:730:run: scripts/e2e/mirror-timing.sh``), and the `v1.8.0-rc.1` validation window gave it its first live run against a real mirror: `4 references through mirror-docker-io…, one cold fetch and one warm fetch each`, verdict `SEPARATED  every hit (<=46ms) was faster than every miss (>=147ms)`, without failing the e2e job.
Q1038 is closed; this row inherits the caution it carried, that an exit 2 is a reading that was not taken and not a finding.

**That answer does not transfer to the census, and an earlier reading of this row said it did.** "The census should take the same route" was inferred from where `mirror-timing` ended up rather than checked, and the two scripts need different things.
`mirror-timing.sh` fetches over HTTP from inside the guest, which any runner can do.
The census reads the mirror pods' proxy logs and resolves each address against this cluster's pods and nodes (`kubectl logs deployment/… -n gag-registry-mirror`, `get pods --all-namespaces`, `get nodes`), and the e2e job runs inside a Kata worker whose kubectl addresses the kind cluster `make e2e` stands up.
So the census can only be taken by something already holding the dogfood cluster's credentials, which the e2e job never does.

**Wired 2026-09-12** at the end of `validate-release.sh`'s e2e leg instead, before the run's exit status is acted on so a red matrix still yields the reading.
That is the earliest point after the run at which the gate holds both the credentials and any still-live workers: the AGC reaps a worker pod on a TTL after its job, so each later minute turns a resolvable client into an exit 2.

**The first `v1.8.0-rc.1` window predated the step and took no reading**, which was this row's own point restated by events: nothing invoked the script, so the window came and went without it, and teardown scaled the mirror deployments to zero with the access logs it reads.

**The second window ran it on 2026-09-14 and it still did not report.** Two client addresses reached the mirrors.
One graded: `10.36.10.5`, a `gag-dogfood-e2e` runner pod carrying `actions-gateway/component=workload`, which is the answer the source read predicts.
The other, `169.254.4.6`, resolved to no pod and no node, so the script refused the whole census, exit 2.

**The refusal's stated cause does not fit the address, and that is the new finding.** The script offers "most likely a worker that has already been deleted", which is the TTL race this row and Q1038 both warn about.
But `169.254.0.0/16` is link-local: pods on this cluster are `10.36.x.x` and nodes `10.142.x.x`, so a reaped worker's address would have been unresolvable *and* in the pod CIDR.
A link-local source was never a pod at all, so the reaped-worker explanation cannot be the right one here.
What it actually is has not been measured.
`169.254.169.254` is GKE's metadata server and node-local DNS binds `169.254.20.10`, so a Dataplane V2 host-side address is the obvious family to check first, but `.4.6` matches neither and nothing here has confirmed a mechanism.

**The class is one this repo already reasons about explicitly, and that is the useful part.** `169.254.0.0/16` sits in the emitted DNS egress NetworkPolicy as an `ipBlock` for exactly this reason (Q136): on NodeLocal DNSCache clusters, pods send DNS to a per-node `hostNetwork` cache at a link-local address **no pod or namespace selector can match** ([network-architecture.md](../design/network-architecture.md), [05-security.md](../design/05-security.md)).
A selector cannot match such an address, and neither can this census, which resolves every client against the pod list and the node list and has nothing else to try.
So the blind spot is not novel: the policy layer met this class, named it, and handles it with an `ipBlock` because no selector can.
The census met the same class and reports it as a timing accident.

So the work has moved rather than shrunk: the step exists and fires, and what is now missing is a census that can either grade a non-pod client or say why it is out of scope.
Grading every address as pod-or-node is the assumption that broke, and a refusal is the right default for an address the script cannot explain -- but a refusal that is permanent for a structural reason is not a reading waiting on better timing, it is a census that cannot complete on this cluster.

**Done when** one window has produced a graded verdict from the script, pass or finding, and this row records which one.
An exit 2 is a reading that was not taken, so it does not close this.

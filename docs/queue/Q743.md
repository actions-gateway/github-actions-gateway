---
id: Q743
rank: b00
labels:
    - ci
    - retro
status: ready
size: S
target: ../../.github/workflows/doc-links.yml
---

# `doc-links.yml` has no `merge_group`, so five gates skip the queue

Measured 2026-08-08: 9 of 25 workflows declare it; not this one, so its 5 gates never see the candidate merge.
Q742 ratchets the em-dash ceiling; this covers the other 4 gates and any later ratchet.
`status-lint.yml` already pairs `merge_group` with that `changes` job, so the Q574 dependency this row used to claim does not hold.
Measured 2026-08-18: 5 workflows are queue-blind, and Q942 covers why rule 8 cannot see them.

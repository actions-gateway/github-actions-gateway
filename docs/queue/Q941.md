---
id: Q941
rank: b3z
labels:
    - docs
    - debt
    - retro
status: ready
size: S
target: ../development/parallel-dispatch.md
---

# `session-worker`'s contract is silent where four workers needed it

Reported by workers during the 2026-08-18 batch, each quoting the copy loaded into its own context.
Four gaps: §4 says a per-item store commits the row with the code, contradicting CLAUDE.md's split-when-code-is-touched (Q889) — four workers hit this one independently and all followed the repo; §6 covers the post-`ready` window only "with no dispatcher", so a dispatcher awaiting a wake from an already-exited watcher is unhandled; nothing covers closing a second pre-existing row naming the same defect; nothing covers changing a shared script's output format, which no path-based sweep can see and the consuming side cannot detect either.
A fifth surfaced later: nothing says to check a pinned dependency's changelog when a row's asserted cause could have been retired by a version bump the repo already took, which is what settled Q835.
The skill lives in `karlkfi/claude-skills`, so this needs a human to file upstream.
The repo-side halves are documented in [parallel-dispatch.md](../development/parallel-dispatch.md).

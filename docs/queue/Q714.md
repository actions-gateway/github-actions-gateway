---
id: Q714
rank: ab
labels:
    - bug
    - feature
status: ready
size: M
target: ../plan/capacity-aware-intake.md
---

# Gate intake on workers that schedule but never start

`podUnschedulable()` keys on `PodScheduled=False`, so an `ImagePullBackOff` worker trips no rung: it binds, never starts, and each claim spends a JIT record and holds a lock to `pendingPodDeadline`.
Measured; the 1.4 DinD placeholder hits it.

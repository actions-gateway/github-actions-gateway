---
id: Q523
rank: b14
labels:
    - debt
status: ready
size: M
target: ../../cmd/gmc/internal/controller
---

# GMC controller structural dedup batch

`applyNamespacePSA` ×2, `tracingEnv` ×2, egress-stale eval ×2, roleRef-recreate appliers ×3, pod-scheduling copies ×2, `agc_vpa` re-inlines `applyManagedChild` (return the op result to share).

---
id: Q698
rank: b0l
labels:
    - tests
    - debt
    - retro
status: ready
size: S
target: ../../cmd/agc/internal/provisioner/deletion_inventory_test.go
---

# The delete-path inventory misses a `Delete` whose context arg is named unconventionally

`isContextArg` keys on a first arg named `ctx`-ish or a `context.*` call, so `Client.Delete(c, pod)` is skipped silently: fail-open inside a fail-closed gate. Widen to any 2+ arg `.Delete(`, letting the inventory classify the noise.

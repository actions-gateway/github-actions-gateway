---
id: Q532
rank: b19
labels:
    - tests
    - ci
    - retro
status: ready
size: S
target: ../development/testing.md#the-build-tag-gate
---

# Enforce `BUILD_TAGS` ↔ `.golangci.yml` build-tags sync

testing.md claims the vet-tags coverage assertion catches a tag missing from lint; it only checks go-vet-tags.sh's own list — `autoscaler` was absent from `.golangci.yml` from Q474 until Q479.
Assert the two lists match under `make scripts-test`.

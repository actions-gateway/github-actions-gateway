---
id: Q690
rank: b3a
labels:
    - flake
    - tests
status: deferred
size: S
target: ../development/testing.md#two-clocks-in-one-assertion
---

# `release-sentinel-test`'s live-run case fails under the fan-out

**Event:** the live-run cases fail on `main` after the Q690 fix (they count the watcher's own polls against a stubbed `sleep`/`date`; no real seconds bound them). Reproduced: a wall clock outrunning the timer, not load. → top of Queue, escalated.

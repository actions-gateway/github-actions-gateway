---
id: Q987
rank: ajj
labels:
    - docs
status: ready
size: S
target: ../../CLAUDE.md
---

# Point live `lint-backlog.sh` references at `lint-queue.sh`

`scripts/docs/lint-backlog.sh` retired with the table (Q889) and the store's lint is `scripts/docs/lint-queue.sh` behind `make queue-lint`, but `CLAUDE.md`'s agent-reference table still names the old path as *the* lint, and `maintaining-backlog.md` describes it enforcing rules 8 and 11 in the present tense.
A reviewer ran the path `CLAUDE.md` gave and got exit 127, which is "file not found" and reads like a lint failure — it nearly had a clean row reported as failing its own gate.
Nothing executable is broken: the two references under `scripts/` and `.github/workflows/` are comments, and `scripts/README.md` documents the replacement correctly.
19 files mention the old name and most are correct history (`technical-debt.md` records its retirement), so this is not a rename — only the references that instruct a reader to run it today are the defect.
Found in review of #1702.

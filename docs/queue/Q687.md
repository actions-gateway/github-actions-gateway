---
id: Q687
rank: b1p
labels:
    - docs
    - retro
status: ready
size: S
target: ../development/README.md
---

# Document the predicate for a genuine human prompt in a transcript

A real prompt is `origin.kind == "human"` minus four classes that also carry it: hook denial text, `<bash-input>`, `<create-pr-command>`, `<system-reminder>` prefixes. One use so far.

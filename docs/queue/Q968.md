---
id: Q968
rank: a3
labels:
    - flake
    - tests
status: ready
size: M
target: ../../cmd/agc/internal/controller/integration/v2_restart_reclaim_test.go
---

# `RestartReclaimsWorkerOrphanedWhileDown` never stamps the worker

**Top of queue per the [flakes-first rule](../development/maintaining-backlog.md#flake-fixes-go-first).**

Measured 2026-08-24 on a local `make test-integration` over the full `cmd/agc` suite: `TestV2_RunnerSet_ScaleSet_RestartReclaimsWorkerOrphanedWhileDown` failed at cmd/agc/internal/controller/integration/v2_restart_reclaim_test.go:240 (the `require.Eventually`) after 31.8 s, `Condition never satisfied`, on "a completion delivered to the restarted AGC's session must stamp the orphaned worker, giving it a reap deadline".
Re-run alone against the same tree it passed in 1.99 s, so the failure is order- or load-dependent rather than a defect in the assertion's subject.

The suite log shows a neighbouring session losing its scale-set lock in the same window (`session already exists (HTTP 409)`, `v2-rs-ss-lifetime`), which is a candidate for the interference and is **unverified**: nothing has yet shown the two are related, and 31.8 s against a 1.99 s solo run is also consistent with a starved envtest apiserver alone.

The mechanism is what the work is.
Reproduce under the full suite before changing anything, and settle whether the wait is racing a real reclaim or a `Eventually` budget that only holds when the box is idle.
Found while working Q719; out of that item's scope, filed rather than fixed.

---
id: Q740
rank: b0i
labels:
    - feature
    - docs
status: ready
size: M
target: ../operations/runner-template-library.md
---

# Ship a signed build-capable runner image for the DinD templates

Q554 ships `kata-dind`/`privileged-dind` with an `example.invalid` `workerImage`: the runner needs a docker CLI and no image we publish has one.
Ship actions-runner + CLI + buildx, signed, SBOM, digest-pinned, and drop the placeholder.

---
id: Q740
rank: afi
labels:
    - feature
    - docs
status: deferred
size: S
target: ../operations/runner-template-library.md
---

# Point the DinD templates at the published build-runner image

**Event:** the first release publishing `ghcr.io/actions-gateway/build-runner`.
Swap the `.invalid` placeholder in both DinD templates for its digest; add both to `check-release-pins.sh`.
Before then it would ship a `manifest unknown` ref on `main`.

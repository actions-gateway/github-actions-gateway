---
id: Q885
rank: b1x
labels:
    - debt
    - retro
status: ready
size: S
target: ../../.githooks/pre-commit
---

# A pathspec `git commit` bypasses the lint-backlog pre-commit backstop

Measured 2026-08-16: an over-cap row left unstaged commits clean via `git commit -- docs/STATUS.md` while the hook runs; `--staged` catches it once `git add`ed. CLAUDE.md prefers that pathspec form. Mechanism unverified.

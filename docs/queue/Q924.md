---
id: Q924
rank: aq
labels:
    - docs
    - debt
status: ready
size: S
target: ../development/maintaining-backlog.md#retiring-a-flake-watch-row
---

# maintaining-backlog.md's flake gate references are stale post-Q889

Measured 2026-08-18: it names `scripts/docs/lint-backlog.sh`, which [Q889](../plan/q889-backlog-item-store.md) deleted, and twice a `BACKLOG_ALLOW_FLAKE_DELETE` override no code reads — the linter takes `QUEUE_ALLOW_FLAKE_DELETE`.

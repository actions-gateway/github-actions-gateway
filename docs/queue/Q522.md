---
id: Q522
rank: b12
labels:
    - debt
status: ready
size: M
target: ../../cmd/gmc/internal/controller/shared_pki.go
---

# Consolidate GMC PKI/cert generation helpers

`generateEgressProxyCert` copies `generateProxyCert`; `generateMetricsCertsV2` copies `generateMetricsCerts`; both re-implement `randSerial`/`encodeCertPEM` from `shared_pki.go`; the renewal reason ladder repeats at 5 sites.

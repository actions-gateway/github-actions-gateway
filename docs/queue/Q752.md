---
id: Q752
rank: b34
labels:
    - flake
    - tests
status: deferred
size: S
target: ../development/testing.md#a-backstop-shorter-than-the-subjects-own-budget-is-not-a-backstop
---

# `record-launch-test`'s 10s launch bound fails under the `make check` fan-out, passes standalone

**Event:** the tree case fails on `main` after the fix (the stop wait is now `wait` on the wrapper, no clock). Trigger unreproduced: 0/40 under load; a serial `make check` sample was discarded as tainted. → top of Queue, escalated.

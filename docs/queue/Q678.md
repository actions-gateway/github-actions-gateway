---
id: Q678
rank: b2u
labels:
    - debt
    - ci
status: deferred
size: S
target: ../../scripts/README.md
---

# Re-assess the script entry point once most gate cores are Go

**Decision:** enough gate cores have moved to Go to judge the seam on more than one instance.
Every devtools program is fronted by a `scripts/<group>/` script doing file selection plus the build; whether that still earns its keep is unmeasured.

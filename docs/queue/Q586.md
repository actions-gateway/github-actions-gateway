---
id: Q586
rank: b19
labels:
    - tests
    - dogfood
status: ready
size: M
target: ../../scripts/dogfood/setup.sh
---

# The dogfood bootstrap scripts have no test coverage

Q581 covered the teardown half (`e2e-stop.sh`, `delete.sh`) and split this out: bootstrap aborts loudly instead of stranding nodes, so it ranks lower. `setup.sh` and `e2e-setup.sh` need a `*_LIB_ONLY` seam first; keep the keychain read stubbed.

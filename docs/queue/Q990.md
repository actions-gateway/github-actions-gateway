---
id: Q990
rank: aah
labels:
    - debt
    - retro
status: ready
size: M
target: ../../scripts/docs/queue.py
---

# `next-task` tells the session to check for an open PR, and never checks

`cmd_next` prints "check for an open PR first" into the kickoff prompt, `next-task.sh`'s header says the session re-verifies the pick, and CLAUDE.md step 1 says it again.
Three statements of the rule, no code that performs it, at the one chokepoint holding the ID.
Measured 2026-08-25: a session worked Q564 end to end (28 files, +806/-44) against PR #1705, open and CLEAN since ~100 minutes before it started; the pre-push ref sweep caught it, four hours in, so nothing was pushed.
Have `queue.py next` search open PRs for the ID and skip or flag a claimed item.
Two constraints: it must fail loud, never silently pass, when `gh` is absent or offline, and it must stay off the `lint` path, which gates run with no network.
A bare Q-ID typed by hand skips this entirely — that half is a `session-worker` skill amendment, chipped 2026-08-25.

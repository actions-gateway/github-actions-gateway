---
id: Q548
rank: b3j
labels:
    - flake
    - ci
status: deferred
size: S
target: ../../scripts/e2e/chart-released-upgrade-check.sh
---

# Released-upgrade check dies on tar EPIPE reading the chart's values.yaml

**Event:** `tar: Cannot write: Broken pipe` fails `chart-released-upgrade-check` on `main` after the Q548 fix (the awk consumer reads the whole stream instead of exiting at first match). → top of Queue, escalated.

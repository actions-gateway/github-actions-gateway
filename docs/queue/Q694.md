---
id: Q694
rank: b0d
labels:
    - tests
    - retro
status: ready
size: S
target: ../../scripts/agent/pr-requeue-eligible-test.sh
---

# `pr-requeue-eligible`'s timeline and GraphQL reads are untested behind the `gh` stub

The checker gates an unattended merge, but the stub returns post-`--jq` text, so a malformed `--jq` in `human_enqueued` or `in_queue` fails as a live crash, not a red suite. All three reads ran live in Q805; the suite still cannot see them.

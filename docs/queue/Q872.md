---
id: Q872
rank: a2
labels:
    - ci
    - debt
    - retro
status: ready
size: S
target: ../../.github/workflows/e2e-test.yml
---

# `e2e-gate` reports green on a PR where the suite never ran

`if: always()` counts a `skipped` heavy job as passing, so a PR breaking e2e looks identical to one that does not, and #1515 read ready 13 times.
Say so in the job summary; the required check and Q675's trade stay as they are.

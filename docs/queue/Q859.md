---
id: Q859
rank: a7
labels:
    - ci
    - debt
    - bug
    - retro
status: ready
size: S
target: ../../devtools/agent/pipedgate/main.go
---

# piped-gate's overlap probe judges the hook's checkout, not the session's

`pipedgate` takes its repo root from the registry path, so it probes the worktree the hook was installed in.
`EnterWorktree` breaks that: the session keeps the launch checkout's hook, so PR-create and push both flagged overlaps this branch lacks.

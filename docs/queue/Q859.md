---
id: Q859
rank: a4
labels:
    - ci
    - debt
    - bug
    - retro
status: ready
size: S
target: ../../scripts/agent/claude-piped-gate-hook.sh
---

# piped-gate's overlap probe judges the hook script's checkout, not the session's worktree

`main.go:188` probes the hook's installed worktree by design, assuming it is the session's. `EnterWorktree` breaks it: the session keeps the launch checkout's hook, so PR-create and push both flagged overlaps this branch lacks. A cwd fix reverses it.

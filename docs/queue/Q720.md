---
id: Q720
rank: b0x
labels:
    - feature
    - docs
status: ready
size: S
target: ../design/03-api-contracts.md
---

# Publish the queue depth GitHub reports on every poll

Every poll carries `totalAvailableJobs`; the listener stores the struct and `Status()` exports only assigned plus running.
Export it as a gauge so an operator can answer why a job sits queued, the top demand signal.

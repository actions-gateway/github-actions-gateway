---
id: Q549
rank: a0
labels:
    - flake
    - tests
status: ready
size: M
target: ../plan/q549-scaleset-rerun-flake.md
---

# `E2E_AGC_ScaleSetDrainedWorkerClaimAndRerunLandUnderChartRBAC` times out

**Top of queue per the [flakes-first rule](../development/maintaining-backlog.md#flake-fixes-go-first).** Escalated 2026-08-28: the fix did not hold, and two of the three new sightings are the shape the spec exists to catch rather than a re-staging.

Three failures on `main` or in the merge queue in three days, all after the 228-run soak this row was parked on:

| When | Lane | Run | Failed at | Shape |
|---|---|---|---|---|
| 2026-08-26 05:25 UTC | `e2e-calico` merge_group | [32933225396](https://github.com/actions-gateway/github-actions-gateway/actions/runs/32933225396) | `cmd/gmc/test/e2e/worker_scaleset_recovery_test.go:299` | no re-run, AGC unchanged |
| 2026-08-26 05:41 UTC | `e2e-test` merge_group | [32934571793](https://github.com/actions-gateway/github-actions-gateway/actions/runs/32934571793) | `cmd/gmc/test/e2e/worker_scaleset_recovery_test.go:264` | the sampler saw nothing |
| 2026-08-28 15:35 UTC | `e2e-test` push to `main` | [33185281929](https://github.com/actions-gateway/github-actions-gateway/actions/runs/33185281929) | `cmd/gmc/test/e2e/worker_scaleset_recovery_test.go:299` | no re-run, AGC unchanged |

**Neither known mode covers any of them, which is what the trigger asked for.** Mode A was the AGC replaced inside the claim window; the two line-299 failures reach `Fail` only after `agcPodIdentity() == pinnedAGC`, so the control plane that observed the disruption was still running: in the 08-28 run a single `ssrec-agc` pod, 114 s old, zero restarts.
Mode B was Q809's lost claim; those same two failures reach `Fail` only after `evictionRecoveryEvidenceLost()` returns false, so the pod's disruption record was still there to claim.
That is the spec's designed catch: the chart role is missing a verb the recovery path needs, or the deletion-mark discriminator regressed.
The 08-26 line-264 failure is a third shape again, `Expect(seq).NotTo(BeEmpty())`, the sampler never observed the probe pod at all, so the attempt never reached the claim window.

Start from the AGC logs the failing runs captured, grepping `could not claim scale-set worker disruption`, and attribute per the [triage table](../plan/q549-scaleset-rerun-flake.md) before any code change, since the two line-299 sightings are one hypothesis and the line-264 sighting is another.
It costs a merge-queue eviction each time it fires, so the two lanes are both exposed.

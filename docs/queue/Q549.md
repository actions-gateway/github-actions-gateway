---
id: Q549
rank: b3k
labels:
    - flake
    - tests
status: deferred
size: S
target: ../plan/q549-scaleset-rerun-flake.md
---

# `E2E_AGC_ScaleSetDrainedWorkerClaimAndRerunLandUnderChartRBAC` times out

**Event:** the re-run wait fails on `main` in a shape neither mode covers ([triage](../plan/q549-scaleset-rerun-flake.md)).
A, AGC replaced mid-window, pinned; B, the claim lost, measured and fixed under Q809. → top of Queue, escalated.

Soak measured 2026-08-23: 228 green `e2e-test` runs on the events that actually execute the suite (`merge_group` + push to `main`) since Q809's fix merged 2026-08-12T18:32Z, or 59 counting push-to-`main` alone.
Both clear the ≥50 correctness bar, and nothing has recurred.
Held rather than retired only because this is the one flake-watch row with a demonstrated post-fix recurrence; the next groom retires it absent a new sighting.

---
id: Q878
rank: ah
labels:
    - tests
    - ci
    - retro
status: ready
size: S
target: ../../scripts/docs/git-merge-status-test.sh
---

# Give the sibling merge-driver suites Q820's tree reading

`git-merge-status-test` and `git-merge-roadmap-test` share Q820's shape and already carry its `ERR` trap, but not the `Q820:` tree state it added, so a sighting in either names a line and nothing else.
Neither has flaked yet.

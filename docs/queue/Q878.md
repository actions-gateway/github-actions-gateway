---
id: Q878
rank: ah
labels:
    - tests
    - ci
    - retro
status: ready
size: S
target: ../../scripts/docs/git-merge-roadmap-test.sh
---

# Give the sibling merge-driver suites Q820's tree reading

`git-merge-roadmap-test` shares Q820's shape and already carries its `ERR` trap, but not the `Q820:` tree state it added, so a sighting names a line and nothing else.
It has not flaked yet.

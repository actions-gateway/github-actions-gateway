---
id: Q862
rank: a4
labels:
    - ci
    - debt
    - retro
status: ready
size: S
target: ../../devtools/agent/pipedgate/repostate.go
---

# The PR-overlap check intersects paths, so disjoint hunks read as overlap

Three sightings in two days: #1505, #1527, #1531, each denying a create over disjoint hunks and costing a manual `git merge-tree` plus an override. `prOverlapWarning` intersects path sets only; line ranges need a per-PR diff fetch.

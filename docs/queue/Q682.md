---
id: Q682
rank: b08
labels:
    - tests
status: ready
size: S
target: ../plan/q645-abandoned-completion.md#the-remedy
---

# Measure sibling `skipped` completion semantics live (Q260 fan-out)

Inv. H covered only the winner's own delivery. The default-on sibling fan-out sends `skipped` mid-run; whether it concludes anything is unmeasured (brokertest assumes not). Needs a two-delivery probe arm.

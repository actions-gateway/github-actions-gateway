---
id: Q516
rank: b3k
labels:
    - ci
    - flake
status: deferred
size: S
target: ../../scripts/go/go-lint.sh
---

# Stale sibling-worktree lint cache fails `make check` cross-session

**Event:** local lint reports findings citing a deleted `.claude/worktrees/*` path after the Q516 fix (go-lint.sh scopes `GOLANGCI_LINT_CACHE` per worktree under `tmp/`; CI keeps the default). → top of Queue, escalated.

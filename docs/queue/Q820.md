---
id: Q820
rank: a8
labels:
    - flake
    - ci
status: deferred
size: S
target: ../plan/q820-scripts-test-temp-file-flake.md
---

# `git-merge-plan-index-test` dies on a temp-file error under the fan-out

**Event:** the `unable to create temporary file` signature recurs on `main` after the fix.
Cause was git's own detached `git maintenance --auto`, pruning a fanout directory between a concurrent commit's `mkdir` and `open`; fixture repos now set `maintenance.auto false`, asserted on behaviour.

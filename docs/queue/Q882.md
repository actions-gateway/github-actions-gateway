---
id: Q882
rank: b1v
labels:
    - debt
    - retro
status: ready
size: S
target: ../../.claude/piped-gate-guard.json
---

# piped-gate tells a release branch to rebase onto main

It read `release-1.5` as a stale PR branch.
Rebasing is the one thing that must never happen there: it publishes everything merged since the tag.
Recurs on every backport.

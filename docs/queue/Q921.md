---
id: Q921
rank: aji
labels:
    - tests
    - ci
status: ready
size: S
target: ../development/testing.md#a-fixture-repo-must-not-run-background-git
---

# Gate the fixture-repo `maintenance.auto` invariant so it cannot regress

16 suites carry `maintenance.auto false` with no assertion; a text query cannot check them, since one drives git from embedded Python.
`GIT_TRACE` in `run-parallel.sh` would measure a run that already happens.

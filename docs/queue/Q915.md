---
id: Q915
rank: aig
labels:
    - ci
    - debt
status: ready
target: ../../scripts/ci/git-merge-gate-lists.sh
size: S
---

# gate-lists merge driver refuses every merge; its suite cannot see it

`MANAGED_VARS` still names `STATUS_GATES` ([git-merge-gate-lists.sh:97](../../scripts/ci/git-merge-gate-lists.sh)), which #1603 renamed to `QUEUE_GATES`; `lift_vars` hard-fails on a managed variable it cannot find, so the driver refuses `mk/gate-lists.mk` outright and git leaves ordinary conflict markers.
Measured 2026-08-18 rebasing Q865 onto #1627, both of which appended a gate: `merge-gate-lists: base: STATUS_GATES is not assigned in this file`, resolved by hand.
The suite stays green because its fixture declares `STATUS_GATES` (`git-merge-gate-lists-test.sh:47`), so it asserts a file shape the repo no longer has.
`DOCS_GATES` is unmanaged too, added after the driver.
Fix the name, add `DOCS_GATES`, and derive the fixture from the real file so the suite cannot pass on a shape that has moved.

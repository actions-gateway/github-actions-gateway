---
id: Q912
rank: b3v
labels:
    - ci
    - debt
    - retro
status: deferred
size: S
target: ../../test/fakegithub/main.go
---

# endpoint-parity gate startup failure has an unproven root cause

**Event:** any e2e-venue gate goes flaky again.
The failure now names which of three cases it hit and quotes the fake's own output, which is the evidence this row waits on and cannot manufacture.
The Q871 gate went red under `make check` on its own PR, then green once the fake's output moved from an inherited pipe to a file, and the causal link is unproven.
It never reproduced standalone: six concurrent gate runs and four under a live `make scripts-test` all passed.
The mechanism fitting every observation is that the fake logs before it binds, while `scripts-test` is a nested `run-parallel` fan-out where 85 suites write into one outer pipe.
The readiness bound also went 10s to 30s, which is a bound rather than a fix.

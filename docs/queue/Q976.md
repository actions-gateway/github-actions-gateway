---
id: Q976
rank: b46
labels:
    - debt
    - security
size: S
status: ready
target: ../../.github/dependabot.yml
---

# Repoint the dependabot docker ecosystem at the real Dockerfile

The `docker` ecosystem lists five directories holding no Dockerfile; #870 consolidated the only one to `/`, and the last bump was #643 on 2026-07-17.
Nothing bumps a base digest, Q740's `docker:<N>-cli` included.

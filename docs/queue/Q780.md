---
id: Q780
rank: aw
labels:
    - debt
    - ci
    - retro
status: ready
size: S
target: ../operations/release.md#writing-the-curated-notes
---

# Enumerate Kubernetes Event reasons mechanically for the release notes

Q850 built the enumeration and its self-test: `reasontiers` reads each reason's index off the callee's declaration, which is what the v1.4.0 regex got wrong.
Remaining work is wiring it into the `api-surface-since.sh` pre-flight.

---
id: Q657
rank: b10
labels:
    - tests
status: ready
size: S
target: ../plan/eviction-oversubscription-validation.md#the-17s-outlier-is-unattributable-q657-2026-08-03
---

# Repeat the eviction measurement now the spec attributes the disruption

Three clean runs say lock TTL (9m36–9m45s); the lone 17s run predates the `runner_name` identity check, so "not reliably ungraceful" is unsupported, not refuted.
Repeat with the check to see whether a fast conclusion recurs.

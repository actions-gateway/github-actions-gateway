---
id: Q829
rank: b31
labels:
    - flake
    - ci
status: deferred
size: S
target: ../../scripts/fetch/download-verified.sh
---

# A pinned download outlives `download-verified.sh`'s retry budget

**Event:** any `download-verified.sh` step fails on `main` after the fix. The schedule is the sibling's (5 retries, 5 s doubling to 60 s, jittered, 135-202 s), so a failure means the denial outlasted ~3 min, not 10 s. → top of Queue, escalated.

---
id: Q600
rank: b3i
labels:
    - flake
    - tests
status: deferred
size: S
target: ../development/testing.md#synchronize-on-the-signal-you-assert-on
---

# `TestMultiplexer_DuplicateJobDeliveryProvisionsOnce` reads too early

**Event:** it fails `expected 1, actual 0` on `main` after the Q600 fix (the test now waits on `handlerMax >= 1`, the counter the invariant reads, not only on the losers' dedup metric). → top of Queue, escalated.

---
id: Q863
rank: a3
labels:
    - flake
    - ci
status: ready
size: S
target: ../../Dockerfile
---

# A `docker buildx` base-image pull has no retry, so one registry 403 fails an image build

Measured 2026-08-14 on #1515: `trivy (proxy)` 403'd HEAD-ing the pinned distroless digest while agc/gmc/fakegithub pulled it green in the same run; green on rerun, no code change. Same class as [`gh_retry`](../../scripts/dogfood/validate-release.sh).

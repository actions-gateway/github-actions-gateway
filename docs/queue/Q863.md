---
id: Q863
rank: b3u
labels:
    - flake
    - ci
status: deferred
size: S
target: ../../.github/workflows/security-scan.yml
---

# A `docker buildx` base-image pull has no retry, so one 403 fails a build

**Event:** a `trivy` shard 403s on a base-image pull on `main` after the fix. → top of Queue, escalated.
Mitigated by a second build attempt 30s later, and by `retry.sh` in the local mirror.
`publish.yml`'s build is still unretried — [Q899](Q899.md), deliberately left.

---
id: Q735
rank: b1o
labels:
    - debt
status: ready
size: S
target: ../../devtools/release/semverfloor/narrow.go
---

# Narrow the semver floor past test files in released package dirs

Distinct from Q705 (comment-only): `go list -deps` reports directories, so a `_test.go` beside compiled source reads as shipped. Measured 2026-08-08: 2 of 68 raising commits in v1.2.0..v1.3.0 ship only `_test.go`, both `fix`, so no verdict flips.

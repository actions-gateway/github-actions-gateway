---
id: Q739
rank: b0c
labels:
    - debt
    - retro
status: ready
size: S
target: ../development/testing.md#the-launch-record
---

# Nothing makes `record-launch.sh` fire, so the handle it writes is optional

Q709 shipped the record but not the trigger: nothing reaches for `record-launch.sh`, so a run launched without it has no handle after a compaction. Candidate seam: foreground-guard `hint`, unverified.

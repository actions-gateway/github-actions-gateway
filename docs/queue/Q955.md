---
id: Q955
rank: b42
labels:
    - ci
    - debt
status: deferred
size: S
target: ../development/skills.md#vendored-from-the-skills-repo
---

# Nothing asks whether the vendored skill files have moved upstream

**Decision:** whether CI may hold a read token for the private `karlkfi/claude-skills`, or upstream drift stays a thing somebody looks at.
Q890 shipped the local half: `make vendored-skills-check` holds each vendored file to a declared digest, so a fork here is reviewable.
It cannot ask the other question, and that is the residue rather than an oversight, which is why this is a separate row.
The repo is private, so a fetching gate needs a token this repo does not carry, and a gate whose oracle is the network fails when a third party sneezes.
Measured 2026-08-21: upstream took 8 commits on `queue.py` and 1 to 2 on each of the other three in the five days after Q889 vendored them, none of it visible here.

---
id: Q951
rank: a5
labels:
    - flake
    - ci
status: deferred
size: S
target: ../../scripts/ci/check-tools-test.sh
---

# `version-cmds-answer` flakes on kubectl, cause unverified

**Event:** `version-cmds-answer` or `installed-floors-report-a-version` fails on `main` again.

Measured 2026-08-21 on an unmodified tree: `make check` failed `check-tools-test` with `no version from: kubectl, checked=5`, and the same test run alone passed `5 probed` immediately after.
Both probes named the failing tool and nothing else, so the occurrence left nothing to diagnose it with.
They now record each failing probe's exit status and combined output, which separates a binary that failed to run from one that ran and answered without a version.
**Still open:** the cause.
By hand the registry's probe for it, `kubectl version --client`, exits 0 and prints `v1.36.3` both in and out of the sandbox; the only difference in the failing run was sitting inside `run-parallel`'s 43-way fan-out, so saturation is the hypothesis and it stays **unverified**.
Whether a retry belongs here is undecided, and settling it needs the evidence the next occurrence now carries.

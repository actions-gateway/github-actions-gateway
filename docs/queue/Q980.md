---
id: Q980
rank: b47
labels:
    - tests
size: S
status: ready
target: ../../deploy/dogfood-e2e/overlays/kata/kustomization.yaml
---

# Assert the dogfood e2e overlays supply their own workerImage

Q740 removed the library's `.invalid` placeholder, so an overlay that lost its `workerImage` patch now gap-fills the default runner instead of failing at pull.
That image has no helm, kubectl or jq, so the suite would die mid-run.
Nothing checks it.

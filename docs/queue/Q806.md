---
id: Q806
rank: b30
labels:
    - flake
    - ci
status: deferred
size: S
target: ../../.github/workflows/security-scan.yml
---

# A transient syft download fails the SBOM step in scan and publish

**Event:** an SBOM step fails on `main` fetching syft after the fix. Both workflows pull the pinned tarball via `download-verified.sh` (digest-gated, jittered backoff since [Q829](Q829.md)), so the budget ran out, not one denial. → top of Queue.

---
id: Q943
rank: b00
labels:
    - ci
status: ready
size: XS
target: ../plan/merge-queue.md
---

# `doc-links-gate` runs on the candidate merge but nothing blocks on it

Q743 gave `doc-links.yml` its `merge_group` trigger and an aggregate `doc-links-gate` job, so the docs gates now see the queue's candidate merge.
The queue arbitrates on the ruleset's required checks alone, so a red verdict there is visible and not binding until `doc-links-gate` joins the nine in `default-protect` (id 17350763).
That is a repo-settings change and must follow the workflow onto `main`: registering a check no run reports wedges every merge ([merge-queue.md](../plan/merge-queue.md) Phase 2).
Measured 2026-08-18: 10 of 28 workflows declare `merge_group`; 9 contexts are required.

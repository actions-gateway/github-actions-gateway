---
id: Q716
rank: a7
labels:
    - security
    - tests
status: ready
size: S
target: ../design/05-security.md
---

# Assert a worker pod cannot reach the cloud metadata server

Three docs advise denying `169.254.169.254/32` and Q226 measured HTTP 200 from inside a Kata guest, but no test names the address. Kata bounds the kernel, not the pod network. Add a policy-enforcing-CNI negative assertion.

---
id: Q215
rank: b2h
labels:
    - feature
status: deferred
size: L
target: ../plan/caching-and-worker-storage.md
---

# Worker cache backend (actions/cache + Docker layer cache)

**Demand:** an in-cluster cache ask, OR [Q408](Q408.md) Phase 1 lands and removes the working one.
`actions/cache` reaches its store today, so the gap is local; the read-only mirror role cannot hold a read-write cache, which is the design question.

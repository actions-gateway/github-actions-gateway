---
id: Q677
rank: b15
labels:
    - bug
status: ready
size: S
target: ../../cmd/agc/internal/listener/session.go
---

# `CreateSession` sends a kind-unscoped runner name

It derives `<CR name>-<index>` for both kinds, but Q466 kind-scoped the *registered* name to `rs-<set>-<index>`, so a RunnerSet's `agent.name`/`ownerName` name no registered runner. Broker reaction unmeasured; it validates `agent.id`. From Q538.

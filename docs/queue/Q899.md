---
id: Q899
rank: aii
labels:
    - ci
    - debt
status: ready
size: S
target: ../../.github/workflows/publish.yml
---

# `publish.yml`'s image build has no retry, so a 403 fails a release

The half of [Q863](Q863.md) its fix deliberately left: same unretried base-image pull, but the release build is `no-cache: true` and five steps read `steps.build.outputs.digest`, one of them the signed-provenance subject.
A second attempt has to carry that digest through without ever signing the wrong one, which is why it is not a copy of the `security-scan.yml` change.

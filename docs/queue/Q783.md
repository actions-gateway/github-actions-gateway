---
id: Q783
rank: b1p
labels:
    - docs
status: ready
size: S
target: ../../CONTRIBUTING.md#skip-the-reflow-commit-in-git-blame
---

# Re-point `.git-blame-ignore-revs` at the squashed reflow commit

The file names `0acea95c`, the pre-merge branch SHA, which is not on `main`, so on git 2.55.0 it is a silent no-op rather than an error.
Re-point it at the squash `af3f9934`, and decide whether the v0.3.0 reflow `d3e58aeb` belongs too.

---
id: Q761
rank: b33
labels:
    - flake
    - tests
status: deferred
size: S
target: ../development/testing.md#a-backstop-shorter-than-the-subjects-own-budget-is-not-a-backstop
---

# `TestAbandonedProbe_Redispatched` bounds a verdict on 20s of wall clock

**Event:** a verdict wait fails on `main` after the fix (each derives its bound from the probe's own timeout, window and tails). Proven by construction: the old 20s bound expired inside a 43.3s budget. → top of Queue, escalated.

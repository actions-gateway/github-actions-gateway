---
id: Q819
rank: b00
labels:
    - ci
    - debt
    - retro
status: ready
size: S
target: ../../scripts/ci/run-parallel.sh
---

# `run-parallel.sh` labels gate output but never times it, so a slow gate hides

Measured 2026-08-12: `doc-links` sat at 161s in CI, 38s locally, unseen through three CI-speed rounds, because 30 fast gates run concurrently and the runner labels output without timing it. Emit per-label wall time; `run-parallel-test.sh` pins it.

---
id: Q570
rank: b3h
labels:
    - flake
    - tests
status: deferred
size: S
target: ../../cmd/gmc/test/e2e/migration_v1_to_v2_test.go
---

# `E2E_Migration_MigratedTenantReconcilesIntoAWorkingControlPlane` times out when the v1 proxy pool takes both workers

**Event:** the migrated proxy-pool wait times out on `main` after the Q570 fix (the tenant pins its inline pool with `WithProxyReplicas(1, 1)`, so its HPA cannot take the worker the migrated pool needs). → top of Queue, escalated.

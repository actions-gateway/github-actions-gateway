---
id: Q982
rank: a3
labels:
    - flake
    - tests
size: S
status: ready
target: ../../scripts/ci/check-template-library.sh
---

# check-template-library.sh races on printf into grep -q under pipefail

Line 334 pipes `printf` into `grep -qx`, which exits on first match and closes the pipe; `printf` takes SIGPIPE and `pipefail` fails the gate.
The dead pipeline reads as "no match", so entries that exist are reported missing.
CI only; 0/15 locally.

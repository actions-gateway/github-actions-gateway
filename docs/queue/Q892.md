---
id: Q892
rank: awr
labels:
    - ci
    - tests
    - debt
    - retro
status: ready
size: S
target: ../../scripts/docs/git-merge-plan-index-test.sh
---

# A suite that copies a gate into a fixture is invisible to grep

A suite that copies a gate into a fixture inherits its inputs, so changing one breaks it with no textual reference to grep.
Twice in `git-merge-plan-index-test`, both directions, at the Q889 cutover.
Name the consumers, or detect the copy.

---
id: Q910
rank: as9
labels:
    - ci
    - debt
status: ready
size: S
target: ../../scripts/manifest/check-promql.sh
---

# Parse the shipped dashboards' PromQL, not just the rules file

[check-promql.sh](../../scripts/manifest/check-promql.sh) pins `RULE_FILE` to `deploy/monitoring/prometheusrule.yaml`, so `promqlcheck` reads one file and the two shipped dashboards' 44 + 18 `targets[].expr` are parsed by nothing.
Measured 2026-08-18: replacing a tenant-dashboard expression with `sum by ((((` still passes `manifest-validate`, whose dashboard step is a `jq` syntax parse (Q827) that a malformed query string satisfies.
Same class as Q827 one step on: syntax is gated, the queries are not.
Distinct from [Q868](Q868.md), which gates the render.

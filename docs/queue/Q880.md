---
id: Q880
rank: b1s
labels:
    - ci
    - dogfood
    - retro
status: ready
size: L
target: ../../scripts/dogfood/validate-release.sh
---

# Validate release candidates in CI instead of a local dogfood gate

Auditable, keyed to the tag, and replaces the `refs/validated/` marker a maintainer's local gate writes today.
First milestone is Workload Identity Federation: CI has no GCP access, and a key contradicts the no-PEM feature.

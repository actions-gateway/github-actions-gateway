---
id: Q722
rank: b0t
labels:
    - tests
    - docs
status: ready
size: S
target: ../design/appendix-a-capacity-slos.md
---

# Re-measure per-session AGC memory against the scale-set listener

The ~12 KiB figure on four marketing surfaces comes from `mem_test.go`, which imports classic `agentpool`/`listener`, never `scalesetlistener`.
The pod-count claim is unaffected, and the sunset review suggests re-measuring strengthens it.

---
id: Q1067
rank: b43k
labels:
    - ci
    - debt
status: ready
size: S
target: ../../scripts/go/go-work-tidy.sh
---

# go-work-tidy.sh needs GNU realpath, which no tool check lists

`go-work-tidy.sh` line 103 runs `realpath --relative-to="." "$mod_dir"` for every module `go list -m all` returns. macOS ships a BSD `realpath` at `/bin/realpath` that rejects the flag (`realpath: illegal option -- -`), and `scripts/ci/check-tools.sh` does not list `realpath` at all, so `make doctor` cannot warn.
Measured 2026-09-07 on two Macs reviewing #1858: on one, `make tidy-check` exits 1 at that line before checking anything; on the other, the same target passes, because line 95's producer, `go list -f '{{if not .Main}}{{.Dir}}{{end}}' -m all`, exits 1 there with `go: can't compute 'all' using the vendor directory`, its stderr goes to `/dev/null` behind `|| true`, and the empty result means the loop never reaches line 103.
So on that host the dependency-ordering step is silently dead rather than passing, and the verdict depends on the host's `go` flags rather than on the tree.
CI runs it on `ubuntu-latest`, where `realpath` is GNU, so the gate is sound there and unmeasurable on the host it tells a contributor to reproduce on.

Two fixes, either sufficient: replace the call with a `--relative-to`-free form ([bash-style.md](../development/bash-style.md) prefers portable builtins), or add `realpath` to `check-tools.sh` with a GNU-flag probe the way it probes other tools' modes.
Doing the first makes the second unnecessary.
Found in review of #1858 (Q929); the PR did not touch `scripts/`.

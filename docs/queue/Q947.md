---
id: Q947
rank: b30
labels:
    - ci
    - debt
status: ready
size: S
target: ../../.github/workflows/status-lint.yml
---

# `status-lint`'s filter omits the two wrapper scripts its jobs run

Both lists name `queue.py` and `check-queue-rules.py` but not `scripts/docs/lint-queue.sh` or `scripts/docs/check-queue-rules.sh`, which are what the jobs actually `run:`, so a change confined to a wrapper skips the workflow that gates it.
[check-path-filters.sh](../../scripts/ci/check-path-filters.sh) cannot see it: assertion 3 reads pattern → disk, never disk → pattern.
Measured 2026-08-21 by scanning every workflow's `run:` script paths against its own filter — the only two real hits, the other two being comment mentions of `check-path-filters.sh` in the e2e lanes.

---
id: Q869
rank: a0
labels:
    - speed
    - ci
    - debt
status: ready
size: L
target: ../plan/q869-per-item-queue-store.md
---

# Store each backlog item in its own file with a computed priority order

Priority is stored as line position, so the top *k* rows conflict for every k ≥ 2; the ID-keyed driver never runs on GitHub, making the cost a rebase-forced CI re-run. [Plan](../plan/q869-per-item-queue-store.md).

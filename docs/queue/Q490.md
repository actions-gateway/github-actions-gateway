---
id: Q490
rank: b3m
labels:
    - tests
    - flake
status: deferred
size: S
target: ../development/testing.md#synchronize-on-the-signal-you-assert-on
---

# `TestAGC_Q260_FanoutCompletionReconciles` cancels a job every delivery completed

**Event:** it fails `cancelled` vs `completed` on `main` after the Q490 fix (the stub publishes `CompleteJobCalls` only after committing the accounting, and the test waits on `DeliveryResults`). → top of Queue, escalated.

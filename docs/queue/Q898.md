---
id: Q898
rank: b2zi
labels:
    - ci
    - debt
status: ready
size: S
target: ../../.github/workflows/unit-test.yml
---

# The other eight gates cannot say whether their heavy job ran

Every `*-gate` counts a `skipped` need as a pass, so a docs-only PR later given code reads green while unit, integration and security never saw it.
Q872 gave the two e2e lanes a job-summary line saying which; the other eight still need one.
That summary is what replaces the per-PR by-hand check in [testing.md](../development/testing.md#path-gated-workflows-verify-the-heavy-gates-actually-ran).

---
id: Q929
rank: a4
labels:
    - flake
    - ci
status: ready
size: M
target: ../../scripts/go/go-lint.sh
---

# The lint gate returns two verdicts on one tree

Measured 2026-08-18: `c99129aa` passed `lint` in run 32145897225 and failed in 32172968731, `nolintlint` calling two staticcheck directives unused in only one.
Same Go, cache key and scope. #1639 deleted the directives; the flake outlives them.

Not recurred: `main` has taken 28 green `unit-test` runs and no failure at all since 2026-08-18T18:47Z (measured 2026-08-23).
The two directives that exhibited it are gone and the tree now carries no `nolint:staticcheck` anywhere, so `nolintlint` has nothing staticcheck-shaped left to disagree about.
The 79 remaining directives are gosec accepts and whether the same nondeterminism reaches those is **unmeasured**, which is also why the non-recurrence is weak evidence either way.
Re-measure the surface before picking: the `M` was sized against a defect that currently has no way to express itself.

---
id: Q310
rank: b2p
labels:
    - feature
status: deferred
size: L
---

# Operator diagnostic aggregator (`gag status` / kubectl plugin)

**Demand:** operators ask for gateway diagnostics beyond raw kubectl + the runbook. Add a `gag status <gateway>` / kubectl plugin aggregating session, pool, and runner state per gateway.

---
id: Q559
rank: b3i
labels:
    - flake
    - tests
status: deferred
size: S
target: ../development/testing.md#the-two-effects-can-be-one-object-read-through-two-clients
---

# `TestV2_RunnerSet_CapacityGate_FixedClusterSkipsAcquire` acquires the job it must refuse

**Event:** it fails `reason=capacity` on `main` after the Q559 fix (the wait reads the condition through the reconciler's own cached client — the observer the admission rung reads — not the apiserver). → top of Queue, escalated.

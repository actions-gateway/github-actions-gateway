---
id: Q707
rank: b0a
labels:
    - debt
    - tests
status: ready
size: M
target: ../development/testing.md#stopping-a-run-name-the-target-never-the-program
---

# Ownership lease for local singleton tiers (e2e/kind slots)

Measured: of 38 local shell-kill targets only 2 carried a worktree anchor; the rest match every session (Q690 killed the `make check` verifying it). e2e/kind slots are singletons with no ownership record, so reclaim guesses.
Copy `lib/lease.sh`.

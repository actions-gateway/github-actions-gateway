---
id: Q573
rank: b16
labels:
    - ci
    - tests
    - retro
status: ready
size: S
target: ../../api
---

# Detect served API fields nothing reads

Measured 2026-08-08: only 2 of 106 served v2beta1 fields were unread (`Sharing`, `allowedNamespaces`), both Q166's, and Q166 has landed, so the gate baselines empty. Q526's unwritten *status* field: spec-only scope misses it.

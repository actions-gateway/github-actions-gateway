---
id: Q895
rank: a3
labels:
    - ci
    - tests
    - debt
    - retro
status: ready
size: S
target: ../../scripts/go/go-test.sh
---

# A cached Go test result let `make check` pass what CI failed

Measured 2026-08-17 on Q889 phase 6: `make check` reported `ok … devtools/agent/pipedgate (cached)` and exited 0, while the same package run directly failed 5 assertions and CI's `unit-test` failed with it.
The suite reads `.claude/piped-gate-guard.json` at runtime, which the change had edited.
Cache invalidation is the suspected mechanism and is unverified.
The fix then tripped `no-plan-refs-check` twice, so the cold-cache re-run earned its place.
A green gate that ran no test is the class the repo already names.

---
id: Q895
rank: a2i
labels:
    - ci
    - tests
    - debt
    - retro
status: ready
size: S
target: ../../scripts/go/go-test.sh
---

# A cached Go test result let `make check` pass what CI failed

Measured 2026-08-17 on Q889 phase 6: `make check` reported `pipedgate (cached)` and exited 0 while the package run directly failed 5 assertions, as did CI.
The suite reads its registry at runtime; the cause is unverified.

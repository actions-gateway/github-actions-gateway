---
id: Q856
rank: b1b
labels:
    - ci
    - debt
status: ready
size: S
target: ../development/testing.md#path-gated-workflows-verify-the-heavy-gates-actually-ran
---

# Nothing reconciles a `*-gate` job's `needs` with its workflow's job ids

An omitted job runs and reports red while the required gate reports green, so it blocks nothing.
Q845 fixed the one instance (`uses-pinned`); a sweep of 9 aggregators found no other.
Same workflow parser as [Q574](Q574.md).

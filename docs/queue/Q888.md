---
id: Q888
rank: b1w
labels:
    - debt
    - retro
status: ready
size: S
target: ../../claude-usage/compute_metrics.py
---

# Refuse a merge into a CSV whose classifier has changed

`merge_max_into` only revises upward, so a sharpened classifier's downward correction is dropped silently, and the total still rising on new data hides it. Docstrings say rebuild; nothing enforces it.

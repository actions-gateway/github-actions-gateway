---
id: Q904
rank: air
labels:
    - ci
    - debt
status: ready
size: S
target: ../../Makefile
---

# Nothing gates a `.build/` tool rule declaring its version-pin file

Q842 set the pattern and a Makefile comment states it, but no gate holds it, so cosign was added without one and served a stale verifier until Q857.
The next non-Go pinned tool repeats it.

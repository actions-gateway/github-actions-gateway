---
id: Q736
rank: b1e
labels:
    - ci
    - tests
status: ready
size: S
target: ../../cmd/gmc/Makefile
---

# No zero-match guard on the test-integration `RUN=` filter

Measured 2026-08-08: `go test -run <nonsense>` exits 0 printing `[no tests to run]`, so a mistyped `RUN=` reads as a pass. `make test`/`make e2e` now fail on zero match (Q680/Q679); the two module targets need a shared wrapper to do the same.

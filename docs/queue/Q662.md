---
id: Q662
rank: b11
labels:
    - debt
status: ready
size: S
target: ../../.claude/foreground-guard.json
---

# foreground-guard registers the fast e2e targets as 30-minute commands

Every `e2e-*` target is registered at 30 min; only `make e2e`/`e2e-up` are slow (`e2e-github-cleanup` takes seconds).
`foreground-guard-patterns-test.sh` asserts the fast ones as must-match, so narrowing means flipping them.
Runtimes unmeasured.

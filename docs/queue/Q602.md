---
id: Q602
rank: b3d
labels:
    - flake
    - tests
status: deferred
size: S
target: ../development/testing.md#synchronize-on-the-signal-you-assert-on
---

# `TestListener_AbandonedJobDoesNotSurviveARestart` stops the listener before the abandon's message delete is issued

**Event:** it fails `Condition satisfied` on `main` after the Q602 fix (the test now waits for `deleteAttempts > 0`, the delete the invariant rests on, not only the abandoned counter, which rises at in-memory settle). → top of Queue, escalated.

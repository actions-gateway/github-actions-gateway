---
id: Q642
rank: b3e
labels:
    - flake
    - tests
status: deferred
size: S
target: ../development/testing.md#the-clock-is-as-often-a-deadline-around-a-process-as-a-sleep-inside-one
---

# `progress-watch-test`'s `interval 0` case fails under the fan-out

**Event:** the `interval 0` case fails on `main` after the Q642 fix (it asserts on ticks a stubbed `sleep` records; no deadline bounds the watcher). Reproduced: 8/1760 launches alive at the old 10 s deadline. → top of Queue, escalated.

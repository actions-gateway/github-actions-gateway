---
id: Q664
rank: b3c
labels:
    - flake
    - tests
    - ci
status: deferred
size: S
target: ../design/02-architecture.md
---

# `E2E_AGC_StuckPendingPodReaped` times out, the workers never reaped

**Event:** it times out on `main` after the Q664 fix (the reconcilers cap work-queue retry at 30s, so a reconcile error cannot strand the reap deadline `RequeueAfter` carries).
Not reproduced locally in 4 runs. → top of Queue, escalated.

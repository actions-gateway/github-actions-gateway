---
id: Q866
rank: ag
labels:
    - ci
    - debt
status: ready
size: S
target: ../../devtools/docs/metrictiers/main.go
---

# `metrictiers` reads its ledger table with a hand-rolled scan, not the shared parser

Q612 moved every Markdown gate onto `devtools/docs/markdown`; this one shipped after and scans for `\|`-prefixed lines. Measured 2026-08-14: a fenced row in the section counts as a ledger entry and fails the gate. Q850's sibling uses the parser.

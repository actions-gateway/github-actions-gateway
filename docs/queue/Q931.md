---
id: Q931
rank: ajj
labels:
    - ci
    - debt
status: ready
size: M
target: ../../.github/workflows/e2e-reusable.yml
---

# Make the e2e bake's `type=gha` cache real, or drop the dead config

Q900 measured it: a `run:` step sees no `ACTIONS_RUNTIME_TOKEN` or `ACTIONS_RESULTS_URL`, before *or* after `docker/setup-buildx-action` v4.2.0 (probe run 32186577410).
Buildx v0.36.1 then drops both cache entries silently, which is why the bake is green and caches nothing.
The control is `security-scan.yml`: its `docker/build-push-action` does hold the token, and emits an `importing cache manifest from gha:` vertex absent from all three e2e bakes read here.
Nothing writes `scope=images`, so `deps` recompiles everywhere.
102.6 s / 99.5 s / 99.4 s on e2e runs 32145897962, 32142595219, 32143847937; 78.2 s / 81.9 s / 82.0 s on three trivy shards of run 32186370352.
Both repairs are decisions rather than edits: `crazy-max/ghaction-github-runtime` adds a third-party action, and `docker/bake-action` costs the backgrounded bake.
`docs/plan/e2e-ci-speed-round-2.md:100` and the `cache-from` comment in `security-scan.yml` still assert the restore works; correct both with whichever repair lands.

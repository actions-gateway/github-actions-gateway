---
id: Q596
rank: b3f
labels:
    - flake
    - ci
status: deferred
size: S
target: ../development/testing.md#the-v2-api-sync-gate
---

# `check-v2-api-sync-test`'s `tree-in-sync` fails under the fan-out

**Event:** `tree-in-sync` fails on `main` after the Q596 fix. Trigger unreproduced (2418 runs under load); what shipped is evidence — a failed read reads as trouble, not drift, and the assertion prints the gate rc and output. → top of Queue.

---
id: Q794
rank: b0n
labels:
    - debt
status: ready
size: S
target: ../operations/migration-v1-to-v2.md
---

# `gag-migrate` no longer needs to pin `acquisitionProtocol: Classic`

It writes Classic onto every set it emits, whose stated reason — keeping multi-label matching — Q726 removed. Now conservative rather than necessary, but flipping it changes a migrating tenant's protocol silently, so it is a decision, not a cleanup.

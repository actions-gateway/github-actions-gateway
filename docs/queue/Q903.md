---
id: Q903
rank: aii
labels:
    - ci
    - debt
status: ready
size: S
target: ../../Makefile
---

# Nothing gates `COSIGN_VERSION` against `publish.yml`'s `cosign-release`

Only a comment keeps the two in step, so `make verify-release` can use a verifier the publish job never signed with.
Unlike kind, polaris, shellcheck and syft, cosign has no updatecli manifest.

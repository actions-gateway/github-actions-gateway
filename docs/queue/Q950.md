---
id: Q950
rank: b42
labels:
    - docs
    - debt
status: ready
target: ../../scripts/docs/em-dash-baseline.txt
size: XS
---

# 19 em-dash ceilings overstate their file's count by 91 in total

The baseline documents itself as freezing each listed file at its current count, and 19 of its 228 entries no longer do: measured 2026-08-21 on `main`, they sit 91 above the tree, 27 of that in `testing.md` alone.
A ceiling drifts up whenever a file loses em-dashes without a regeneration, which is every commit that cuts prose without running `make em-dash-baseline`.
The diff ratchet added for Q742 makes the gap inert, since it fails a gain against the base count rather than the entry, so this is legibility rather than a hole: the entries misreport, and the `make em-dash-baseline` diff is [Q650](Q650.md)'s measure of what it cleared.
Regenerate, and read the diff as the cleanup already banked.

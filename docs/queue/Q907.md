---
id: Q907
rank: b2zj
labels:
    - docs
    - debt
status: ready
size: S
target: ../development/maintaining-backlog.md#retiring-a-flake-watch-row
---

# CLAUDE.md sends a fixed flake row straight to the retired ledger

CLAUDE.md's closing checklist says a `flake` item "moves to the retired ledger instead of being deleted", skipping the first of [maintaining-backlog.md](../development/maintaining-backlog.md#retiring-a-flake-watch-row)'s two stages: mitigation shipped goes to Deferred § Flake watch and the row stays here, and only soaked or obsolete retires, during grooming and never automatically.
Measured 2026-08-18: an orchestrator gave two workers in one batch the ledger answer, then self-corrected.
CLAUDE.md loads into every session, so the compressed line is the reachable one.

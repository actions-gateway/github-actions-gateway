---
id: Q858
rank: al
labels:
    - ci
    - debt
status: ready
size: S
target: ../development/maintaining-backlog.md#a-hand-resolved-conflict-drops-rows-the-markers-never-named
---

# Reconciling a hand-resolved keyed merge's row set has no automated form

Q840 shipped the manual `comm` against `ORIG_HEAD`; no CI gate can run it, since `ORIG_HEAD` is gone by push time. Needs a local form. Measured: rule 8 covers `flake` only, so every other dropped row passes clean.

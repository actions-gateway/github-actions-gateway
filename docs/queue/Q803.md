---
id: Q803
rank: b32
labels:
    - flake
    - ci
    - tests
status: deferred
size: S
target: ../development/testing.md#coverage-measurement-and-the-ratchet
---

# `cmd/proxy` coverage is nondeterministic and its floor has no headroom

**Event:** `cmd/proxy` fails `cover-check` on `main` after the fix. Tolerance is now max(0.5pp, 3 statements) = 0.86pp here, against a floor of 79.3 that runs actually reach, so a breach costs 3 statements, not Q803's 2. → top of Queue, escalated.

---
id: Q825
rank: ad
labels:
    - flake
    - tests
    - bug
status: ready
size: S
target: ../../scripts/agent/claude-piped-gate-hook-test.sh
---

# The no-toolchain case flakes twice over: pipeline status, and a rebuild

`rc=$?` at :150 is the pipeline's status, not the hook's (`jq` SIGPIPEs).
It also `rm -f`s `.build/pipedgate`, rebuilt under it by the session's own Bash calls ([why](../development/testing.md#the-make-check-pre-review-gate)).
5 sightings.

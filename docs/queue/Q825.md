---
id: Q825
rank: ab
labels:
    - flake
    - tests
    - bug
status: ready
size: S
target: ../../scripts/agent/claude-piped-gate-hook-test.sh
---

# Two mechanisms flake the no-toolchain case: pipeline status, and a shared binary rebuilt under it

2026-08-12: `rc=$?` at :150 is the pipeline's status, not the hook's (no stdin read, so `jq` can SIGPIPE). 2026-08-14: the case `rm -f`s the shared `.build/pipedgate`, which a concurrent Bash-hook run rebuilds, so `out` is a real deny. 3 sightings.

---
id: Q604
rank: b3c
labels:
    - flake
    - tests
status: deferred
size: S
target: ../../scaleset/scalesetstub/stub.go
---

# `stallJob` installs its runner-name conflict after the job is already pollable

**Event:** a `stallJob` caller fails `must be held for a re-offer` on `main` after the Q604 fix (`EnqueueStalledJob` registers the conflict in the same locked section that makes the job pollable). → top of Queue, escalated.

---
id: Q814
rank: aq
labels:
    - ci
    - debt
    - retro
status: ready
size: S
target: ../../scripts/agent/pr-requeue-eligible.sh
---

# An ordinary `DIRTY` wake captures no conflict set, because `--assess` refuses before it probes

Measured 2026-08-12 on #1431: `--assess` refuses at `human_enqueued`, ahead of the probe, so it records `base_oid -`. Q810 captures a queue eviction only, not an ordinary DIRTY wake ([the class](../development/parallel-dispatch.md#conflict-policy)).

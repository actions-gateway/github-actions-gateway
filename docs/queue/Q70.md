---
id: Q70
rank: b2d
labels:
    - security
    - ci
status: deferred
size: S
---

# Flip worker-image trivy leg to blocking

**Event:** upstream `actions-runner` base scans clean. The worker leg of [security-scan.yml](../../.github/workflows/security-scan.yml) is report-only (~36 upstream HIGH/CRITICAL CVEs); when a bump clears them, set its `exit-code` to `1`.

# Plans

Topic-organized index of plan files.
For current status and priorities, see [docs/STATUS.md](../queue/README.md).

Each file is a self-contained plan with rationale, scope, and (where appropriate) a status table near the top.
Authoritative state always lives in the individual file.

Legend: ✅ done, ⚠️ partial / mixed (open **Queue** item remains), 💤 deferred (parked with a trigger, tracked in [deferred in the backlog](../queue/README.md)), ❌ open, ⓘ informational (forward-looking spec or design rationale, no progress to track).
A plan with only deferred residuals is ✅, not ⚠️ — see [maintaining-backlog.md](../development/maintaining-backlog.md#an-open-marker-means-an-open-item-remains--deferred-residuals-dont-count).

## Implementation roadmap

The five-milestone delivery from [docs/design/06-implementation-phases.md](../design/06-implementation-phases.md).

| Plan | Scope | Status |
|---|---|---|
| [milestone-1.md](milestone-1.md) | Wire-protocol probe; broker + githubapp packages | ✅ Done |
| [milestone-2.md](milestone-2.md) | AGC controller, reconciler, agent pool, token manager | ✅ Done — full session lifecycle exercised end-to-end by M3's real-GitHub dispatch e2e; goleak coverage landed |
| [milestone-3.md](milestone-3.md) | Worker pod, Named Pipe handoff, pod provisioner, eviction retry | ✅ Done — Investigation A (Named Pipe) complete; Q6 live-GitHub dispatch validated 2026-05-30 |
| [milestone-4.md](milestone-4.md) | GMC, ActionsGateway CRD, proxy binary, webhook, TLS pinning | ✅ Done — all success criteria live-validated on a real `kind` cluster 2026-06-11/12 (§12) |
| [milestone-5.md](milestone-5.md) | Hardening + 1,000-session load testing + posture audit + packaging | ⚠️ Packaging (Q12) now live-validated end-to-end (Q219, §1.5 — found+fixed an egress-proxy registration bug); load harness (Q13), polaris + kube-bench (Q14) shipped. Only staging-cluster residuals remain: 1,000-session proxy-HPA-under-burst + gVisor isolation ([Q15](../queue/Q15.md), deferred) |
| [release-1.0.md](release-1.0.md) | The 1.0 release gate: multi-arch images + cosign-signed Helm chart, GA install docs, public website launch, pinned-digest GitHub Release | ✅ Done — 1.0 SHIPPED 2026-06-16 (`v1.0.0` live on GHCR, verified end-to-end against the published artifacts) |
| [release-1.3.md](release-1.3.md) | The 1.3 release gate: worker right-sizing as the headline capability, plus the one-release-ahead deprecation notice that lets `v2.0.0` remove `v1alpha1`, `v2alpha1`, and the classic acquisition machinery together | ✅ Done — 1.3 SHIPPED 2026-08-03. Every gating ledger item closed, and `validate-release.sh v1.3.0-rc.5` passed the dogfood validation on the day the tag was cut. `v1.3.0` is published off `main` as a final release with the signed v2 CRD bundle, the `gag-migrate` binaries, and [curated notes](../releases/v1.3.0.md) |
| [release-1.4.md](release-1.4.md) | The 1.4 release gate: why the minor was forced by merged work before anyone chose it, the three admitted items (Q166, Q554, Q691), and why the proxy-hardening cluster stays whole for 1.5 | ✅ Scope complete. All three gate items shipped (Q166 in #1347, Q554 in #1344, Q691 in #1336), and the three blockers a 2026-08-09 audit found are all closed: the no-PEM credential row (#1362), [Q747](../queue/Q747.md)'s probe, whose enforcer was dead rather than its policy wrong (#1363), and the unreplaced `workerImage` failure mode that [Q714](../queue/Q714.md) contradicted (#1361). Awaiting the release-candidate dogfood validation in [release.md](../operations/release.md), which needs the actual RC image and so is deliberately not a Queue row |
| [release-1.5.md](release-1.5.md) | The 1.5 release gate: two defects in already-claimed capabilities (Q712 unwired runner-group binding, Q713 missing scale-set-tier latency series), plus the candidates held but not yet accepted | ✅ Shipped as `v1.5.0` on 2026-08-15, validated on `rc.3`. All four gate items landed 2026-08-11 (Q713 in #1390, Q712 in #1391, Q715 in #1392, Q726 in #1393), taking the floor to MINOR, and the [marketing reconciliation](release-1.5.md#in-scope-reconcile-the-marketing-surfaces) closed 2026-08-12 (Q801, Q821). Q776 closed the last one: the [acquisition-tier ledger](../operations/observability-metrics.md#acquisition-tier-reach) carries all 53 `actions_gateway_*` series with the tier that emits each, `make metric-tiers-check` holds it to the source in both directions, and the re-walk it required found two classic-only series on no list plus one that reached no operator doc. Only the release-candidate dogfood validation in [release.md](../operations/release.md) remains, which needs the actual RC image and so is deliberately not a Queue row |
| [v2-ga.md](v2-ga.md) | The last graduation rung (`v2beta1` → `v2`) and the `v2.0.0` release that executes the three coupled removals: soak criteria, the graduation hop, removal sequencing, docs | 💤 Parked — every phase is event-gated, so no Queue row remains: the soak and the hop start after `v1.3.0` ships ([Q413](../queue/Q413.md)), Phase 3's removals ride [Q273](../queue/Q273.md)/[Q264](../queue/Q264.md), and the Phase 2 alias decision is [Q452](../queue/Q452.md) |

## Security

| Plan | Scope | Status |
|---|---|---|
| [security.md](security.md) | OWASP-style code review with finding-level workstreams | ✅ Done — every workstream shipped; sole residual is the deferred live Ed25519 probe (M-11b, [Q11](../queue/Q11.md)). Phase 1 live `kind` validation covered by the M3/M4 live runs |
| [security-audit-2026-06.md](security-audit-2026-06.md) | Second full security audit (2026-06-12): GMC tenant isolation & provisioning, proxy/broker/probe network trust boundaries, CI/CD supply chain, AGC credential/crypto — four parallel tracks + `make vulncheck` | ✅ Done — every High/Medium finding resolved (Q121–Q130); remainders intentionally deferred |
| [worker-egress-proxy.md](worker-egress-proxy.md) | Worker traffic must route through per-tenant proxy pool | ✅ Done — NetworkPolicy split shipped (commit `4932ce7`); proxied worker→GitHub egress live-validated via M4 §12 |
| [q243-egress-ip-reference-arch.md](q243-egress-ip-reference-arch.md) | Per-tenant egress-IP reference architecture: how the proxy pool's per-tenant choke point is bound to a distinct, stable source IP at GitHub — Cilium Egress Gateway vs per-tenant cloud NAT — plus single-tenant-direct (dogfood) vs production multi-tenant topology, cost model, and a deferred live-validation plan | ⓘ Design reference — design + full live validation done: per-range Cloud NAT gives distinct+stable per-tenant IPs ([campaign](q243-q245-q230-live-validation-campaign.md)), and a real GMC-provisioned EgressProxy pins to a single per-tenant egress IP (live PASS 2026-07-13, `scripts/dev/validate-egress-ip.sh`). Approach-A (Cilium EG) fail-open/kill-switch/SNAT-headroom validation stays deferred |
| [q245-fqdn-intent-backend-split.md](q245-fqdn-intent-backend-split.md) | Decouple tenant egress intent (`CIDR`\|`FQDN`) from the operator-chosen FQDN backend (`--fqdn-policy-backend=none\|cilium\|calico\|gke`), killing the per-CNI enum fragmentation; add a `gke` backend emitting `networking.gke.io FQDNNetworkPolicy`, with the union-vs-default-deny composition invariant | ⓘ Design reference — split + `gke` backend shipped (#576) and **live-GKE validated** (enforcement + fail-closed, [campaign](q243-q245-q230-live-validation-campaign.md)); Phase-3 `aks`/`eks` cluster-scoped backends remain a separate optional follow-up |
| [q243-q245-q230-live-validation-campaign.md](q243-q245-q230-live-validation-campaign.md) | One coordinated live-GKE DPv2 spike batching three deferred egress residuals: Q245 FQDNNetworkPolicy enforcement, Q230 DNS-under-egress-NP, Q243 per-tenant egress IP (cloud NAT) — on one throwaway cluster, torn down same-session | ⓘ Evidence record — Q245 PASS, Q230 PASS, Q243 mechanism PASS + API-binding gap (since closed — full pinning [live PASS 2026-07-13](q243-egress-ip-reference-arch.md#result--pass-2026-07-13)); cluster torn down 2026-07-07 |
| [competitive-analysis-2026-08.md](competitive-analysis-2026-08.md) | The evidence behind the 2026-08-06 marketing corrections: which differentiators are durable and why the pre-claim seat is not, the criteria no comparison in this space covers, under-claims still unfixed, ideas deliberately rejected, why the verdict table drifted, and the per-cell evidence each ARC-column stamp rests on | ⓘ Research record. Corrections shipped; several findings unactioned, listed in its Still open section |
| [secure-multi-tenant-oss-ci.md](secure-multi-tenant-oss-ci.md) | The umbrella goal: a map, a threat model for the untrusted contributor, a definition of done, and the residual risk accepted. Delegates all design to q408 and does not restate it | ⓘ Ongoing goal doc. Kernel isolation and the pod floor are shipped; the network, metadata, cache, and evidence layers are open ([Q408](../queue/Q408.md), [Q716](../queue/Q716.md)) |
| [caching-and-worker-storage.md](caching-and-worker-storage.md) | The umbrella goal for the three caches routinely conflated as one (image pulls, `actions/cache`, build layers) plus RWX job storage: why closing untrusted-PR egress removes the cache that works today, why one backend can still serve several content classes, and why the read-only mirror contract cannot cover a writable job cache | ⓘ Ongoing goal doc, nothing built. States that `actions/cache` already works and the roadmap said otherwise; reframes [Q215](../queue/Q215.md)'s isolation review as the design, and widens [Q539](../queue/Q539.md)/[Q540](../queue/Q540.md) to grade the contract per content class |
| [gpu-and-accelerated-ci.md](gpu-and-accelerated-ci.md) | The umbrella goal for accelerated CI: what already ships, why GPU and Kata do not compose on cloud machine families, and why gang scheduling is the one place the pre-claim gate is structurally defensible | ⓘ Ongoing goal doc. Foundations shipped (scale-to-zero, priority floors, GPU-safe right-sizing); [Q216](../queue/Q216.md) and [Q718](../queue/Q718.md) are demand-gated on purpose |
| [release-ladder.md](release-ladder.md) | The map between the individual release plans: how many releases stand between here and `v2.0.0`, what each carries, why 1.6 exists rather than folding into 1.5, and what is deliberately punted past 2.0 with the signal each waits on | ⓘ Ongoing goal doc. Shape decided 2026-08-09: 1.5 → 1.6 → 2.0. Establishes that the roadmap's near-term section means "committed to a named release", applied 2026-08-09: seven items parked to Deferred, [Q719](../queue/Q719.md) and [Q727](../queue/Q727.md) gated to 1.6 |
| [arc-parity.md](arc-parity.md) | The umbrella goal for the short list where a team on ARC cannot migrate without editing workflows or losing a capability: the measured gap inventory, what is deliberately never parity, and why `containerMode` waits on RWX storage | ⓘ Ongoing goal doc. Q712 and Q726 both closed 2026-08-11; [Q727](../queue/Q727.md) is sequenced behind [Q719](../queue/Q719.md), and [Q765](../queue/Q765.md) needs an appliance |
| [q408-untrusted-pr-egress.md](q408-untrusted-pr-egress.md) | Untrusted-PR posture for Kata workers (G.14): in-cluster registry pull-through mirrors (the Athens pattern applied to images) + delete the e2e open-egress NetworkPolicy, so a Kata worker's reachable set is DNS + GitHub + mirror only | ❌ Open — Phase 0 measured 2026-08-03; Phase 1 (hosted-only gating of `get.helm.sh` + the Actions cache data plane) implemented, its live validation run next, then Phase 2 mirror manifests ([Q408](../queue/Q408.md)) |

## Test plans

Per-milestone test gap plans.
The durable design rationale for what the unit/integration/e2e layers cover lives in [`docs/design/07-test-plan.md`](../design/07-test-plan.md); developer run commands live in [`docs/development/testing.md`](../development/testing.md).

| Plan | Scope | Status |
|---|---|---|
| [test-progress-visibility.md](test-progress-visibility.md) | Make a long test run legible while it is still running, for every audience that waits on one — a human at a terminal, a reviewer reading a CI log, and an agent running it in a background task. End state: release validation runs as a background task by default, reporting progress as it goes | ✅ Done — Phase 0 shipped ([#1152](https://github.com/actions-gateway/github-actions-gateway/pull/1152), Q608): the e2e suite now emits a spec heartbeat and renders its JUnit report into the job summary, covering both CNI lanes and both runner types. Phase 1 shipped (Q615): `validate-release.sh` announces each phase, relays the dispatched run's spec heartbeat into the operator's terminal, and renders its JUnit report there on the way out — which meant replacing `gh run watch`, since it blocks and a heartbeat cannot be interleaved from inside it (it does *not* garble non-TTY output; that was measured and the tempting stronger claim dropped). Phase 2 shipped its mechanism half (Q616): the same stream renders to `tmp/release-validation-status.json` — one object holding phase, both clocks, the newest relayed e2e heartbeat, and the phase that failed *first* rather than the teardown that followed it — and `release-sentinel.sh` sleeps against it, exiting on a transition, a verdict, or a gate that has gone quiet, so an agent reports what the gate did rather than what a clock said. Phase 2 completed with Q617, which made that flow release.md's documented default and the terminal run the alternative — and surfaced the requirement the mechanism half had not: a detached run must carry `ASSUME_YES=1`, since the gate's target-confirmation reads stdin and a detached run has none (measured: it exits 1 before spending anything). Phase 3 shipped (Q618): `go test -json` streams through a renderer that rebuilds the plain test log and interleaves a heartbeat naming what is still running, with no test-code change — the package denominator comes from `go list` (~0.3 s), because the `-list` pre-pass a test-level one needs measured 22 s wall / 131 s CPU and serializes the compile the single invocation exists to overlap. Two things ruled out on measurement, not intuition: the integration tier needs nothing (30–64 % output density — it already narrates itself), and migrating other suites to Ginkgo would make progress reporting **worse**, since `go test -json` sees 73 Ginkgo specs as 1 test but 68 plain tests as 68 |
| [milestone-1-tests.md](milestone-1-tests.md) | M1 unit-test coverage gaps | ✅ Done — all five gaps closed |
| [coverage-to-75-per-module.md](coverage-to-75-per-module.md) | Every Go module's hand-written unit-test coverage to ≥75% (Q255) | ✅ Done — all 8 code modules ≥75% (probe/gmc reached via a `runProbe`/fake-client refactor + tests) |
| [eviction-oversubscription-validation.md](eviction-oversubscription-validation.md) | Five experiments that turn the eviction-recovery and oversubscription claims into measurements: unconfounded eviction latency, the node-drain path, the oversubscription/preemption demo, the quota gate under contention, and the tiers-off/tiers-on utilization delta | ⚠️ Open — filed 2026-07-26 from the [#819](https://github.com/actions-gateway/github-actions-gateway/issues/819) review. Experiment 4 ran in full — envtest half 2026-07-26 (both acquisition tiers), live-GitHub half 2026-07-31: GitHub redelivered the declined job and a sibling gateway ran it green on attempt 1, and redelivery turned out to be a ~7/s loop; experiment 2 (the node drain) ran 2026-07-27 — neither tier re-runs a drained worker, residual tracked as [Q459](archive/q459-drained-worker-recovery.md); experiment 3 (preemption) ran 2026-07-29 — a `PriorityClass` preemption is a graceful delete, not a kubelet eviction, so it reached no recovery either; its residual Q497 shipped the same day ([plan](archive/q497-preemption-recovery.md)). experiment 1 (eviction latency) ran on both acquisition tiers — classic 2026-07-29 and re-measured 2026-08-03, scale-set 2026-08-03, all against the published v1.3.0 images: 9m36-9m45s eviction→conclusion, attributed to the job lock's TTL rather than anything tier-specific, with the Q503 retry loop measured absorbing GitHub's refusal window over 20 paced calls on each tier. Only experiment 5 remains ([Q424](../queue/Q424.md), deferred) |
| [q511-live-github-run-isolation.md](q511-live-github-run-isolation.md) | Two live-GitHub e2e runs in flight at once collide on shared GitHub-side state (the same fixture repo, workflow, and runner name) without either failing, and a run killed with `kill -9` strands a registration that poisons the next one | 🚧 In progress — decided forbid-over-isolate 2026-07-31: the suite's `BeforeAll` preflight and `make e2e-github-cleanup` shipped. Open for the isolation half, deferred to [Q530](../queue/Q530.md) because it needs the fixture workflows in another repo parameterized |
| [q549-scaleset-rerun-flake.md](q549-scaleset-rerun-flake.md) | The two failure modes behind one `E2E_AGC_ScaleSetDrainedWorkerClaimAndRerunLandUnderChartRBAC` timeout: the AGC being replaced inside the claim window, and an undisturbed window whose re-run still never fires | ⚠️ Both modes diagnosed — A mitigated by the UID pin + re-stage (PR #1120); B attributed 2026-08-12 after three occurrences, one on `main`: the disruption was detected and the *claim* lost, to the pod's own deletion or to a conflict from the kubelet's terminal-status write, neither of which is a role or discriminator defect. Fixed under [Q809](../queue/Q809.md); the row stays in Flake watch for the soak ([Q549](../queue/Q549.md)) |
| [q300-gmc-kindnet-e2e-flake.md](q300-gmc-kindnet-e2e-flake.md) | Systemic kindnet `e2e / e2e` leg flakiness: different unrelated specs fail run to run — the original CFS-throttling root cause (kind's 100m CPU limit on kindnetd starving its in-band kube-network-policies enforcer) is fixed and confirmed closed; one unexplained NetworkPolicy fail-open event (07-15) remains under watch | ❌ Open — throttle mechanism closed (0 throttled in every post-fix dump); failure dump upgraded (nfqueue/memory counters, full-window logs) to attribute the next occurrence ([Q300](../queue/Q300.md)) |
| [q820-scripts-test-temp-file-flake.md](q820-scripts-test-temp-file-flake.md) | `git-merge-plan-index-test` dies on a git temp-file error under the `make check` fan-out and passes on rerun: the exclusion record, so the next sighting starts from what is already ruled out rather than re-deriving it | ❌ Open — sighting 4 read every throwaway repo intact at the moment of failure, so the `rm -rf`-racing-a-live-git family that matched the signature is a look-alike, not the cause; no mechanism yet explains a temp-file creation that fails with the tree present ([Q820](../queue/Q820.md)) |

## Speed improvements

Performance plans for build and test pipelines.
Each has inline ✓ markers per item.

| Plan | Scope | Status |
|---|---|---|
| [merge-queue.md](merge-queue.md) | Adopt GitHub's merge queue on `main` to kill the freshness-invalidation loop: every merge (~every 25–30 min) invalidates every in-flight green PR, and re-validation costs a full e2e cycle (a third+ of e2e compute measured as redo). Queue runs the gates once per batch on the candidate merge result | ✅ Done, all three phases. Phase 1 (#1256) and Phase 2 (ruleset `merge_queue` rule, verified live) landed 2026-08-03 with the queue-era process-doc rewrite; Phase 3 re-measured 2026-08-12 and demoted both e2e lanes to merge-group-only, declining the three STATUS-contention candidates |
| [docker-image-speed.md](docker-image-speed.md) | Image build + load-into-kind time | ✅ Done — every item shipped (§1/2/4/5/8/9/13) or explicitly 🚫 not pursued (§7/12); §3/6/10/11 obsoleted by vendoring + in-cluster registry |
| [e2e-tests-speed.md](e2e-tests-speed.md) | E2E suite + CI-pipeline speed improvements | ✅ Done — Round 1 (§1–§14) and Round 2 (§15–§18) all shipped (the top-of-file TOC ✓ markers lag the authoritative status tables) |
| [e2e-ci-speed-round-2.md](e2e-ci-speed-round-2.md) | `e2e / e2e` job wall time after the first two speed plans: the image bake and the suite's serial tail | ✅ Done — all six items shipped: image-build consolidation (§1–3, one root Dockerfile + shared cacheable `deps` stage), the disk-cleanup overlap (§4), and the serial-tail work (§5–6). §6's metrics-server drop was measured and reverted; the kcm sync period is declined on flake risk |
| [ci-wait-time-analysis.md](ci-wait-time-analysis.md) | **Start here before scoping CI or test-tier speed work.** Dated current-state table, an index of the rounds already run, the four traps that cost an attempt each, and what is left | ⓘ Standing reference, not a work item. Records that CI execution is 5% of PR latency, that the tiers below `integration-test` are a flat profile rather than a queue, and that the suite's parallel phase was measured healthy at 4.5× in round 2. Keep the current-state table dated when a round lands |

## Deployment

| Plan | Scope | Status |
|---|---|---|
| [gke-dogfood.md](gke-dogfood.md) | On-demand GKE cluster for dogfooding GAG's own CI — GCP setup, GAG install, workflow variable toggle, start/stop/teardown runbook | ✅ Complete (2026-07-07) — turn-up + per-job-green + concurrent-matrix-green on the ScaleSet default (Q224 closed via [Q264](../queue/Q264.md) P4, #545); v2beta1 dogfood path live (Q231). Turn-up findings Q246/Q247/Q254/Q259/Q260 all resolved. Stays as the living operational runbook; chronological turn-up history split to [archive/gke-dogfood-turnup-findings.md](archive/gke-dogfood-turnup-findings.md) (Q336) |
| [dogfood-runner-rightsizing.md](dogfood-runner-rightsizing.md) | Measure peak CPU/mem per CI job class on GAG and right-size worker pod requests/limits + node pool; decide pod tiers (general + e2e) | ✅ Complete (2026-07-07) — disk-class ceiling resolved; general-worker (2Gi/3Gi) + e2e-worker (runner req 3vCPU/1Gi/3Gi, dind 3Gi/4Gi) pods right-sized from measured peak; "small" tier measured and declined. Kata end-state landed with Q286 (see [archive/kata-on-gke.md](archive/kata-on-gke.md) — Kata guest sizing is NOT a straight port of these measurements) |

## Cross-cutting

| Plan | Scope | Status |
|---|---|---|
| [runner-sizing-profiles.md](runner-sizing-profiles.md) | Worker right-sizing profiles: per-RunnerSet usage observability → recommendations in `RunnerSet.status` → opt-in auto-apply profiles (`Binpack`/`Throughput`/`NodeShare`); automates the manual dogfood right-sizing loop for every tenant, ARC has no equivalent | ✅ Complete (2026-07-28) — Phases 1–3 shipped 2026-07-21/22 and live-validated on dogfood over three sessions: recommendation + derivation + restart persistence, then all three `SizingDrift` states and `Binpack` actuating at Guaranteed QoS (`sampleCount` 36), then `Throughput` actuating Burstable off the same 36-sample history (2026-07-28, Q449). `NodeShare` is the one profile still envtest-only ([Q448](../queue/Q448.md)). Stays in place as the validation record; [Q416](../queue/Q416.md) still cites its Classic findings |
| [capacity-aware-intake.md](capacity-aware-intake.md) | Close the placeability rung of the admission ladder: refuse to claim jobs whose worker pod cannot be scheduled, without suppressing the cluster-autoscaler trigger. Three cheapest-first signals (scheduler verdict for fixed-size clusters, autoscaler declination for elastic ones, `ProvisioningRequest` probe deferred), all off by default | ✅ Complete (2026-07-31) — filed 2026-07-25. Item 0a shipped 2026-07-26 (Q443): the quota rung reached the scale-set tier as `AdvertiseCapacity`, and its dogfood measurement (Q462, 2026-07-31) found it binding with a bias-low margin of 0–2 jobs. Phase 1 shipped 2026-07-27 (Q405), phase 2 the same day (Q406), and Q470 then split the mode enum's two axes — tenant policy on the set, cluster fact on the gateway; their dogfood measurement (Q469, 2026-07-31) found the gate removes no wasted claims on the ScaleSet tier, fixed by the §9g latch (Q512, 2026-07-30), and the [§9h](capacity-aware-intake.md#9h-what-the-dogfood-re-run-measured-for-the-latch-q513) re-measurement (Q513, 2026-07-31) confirmed the latch bounds waste to ~1 probe claim per window with no sawtooth. The Karpenter drift arm landed 2026-07-31 (Q479). Phase 3 is deferred ([Q407](../queue/Q407.md)); rationale in [D.8](../design/appendix-d-alternatives-considered.md#d8-gating-intake-on-capacity-which-signals-are-safe-to-gate-on)/[G.16](../design/appendix-g-future-enhancements.md#g16-provisioningrequest-pre-acquire-capacity-probe-check-capacity) |
| [scaleset-eviction-recovery.md](scaleset-eviction-recovery.md) | Port eviction recovery to the scale-set tier (it existed only on classic, which is scheduled for removal at `v2.0.0`), then decide via probe whether the AGC can release an evicted job's lock early instead of waiting out GitHub's timeout | 🚧 In progress — filed 2026-07-25 from the [#811](https://github.com/actions-gateway/github-actions-gateway/issues/811) feasibility review. Phase 2 (the port) shipped 2026-07-26 (Q417); Phase 1's baseline benchmark (Q396) and Phase 3's fast release ([Q418](../queue/Q418.md)) remain |
| [q501-cancel-relay.md](q501-cancel-relay.md) | Relaying a run cancelled at GitHub to the worker pod, which the Q459 measurement found nothing does — split into the trigger (how the AGC learns) and the actuator (what it then does) | ⚠️ Partial — filed 2026-07-31. Reading the code for the fix found the actuator missing too: the classic tier cancelled a job's context on a definitively lost lock and never deleted the pod, so every Q254 teardown orphaned a worker until `maxWorkerLifetime` (12h). That half shipped as [Q501](../queue/Q501.md). Phase 0 answered 2026-08-01 off a capture already in the repo — Q468 recorded a live cancelled run queueing a `JobCompleted` in ~0.2 s, which on ScaleSet stamps the worker and hands it to the reaper's five-minute grace, so the unbounded defect is classic-only and candidate B is not worth building. One measurement remains: whether `renewjob` reports the loss, which `E2E_GitHub_CancelledRunLeavesNoDeletionMark` is now instrumented to read on its next live run |
| [docs.md](docs.md) | Documentation roadmap across phases | ⚠️ Open — all Phase 1/2/3 items shipped except the Grafana dashboards half of 3.2; the alert rules, SLO recording rules and runbook links landed as `observability-alerting.md`, leaving dashboards as [Q568](../queue/Q568.md) |
| [docs-six-layer-audit.md](docs-six-layer-audit.md) | Six-layer consistency audit of `docs/` (terminology, cross-refs, nav, reuse) | ✅ Done — all six layers resolved; Layer 3 metrics gap closed by Q51; the optional link-check CI gate is a separate non-blocking decision |
| [make.md](make.md) | Makefile UX (help target, e2e workflow, image var consistency) | ✅ Done — Phase 1 + Phase 2 complete; items 2.5/2.7b are cosmetic defers only |
| [go-to-market.md](go-to-market.md) | Adoption plan (OSS, non-commercial): ICP, three-lane landscape, two-tier messaging (what the site claims vs the thesis the strategy plays for), demand evidence vs ARC, channels, AI discoverability, donation posture | ⓘ Strategy — reconciled 2026-08-06 against [competitive-analysis-2026-08](competitive-analysis-2026-08.md); README problem-first rewrite still open |
| [ecosystem-integration-landscape.md](ecosystem-integration-landscape.md) | ~100 Kubernetes ecosystem integrations cataloged + mapped to GAG (conflict / integrate / interact); basis for ecosystem enhancements and "feels-native" conventions | ⓘ Research — items filed on the STATUS Queue/Deferred as Q205–Q218; Q218 (worker disruption-safety) is a v2beta1 gate |
| [v1-classic-sunset-review.md](v1-classic-sunset-review.md) | Two-axis (v1alpha1 API vs classic protocol) strategic review of the "sunset v1 faster" hypothesis: per-workload viability, scaling/perf, structural-ceiling verdict, and a sequenced sunset-timeline recommendation | ⓘ Review — revised 2026-07-05 (§6.2): v2/scale-set is the product, classic is terminal, and the earlier "hold both removals" hedge is dropped — removal is committed to `v2.0.0` gated on v2 *stability*, not adopters. Follow-ups filed as [Q272](../queue/Q272.md)/[Q273](../queue/Q273.md) — both now parked in Deferred, behind upstream scale-set GA and [v2-ga.md](v2-ga.md) Phase 3 respectively; sequencing lives in [v2-ga.md](v2-ga.md) |
| [v2-api.md](v2-api.md) | v2 API decomposition sequencing: split the monolithic `v1alpha1` `ActionsGateway`/`RunnerGroup` into the decomposed `v2alpha1` (`actions-gateway.com`) API across independently shippable milestones M1–M5 (large reusable pod templates, multiple gateways per namespace, optional/shared egress proxy) | ✅ Done: M1–M5 shipped, plus M4 cross-namespace sharing (Q166); deferred residual only ([Q174](../queue/Q174.md)) |
| [v2beta1.md](v2beta1.md) | v2alpha1→v2beta1 graduation plan: what must land before the first stability contract, in what order, with the credential shape correct before the freeze | ✅ Done — graduation shipped (Q74); SPIFFE/SPIRE signer deferred ([Q214](../queue/Q214.md)) |
| [v2-api-gap-analysis.md](v2-api-gap-analysis.md) | Full v1↔v2 gap analysis (API surface + controller/webhook behavior): what v1 does that v2 doesn't, with per-gap evidence; distinguishes genuine gaps from intentional design differences | ⓘ Review, 8 gaps found: the noProxyCIDRs admission gap was fixed same-day (#641), its GHES residual closed by the Q322 follow-up, and Q323/Q324/Q325/Q326/Q327/Q328/Q329 since fixed. Parity/intentional differences recorded so future audits don't re-litigate them. Every gap closed as of 2026-08-09, and with M4 (Q166) shipped and multi-label runner sets landed (Q726, 2026-08-11), v1→v2 functional parity is closed |
| [q264-scale-set-protocol.md](q264-scale-set-protocol.md) | The open residual of the scale-set migration: the terminal PR that removes the deprecated classic acquisition machinery + transitional API fields (the shipped P0–P5 narrative is archived — see [archive/q264-scale-set-protocol-phases.md](archive/q264-scale-set-protocol-phases.md)) | ❌ Open — the one-minor-deprecation-window framing is superseded: [Q264](../queue/Q264.md) is now event-gated on [v2-ga.md](v2-ga.md) Phase 3, stripping classic in the same `v2.0.0` bundle as `v1alpha1` and `v2alpha1` rather than on its own schedule |
| [q273-v2-front-door.md](q273-v2-front-door.md) | Route the front door (README, onboarding, positioning) to the v2 API, add deprecate-v1 banners, and make the `gag-migrate` v1→v2 story exemplary — the do-now slice of the sunset review §6.2 ([Q273](../queue/Q273.md)) | ⚠️ Front-door slice done — v2-first onboarding, the v1 deprecation banners, and the `gag-migrate` polish all shipped. The residual full v1 removal ([Q273](../queue/Q273.md)) is no longer gated on v2beta1 (Q74 shipped); it is parked in Deferred and lands in the `v2.0.0` bundle — [v2-ga.md](v2-ga.md) Phase 3 |
| [q224-fanout-dispatch-lever-spike.md](q224-fanout-dispatch-lever-spike.md) | ⓘ AGC-side lever spike: can two proposed AGC-side levers beat GitHub's server-side fan-out distinct-delivery starvation without the Option E scale-set rewrite? Tested against the mechanism + live evidence | ⓘ Spike — no code changed; verdict: no reliable AGC-side lever, Option E ([Q264](../queue/Q264.md)) is the only structural fix. Referenced by [Q268](../queue/Q268.md) |
| [structural-debt-audit-2026-07.md](structural-debt-audit-2026-07.md) | Codebase-wide structural debt audit (2026-07-20): god functions, oversized files, version-boundary duplication, test-double sprawl, script sprawl — with the evidence behind each Queue row, a scored linter-vs-audit prevention analysis, plus deliberate non-findings and unverified leads | ✅ Done — filed 2026-07-20; the F1 Secret leak shipped same-day (Q373, #727), F2's probe rewrite as Q362, F3's share-and-gate split as Q374, F4's CIDR-rule consolidation as Q364, F5's broker-double consolidation as Q368, F7's CreateOrPatch collapse as Q366 (ownerRef-policy question spun out to Q394), F8's god-function decomposition as Q367, F9's error-taxonomy unification as Q369, F10's script-sprawl cleanup as Q370, the §Prevention gate (nolintlint + a ratcheted funlen) as Q371, and F6's foundation move as Q365 (condition-collector residual spun out to Q403, since shipped as `metrics_v1.go`); re-run trigger deferred as [Q372](../queue/Q372.md) |
| [markdown-gates-parser.md](markdown-gates-parser.md) | Move the four `awk`-based Markdown gates (link/anchor check, backlog lint, roadmap coherence, metrics replay) onto a real parser in `devtools/`, keeping each `scripts/` entry point — regular expressions cannot count brackets, and every defect traces to that | ✅ Done, all three phases shipped 2026-08-02..04. Measured: `check-doc-links.sh` never checked a badge-wrapped link's outer target (3 live in README, one relative), `lint-backlog.sh` shifted every table field on one escaped pipe (Q625 carried a live instance), and its character cap meant bytes or runes depending on the `awk`; the roadmap gate charged a bullet with the paragraph after it, and the metrics replay had mis-booked a removal since June. Q612, Q613 and Q614 all done, with Q654's em-dash gate on the same layer. The merge driver is deliberately excluded |
| [q889-backlog-item-store.md](q889-backlog-item-store.md) | Replace the single `docs/STATUS.md` Queue table with the `session-backlog` skill's per-item store under `docs/queue/`, adopt its Python tooling, and port the lint rules the store has no equivalent for | 🚧 In progress, filed 2026-08-16, superseding the stale [#1534](https://github.com/actions-gateway/github-actions-gateway/pull/1534) attempt and its unlanded Go `queuestore`. Six phases: tooling, the store, the 53 machine consumers, 218 anchor references, the `/dev/queue/` render, then delete the table and groom. [Q889](../queue/Q889.md) |
| [website.md](website.md) | Public GitHub Pages site: MkDocs Material rendering of `docs/` + a custom landing page and "vs ARC" comparison; domain decision folded in (org move) | ✅ Done — scaffold, landing, comparison, and public launch shipped (was Q52/Q99/Q129, all completed) |
| [q645-abandoned-completion.md](q645-abandoned-completion.md) | Investigation H (Q645/Q676): what the run service does with a completion for an acquired-but-never-run assignment, which decides the Q628 release path's shape | ✅ Done 2026-08-04 — five live runs: `abandoned` and `canceled` conclude the run **`success`** in one second (false green, job orphaned `in_progress`), `failed` is refused 401, and told-nothing gets an honest run+job `cancelled` at ~15 min. Remedies shipped: the listener reports nothing for its own unrun delivery, and the provisioner force-cancels the run (measured 2026-08-05, run+job `cancelled` in ~1 s, Q683). Follow-up: [Q682](../queue/Q682.md) |
| [docs-page-boundaries.md](docs-page-boundaries.md) | Redraw the boundary between `index.md`, `why-gag.md`, `roadmap.md`, and `STATUS.md` on reader job rather than on time or audience tier — the roadmap was holding a third, unlinked copy of the feature list | ⚠️ Phase 1 done (2026-08-01) — `features.md` extracted as a link-disciplined capability index (9 of 16 old roadmap bullets had zero links; the longest was 126 words), the roadmap shrank to forward-looking only, and `check-roadmap.sh` rule 5 now gates the link discipline. Phase 2, splitting the parked register (Deferred + Flake watch) off `STATUS.md`, is [Q569](../queue/Q569.md) |
| [q710-overlong-paragraphs.md](q710-overlong-paragraphs.md) | Split the docset's multi-idea paragraphs without losing a qualifier, phased worst-first, plus the parse-layer measurement a blank-line heuristic gets wrong, and the token reconciliation that proves nothing was dropped | ⚠️ Phase 1 done (2026-08-06): three top-level prose paragraphs ≥140 words are left in design chapters `01`–`08`, deliberately, taking the corpus from 154 to 127. Phases 2–5 (long list items, `appendix-h`, `docs/operations/`, admonition blocks) are [Q710](../queue/Q710.md) |

## Archive

Plans whose work has fully landed and which `docs/STATUS.md` no longer references.
Moved here so `ls docs/plan/` shows active work only.
The doc remains available — the rationale is often more valuable than the diff.

| Plan | Scope | Closed |
|---|---|---|
| [archive/velocity-v2.md](archive/velocity-v2.md) | Second velocity pass: proxies that measure progress rather than volume, so net backlog instead of closures alone, fix/feat churn, PR cycle time, and code survival at a fixed horizon | 2026-08-15. The busiest stretch closed 15 backlog rows a day while filing 20, which the old panel hid. Two defects caught in the build: the survival series mixed commit-date and author-date clocks and was only visible because a rate exceeded 1 (the first version clamped it, hiding it), and PR cycle time cannot separate machine from model because the local gate runs before a PR opens |
| [archive/velocity-proxies.md](archive/velocity-proxies.md) | A `claude-usage/` chart for work shipped per day that survives both the 2026-08-09 docs reflow and the mid-project switch from direct commits to PR squash-merges, and the measurement that made tokens-per-word the headline ratio | 2026-08-15, Q824. The plan upgrade moved no velocity series and cannot be read as having bought nothing, since the 5-hour limit it relieved is invisible here; the 2x at the handover splits into commits landing across 1.4x more hours of the day and 1.5x more per hour, which is system spread rather than hours worked. Q824's own remedy was refuted on measurement (cumulative diff additions spike +28.2k at the reflow, worse than the step they replace) and words shipped instead |
| [archive/q850-reason-tier-ledger.md](archive/q850-reason-tier-ledger.md) | Extend the derived acquisition-tier ledger from metrics (Q776) to the other two signals an operator reads: condition reasons and Event reasons, with the scanner's resolution rules and why the reason argument's index is read off the callee's declaration | 2026-08-14, Q850. 71 reasons walked and given a tier (45 condition, 26 Event), `make reason-tiers-check` holding both tables to the source and additionally failing an Event reason with no runbook entry. The walk found two such Events, `AgentDeregistrationFailed` and `OrphanedWorkerRecovered`; it found no condition reason single-tier by accident. The scanner's own first version was wrong in the extraction class this cycle keeps hitting, reading an action string as a reason because two recorders here share a name with the reason at different indexes |
| [archive/q726-multi-label-runner-sets.md](archive/q726-multi-label-runner-sets.md) | Multi-label `runs-on` array targeting: why the scale-set model's one-label ceiling was an upstream fact that stopped being true, which `runnerLabel` becomes the scale set's name, and why label drift is detected rather than patched | 2026-08-11, Q726, the last `runs-on`-shape blocker on the [zero-edit ARC migration](arc-parity.md). Both single-label CEL rules dropped; every `runnerLabel` is registered on the scale set with the first naming it, which keeps a single-label set's create request byte-identical so nothing already at GitHub re-registers. Two measurements reshaped it. ARC added multi-label upstream in 0.14.0 (2026-03-19), so the repo's own "there is no free-form multi-label list per scale set" godoc had gone false; and a GHES appliance below 3.21 without `DistributedTask.AllowRunnerScaleSetCustomLabels` keeps only the name label and **discards the rest with no error**, which is why the change reports what the server returned rather than what it asked for (the advisory `RunnerLabelsIncomplete` condition). Whether the Actions Service honours a labels `PATCH` is unmeasured and deliberately not relied on, so drift is surfaced rather than reconciled |
| [archive/runner-template-library.md](archive/runner-template-library.md) | A curated library of golden `ClusterRunnerTemplate` instances (`plain`, `kata-dind`, `privileged-dind`): promote the two CI-validated dogfood templates into a shipped kustomize base the e2e overlays patch, add a plain baseline entry and a workload to template mapping README; no new CRD, only CI-exercised templates ship | 2026-08-08 — Q554, one of the three `1.4-gate` rows. Both dogfood overlays now consume the shipped bases and the inversion was reconciled rather than assumed: each overlay renders byte-identically to its pre-change form apart from the object rename, the runner image moving to `spec.workerImage`, and the `templateRef` following. Two findings reshaped the build. kustomize has no schema for a CRD, so a strategic-merge patch against one degrades to an RFC 7386 merge and replaces lists wholesale (measured on v5.8.1: a patch naming `initContainers[0].resources` left an init container with only that field, at exit 0), so every overlay patch is JSON 6902, and the gate rejects the other form. And neither the stock runner image nor GAG's own `worker` ships a Docker CLI, so both DinD entries ship a reserved `.invalid` `workerImage` that fails at pull rather than mid-job, which also makes the overlays' image patch mandatory. The admission rule stopped being a convention: `make template-library-check` reconciles shipped against exercised in both directions, and `TestTemplateLibrary_Admitted` applies every entry to a real apiserver each integration run, which is what covers `plain` |
| [archive/q459-drained-worker-recovery.md](archive/q459-drained-worker-recovery.md) | Whether the drained-worker recovery gap Q421 measured should be closed or accepted: does the SIGTERM relay's report leave the run re-runnable, and is a deliberate cancellation distinguishable from a disruption at the pod | 2026-07-29 — decision taken: close, gated on `deletionTimestamp`, and Q502 shipped it on both tiers. Both questions measured at live-GitHub on two runs each: the SIGTERM relay's report leaves the run re-runnable (concludes `failure` in 15–26s, `rerun-failed-jobs` returns 201), and a human-cancelled run's worker publishes the same phase and empty reason *without* a deletion mark, so the mark separates a disruption from both a cancel and a genuine failure. Q519 later caught the predicate comparing the raw mark, which read every real drain as a cleanup delete; ordering the deletion *request* against the termination record fixed it. The last owed item, the live-GitHub confirmation of the Q495 run-identity annotations on a real worker, closed 2026-08-03 (Q544). The measurement also found that a cancel never reaches the worker ([Q501](../queue/Q501.md)) |
| [archive/q583-restart-replay.md](archive/q583-restart-replay.md) | An AGC restart replays the scale-set queue from cursor 0 and provisions workers for jobs long concluded, because the listener acks by cursor only and its provisioned/completed/abandoned guards are process-scoped | 2026-08-03 — Q583, Q597. The row's "measure at the rc.4 gate first" instruction was discharged without the gate: the premise was already in the repo (a 2026-07-05 dogfood reconnect that provisioned 7 workers for a previous pass's jobs, plus Q468's ≥13 h retention), and [Investigation G](archive/q583-restart-replay.md#the-answer-replayed-delete-ok-pruned-2026-08-01) then confirmed it live — a cursor-acked `JobAssigned` replays to a new session, `DeleteMessage` answers 204, and deleting prunes the queue. That closes Q264's P4 `DeleteMessage` unknown too. Delete-acking shipped in the same change: the listener issues the delete half of its ack once every job in a message has concluded, so replay still recovers anything in flight. The residual — three process-scoped guard sets that never shrank — closed as Q597 on 2026-08-03, keyed on the same delete: a guard is retired once every held message carrying a `JobAssigned` for its job is gone, so the sets are bounded by the queue rather than by the process's lifetime. The argument, and the two looser rules that were tried and turned an existing Q575 test red, are in [02-architecture.md](../design/02-architecture.md) |
| [archive/q603-settle-delete-gap.md](archive/q603-settle-delete-gap.md) | The seam Q583's delete-ack left: a job concludes in the listener's memory at `settle` and its message is deleted separately, so a process stopping in between leaves the assignment in the queue for the next one to replay | 2026-08-02 — Q603; hard-kill residual closed 2026-08-05 as Q606. The graceful half was unconditional rather than a race (`run` had no exit flush at all): closed by flushing outstanding deletes as the loop unwinds, on a detached context, plus moving the per-cycle flush to sit directly after each of the two places a job concludes; three regression tests, each red with the exit flush removed. Q606 then took the residual no ordering could reach: the concluded-job guards are persisted to a per-RunnerSet ConfigMap written ahead of every delete, loaded before the session opens, retired by the confirmed delete and by a drained-queue sweep so the stored set stays bounded by the queue. The write-ahead ordering, the sweep's soundness argument, and the Q609 retained exemption are in [02-architecture.md](../design/02-architecture.md). Q689 (2026-08-05) then found a third window this doc did not see, on the other side of the read: the job concludes at GitHub and the process stops before a poll delivers the `JobCompleted`, so nothing is stranded and nothing is killed. The assignment is legitimately held and legitimately replays. No hard kill needed, which corrects Q606's scoping of the residual. Closed by draining the queue's outstanding conclusions on the way out, ahead of the exit flush, plus putting the scale-set tier into the Q222 shutdown barrier it had never been in |
| [archive/e2e-progress-visibility.md](archive/e2e-progress-visibility.md) | Make a running e2e suite legible from the Actions log — which specs are running, which finished, how many remain — on both the hosted and GKE dogfood self-hosted lanes | 2026-08-02 — Q608. Filed and closed the same day. Measured first: at `--procs 6` Ginkgo suppresses spec-start entirely, prints a passing spec as a bare `•`, and two real runs went 98 s and 78 s between any output — while the dogfood lane streams at the same second granularity as hosted, so one mechanism serves both. The shape was forced by a constraint found in the reporter source: Ginkgo intercepts spec stdout, so the suite cannot narrate its own progress and a watcher must run beside it, reading an event stream the suite appends to a file. Also closed a four-month-old false premise — `e2e-tests-speed.md` §13 planned the JUnit upload on the belief that Actions renders it via a "built-in test reporter", which does not exist, so the report had sat as an unrendered 300 KB artifact since |
| [archive/q547-gateway-teardown-worker-reap.md](archive/q547-gateway-teardown-worker-reap.md) | Reaping a tenant's worker pods when its v2 `ActionsGateway` is deleted: the AGC is their only reaper and is torn down with the gateway, while the `RunnerSet`s that own them deliberately survive it | 2026-08-01 — Q547. Filed and closed the same day, from the 2026-07-31 dogfood observation of a pinned billable node. The row's two suggested fixes were both already in place — the pods do carry owner references, and `maxWorkerLifetime` already bounds them at 12h — so the real gap was narrower and the real cause was a regression from the v2 API decomposition: v1 teardown deletes its `RunnerGroup`s and the pods cascade, while v2's standalone `RunnerSet`s deliberately outlive the gateway. The cheap fix (reap from the AGC's SIGTERM) was ruled out by measuring teardown's own ordering — the AGC's `RoleBinding` and `ServiceAccount` are deleted milliseconds after its `Deployment`, so a shutdown-time reap would 403. Shipped instead as a two-controller split: the AGC reaps on the gateway's `deletionTimestamp`, the GMC gates teardown on `activeJobs`/`pendingJobs` before deleting any child, bounded by a 90s deadline that stays under the 2m `kubectl delete --timeout` its callers give it. Neither binary gained RBAC. The dead-AGC residual is accepted, not deferred |
| [archive/q536-ghes-private-ca-trust.md](archive/q536-ghes-private-ca-trust.md) | Let an operator supply the private CA fronting a GHES appliance, so the AGC and its worker pods complete the TLS handshake — the last GHES gap Q506's audit named and declined to fix | 2026-08-01 — Q536. `spec.githubCABundleRef` names a ConfigMap the GMC mounts on the AGC and the AGC projects into every worker pod, additive to the system roots so no bundle can narrow what the AGC validates. Both halves of the row's asserted mechanism were measured first (see [What was measured](archive/q536-ghes-private-ca-trust.md#what-was-measured)): an existing test already proved the trust gap, read the other way, and a sweep of both API trees, the chart values, the GMC flags and both AGC builders found no configuration surface anywhere. Two design points came out of the build rather than the plan — the reference is polled through the uncached reader instead of watched, because the GMC's ConfigMap informer is name-pinned to its own namespace precisely so it needs no cluster-wide ConfigMap read; and Q520 landed on `main` mid-flight over the same function, so the merged `configureTrustPool` keeps its proxy-CA semantics and log strings verbatim. The appliance-side handshake is deliberately unverified, as Q506's own fix was |
| [archive/q575-absent-job-payload-secret.md](archive/q575-absent-job-payload-secret.md) | Why eight worker pods sat `Pending` for ~10 minutes on the `v1.3.0-rc.3` dogfood gate mounting a `job-ss-<jobID>` Secret that did not exist, then recovered unaided — the full trace of the Secret's lifecycle across the provisioner, the scale-set listener, and the worker-pod reaper | 2026-08-01 — Q575. The row offered two directions and the answer was neither exactly: the Secret is always created *before* the pod, and the only deleter that can reach a live pod is `CleanupScaleSetJob` on the terminal `JobCompleted`. What made it a ten-minute stall was a third thing the row did not name — the completion stamp `markJobCompleted` already wrote on the Pending pod was read by the reaper only in its `Running` arm, so the pod fell through to `pendingPodDeadline`, whose 10m default is exactly the observed duration, and was then mislabelled `pending_deadline`/`WorkerPodStuckPending`, blaming a scheduler that had done nothing wrong. Fixed at both ends: the listener handles a batch's completions before its assignments and refuses to provision a job already seen completed (killing the deterministic same-batch case, which a restart replay produces for every job in the queue — the likeliest source of eight at once), and the reaper honours the stamp in the `Pending` arm on a 30s grace under a new `completed_pending` reason. Not verified live; that rides the rc.4 dogfood gate |
| [archive/scripts-directory-regrouping.md](archive/scripts-directory-regrouping.md) | Group `scripts/`'s 100-file flat namespace into blast-radius subdirectories so every CI path filter is a prefix glob: kills `e2e-test.yml`'s `scripts/*` catch-all and the `!(dogfood)` extglob, and settles `e2e-test.yml` vs `e2e-calico.yml` disagreeing ~60× about the scripts behind the `e2e-reusable.yml` they share | 2026-08-01 — Q571. Filed and closed the same day, from Q561 where a MkDocs hook's unit test pulled two docs-only PRs into a ~13 min e2e run and one of them failed an unrelated migration spec. 99 files moved into ten groups, 762 references rewritten across 190 files, the interim `nondocs` filter deleted. The plan's seven-group table needed three more: `fetch/` for the download/retry family that five separate gates call (filing it under `e2e/` would have hidden it from the rest — the Q400 narrowing in a new shape), `manifest/` for a gate the table omitted, `dev/` for developer-only tooling that is not agent tooling. The stated narrowing risk landed backwards: only the e2e filter narrowed, every enumerated filter *widened* to a prefix glob, and `autoscaler-drift.yml` gained the `download-verified.sh` it had always run but never listed. A new `check-path-filters.sh` assertion pins the two e2e lanes to one script set |
| [archive/q291-e2e-calico-egress-github-flake.md](archive/q291-e2e-calico-egress-github-flake.md) | Three real-GitHub egress specs (2 proxy-connect + direct-egress) red the `e2e-calico` leg together when the Felix ipBlock-programming / GitHub-dial window outlasts the curl retry budget under CI load; widen the bounded retry budget without weakening assertions | 2026-08-01 — Q291. Closed by the soak the row was waiting on: 144 green `e2e-calico` runs on `main` since the mitigation merged (#593, 2026-07-11), against a ≥25-run infra bar, with no recurrence — the two pre-fix recurrences (07-04, 07-11) were the last. Retired from Flake watch to the [retired-flake ledger](../development/flake-watch-retired.md), which keeps the "a fix was already attempted here" memory greppable should it ever return |
| [archive/q436-session-recycle-strands-job.md](archive/q436-session-recycle-strands-job.md) | A single-use session recycle whose `DeleteSession` times out leaks the session rather than closing it, stranding the job queued on it — surfaces as the slow `E2E_AGC_MultipleJobsQueued` e2e failure | 2026-08-01 — Q436. Closed by the soak the row was waiting on: ≥136 green `unit-test` runs on `main` since the fix merged (#837, 2026-07-26), against the ≥50-run bar this spec earns as a correctness-guarding test. The investigation's durable finding stands — the strand was fakegithub-only, the real broker redelivers pool-wide — and the fix ages undelivered jobs into the owner pool while counting leaked sessions. Retired to the [retired-flake ledger](../development/flake-watch-retired.md) |
| [archive/release-decision-support.md](archive/release-decision-support.md) | Make release timing answerable from recorded data rather than from narrative: a delta report off the last stable tag, standing "when to cut" triggers, and a scope-ledger convention for release plan docs — planned vs delivered at a glance, punt = delabel, delay = keep the gate | 2026-07-31 — Q556. [`release-delta.sh`](../../scripts/release/release-delta.sh) derives the whole report from disciplines already enforced (Conventional Commits, the delete-on-done Queue's history), so there is no recording step. Two plan decisions changed under implementation: completed Q-IDs come from diffing STATUS.md's Queue section across revisions, not from `docs(status)` subject verbs (squash-merge puts the row deletion in the *work* commit, and set-diffing also tells a parked row from a delivered one), and a `!` commit does not auto-promote the bump to major, since most breaking markers here land on a pre-GA CRD version. [release-1.3.md](release-1.3.md#scope-ledger) carries the first ledger |
| [archive/backlog-dev-site-page.md](archive/backlog-dev-site-page.md) | Publish the backlog on the docs site's `dev` version only (releases stay operator-docs-only, no stale snapshots) with label/status/size filter chips via the existing progressive-enhancement pattern; markdown stays the single source of truth, one `!ENV` conditional gates the repo-internal `exclude_docs` set | 2026-07-31 — Q558. `MKDOCS_EXCLUDE_DOCS` holds the exclusion list and the `!ENV` default is the full repo-internal set, so the two never duplicate; the trap is that the default applies only when the variable is *absent*, so `pages.yml` `export`s it inside a conditional rather than letting a step-level expression fall through to `''` and publish the internal docs on every release. Chips key off table *shape* (a `Labels` column plus an `ID`/`Item` first column), which picks up Flake watch and Progress for free while keeping chips off the identically-headed metric tables. The plan did not anticipate that publishing the tree would surface **724 relative source links** as dead — 34 on `STATUS.md` — so it also adds `hooks/source_links.py`, absolutizing any target escaping `docs/` against `repo_url`; `design/`/`operations/` had been shipping those links dead all along. Durable reference: [website.md § Publication scope](../development/website.md#publication-scope) |
| [archive/q550-runner-registration-leak.md](archive/q550-runner-registration-leak.md) | Every scale-set `generatejitconfig` pre-registers a runner record and nothing in the provision→reap lifecycle removed it; because the name derives from the job ID, a job's leftovers collide with its own retries — the leak that wedged the `v1.3.0-rc.2` validation window with 22 stale records | 2026-07-31 — Q550. Fixed by making the worker pod the registry, as Q417 did for run identity: the provisioner stamps `actions-gateway.com/runner-name`, the reaper deregisters that record before deleting the pod, and a listener start sweeps records under its prefix that are offline, not busy, and claimed by no live pod (the claim check is load-bearing — a Pending worker's record is legitimately offline). The reconciler's Provision closure had been dropping the minted name outright, so nothing downstream could deregister anything. Phase 0 also found the stub never recorded the name it minted, making the leak invisible to the whole suite; teaching it to register on mint separated Q334's working by-name reclaim from the unresolvable-record case that actually accumulates. Live re-verification rides the RC dogfood gate |
| [archive/q506-ghes-endpoint-audit.md](archive/q506-ghes-endpoint-audit.md) | Full inventory of `github.com` hardcoded where a GitHub Enterprise Server endpoint belongs, classified per site with blast radius, and the decision it forced: fix the class or retract the GHES support claim | 2026-07-31 — Q506, decided **Option A, fix the class**. The largest defect was not the one the Queue row named: the GMC derived `GITHUB_API_BASE_URL` nowhere and no supported surface set it, so every GHES tenant failed at token exchange before acquiring a job. Now derived from `gitHubURL` by one shared resolver that also replaced the registrar's substring detector and the scale-set client's copy; the EgressProxy carries every referrer's GHES host into the FQDN policy and CONNECT allowlist behind new referrer watch edges. #3 had no code answer, as predicted — the CIDR default cannot know an appliance's ranges — so the advisory `GitHubEgressIncomplete` condition names the operator obligation. The row's noproxy claim was retired: Q322 had already closed it |
| [archive/q415-migrate-dogfood-validation.md](archive/q415-migrate-dogfood-validation.md) | Close the last unverified row in the v2 GA Definition of Done: migrate a real `v1alpha1` DinD tenant to v2 with `gag-migrate` on the live dogfood cluster and have the migrated tenant run a real GitHub Actions DinD job — the tenant assets, the smoke workflow, the runbook, and the acceptance criteria | 2026-07-31 — Q415; validated end to end on the live GKE dogfood cluster 2026-07-28 (baseline job green, `gag-migrate --apply`, migrated control plane Ready, coexistence, the Q231 recreate, a real DinD job green on the scale-set path), flipping the v2 GA DoD row to ✅. Five defects found — Q463, Q465, Q466, Q467, and Q535 on the [2026-07-31 re-validation](archive/q415-migrate-dogfood-validation.md#re-validation-on-a-fixed-control-plane-2026-07-31-q472) that confirmed the first four with both workarounds removed (Q472) — all since fixed. Q535's fix makes each AGC serve exactly one API ([05-security.md](../design/05-security.md)) |
| [archive/q528-scaleset-acquisition-e2e.md](archive/q528-scaleset-acquisition-e2e.md) | The scale-set tier's acquisition half — session, assignment, JIT provisioning — had never run on a cluster, because the e2e fakegithub spoke only the classic broker protocol and no scale-set session could open against it | 2026-07-31 — Q528; the protocol model moved out of `scalesettest` into a transport-free `scaleset/scalesetstub` (httptest cannot be linked into fakegithub — `cmd/probe/compat`), fakegithub mounts it beside the classic protocol, and the AGC's bootstrap re-points at it under the existing `STUB_AUTH_URL`/`STUB_BROKER_URL` pair, since `spec.gitHubURL` is pinned to https and cannot name the plaintext stub. `E2E_AGC_ScaleSetAcquisition` gates both acquire flows on kind; the spec attaches fakegithub's ordered call log because the whole chain completes in ~34 ms, at which resolution a vacuous pass and a real one look identical |
| [archive/q507-released-tag-upgrade-gate.md](archive/q507-released-tag-upgrade-gate.md) | Why no CI gate answered "does the chart an operator is actually running upgrade to HEAD?", the Q492 defect that gap let reach a green review-ready PR, and the design question of how CI resolves "the last released chart" | 2026-07-30 — Q507; `scripts/e2e/chart-released-upgrade-check.sh` pulls the published OCI chart at the highest stable `v*` tag on origin, installs it, and upgrades to HEAD along the documented path, asserting upgrade-blocking failures are the render-time documented kind (never Helm's bare `ensure CRDs are installed first`), every chart-root `crds/` CRD lands, and admission works afterwards. Runs last among the chart checks in `e2e-reusable.yml`, kindnet leg only. Contract: [testing.md § The released-chart upgrade gate](../development/testing.md#the-released-chart-upgrade-gate-q507) |
| [archive/q766-scaleset-abandoned-recovery.md](archive/q766-scaleset-abandoned-recovery.md) | Port the abandoned-run force-cancel (Q683) and its capacity-gated automatic re-run (Q691) to the scale-set tier, which is `v2beta1`'s only option and the default everywhere else | 2026-08-09, Q766. The row's shape was a trap the plan names: `disruptionAwaitingRecovery` scans *surviving* pods and every arm it matches feeds `rerun-failed-jobs`, so a fourth arm could neither see the dominant case (the `pending_deadline` reap removes the pod) nor act on it correctly (a job that never ran has no failed job to re-run). Detection moved to the reaper's own delete, where the pod is still in hand: the tier's analogue of the classic informer delete event, and a sounder one, since it is synchronous with the delete decision. The scan gained a separate narrower arm for an *external* delete of a still-`Pending` worker, the transient `Failed`-with-mark-and-no-exit-record shape; the two cannot both fire, because the reaper stamps `deletion-reason` and that arm requires its absence. Two reaps stay excluded and one residual is inherent, all three argued in [04-operational-flows.md § On the scale-set tier (Q766)](../design/04-operational-flows.md#on-the-scale-set-tier-q766) |
| [archive/q844-owed-rerun-tombstone.md](archive/q844-owed-rerun-tombstone.md) | Close the scale-set tier's last restart-safety residual: a preempted or drained worker's recovery evidence is the pod, and the deletion removes it, so an AGC down for that window issues no re-run | 2026-08-13, Q844. The mechanism was measured against the code rather than assumed: the recovery scan's only input is a `List` of pods that still exist. A finalizer was rejected as the fix, being exactly what makes `kubectl drain` hang, and drain is one of the causes recovered. The record went in the per-`RunnerSet` guard ConfigMap the listener already writes ahead of every message delete, added at provision and cleared at conclusion, with the reconciler reading it once per process, ahead of both the reaper and the listener's first poll. Reported as `cause="vanished"` rather than as a preemption, because which disruption took the worker went with the pod. Two narrower residuals stay, argued in [04-operational-flows.md](../design/04-operational-flows.md#why-preemption-deletes-rather-than-evicts-and-what-that-costs-us) |
| [archive/q497-preemption-recovery.md](archive/q497-preemption-recovery.md) | Recover a worker displaced by a higher `priorityTiers` tier: key the existing automatic re-run on the `DisruptionTarget`/`PreemptionByScheduler` condition kube-scheduler stamps on its victim, on both acquisition tiers, without touching the drain slice (owned by Q502, since shipped) | 2026-07-29 — Q497; both tiers recover, sharing the existing per-`run_id` retry budget across causes, with a `cause` label added to three eviction counters so a preemption recovery is not read as node pressure. Detection matches the full condition type/status/**reason** triple: the eviction API stamps the same condition type, so a type-only match would have silently recovered the drain path and pre-empted Q459's open decision. Pinned by unit + envtest (`TestAGC_Preemption_ScaleSetWorker_IsRecovered`, whose drain twin differs only in that reason and must still NOT recover); the fake-GitHub spec that made the [original measurement](eviction-oversubscription-validation.md#result-measured-2026-07-29-preemption-is-not-eviction) flipped from "no rerun" to "exactly one rerun" |
| [archive/q444-vap-param-resolution.md](archive/q444-vap-param-resolution.md) | Why every `runnergroups`/`runnersets`/`runnertemplates` write could be denied cluster-wide with `no params found for policy binding` while the param object plainly existed — the trigger, the kube-apiserver code path, the two failure modes, and the deterministic reproducer | 2026-07-28 — Q444 established the mechanism (emptying a `paramKind`'s binding set permanently kills the *shared* param informer a CORE type uses, and the store freezes rather than empties, so it can fail closed OR silently enforce a stale allowlist); Q492 shipped the fix by moving the guard's `paramKind` to the cluster-scoped `PriorityClassAllowlist` CRD, which arm 3 of the reproducer measures surviving the identical transition. The upstream apiserver bug ([k8s#130887](https://github.com/kubernetes/kubernetes/issues/130887)) is still open and no longer blocks us. Durable rule: [kubernetes-conventions.md § paramKind](../development/kubernetes-conventions.md#validatingadmissionpolicy-paramkind-never-a-core-type-q444q492) |
| [archive/local-gate-throughput.md](archive/local-gate-throughput.md) | Cost of one `make check` run and queue depth across concurrent worktree sessions — the measurement record for the local gate: which phase actually costs, why warm caches make every tier unable to discriminate a parallelism knob, and what the desktop pays for each candidate throttle | 2026-07-28 — Q377; six changes shipped: cross-worktree test-cache sharing (`-trimpath`), the N-slot heavy-build semaphore, the parallel cheap gates, the throttle-prefix switch off the QoS clamp (with `jobs`/`slots` re-derived against the new ceiling), moving the gate off a dispatch worker's critical path, and the coverage tier adopting `make test`'s single-invocation shape (1.66× cold, 1.74× warm). Durable how-to: [testing.md § Coverage measurement and the ratchet](../development/testing.md#coverage-measurement-and-the-ratchet) and [§ Resource auto-throttle](../development/testing.md#resource-auto-throttle-on-gui-dev-machines). Residual: none — the worktree-hygiene follow-up (Q491) was retired 2026-08-01 as a workstation concern, not repo work |
| [archive/q435-restart-orphan-reclaim.md](archive/q435-restart-orphan-reclaim.md) | Turns the reaper's restart-safety claim into a measurement: which of the four worker-orphan classes a freshly started AGC actually reclaims, and whether a replayed `JobCompleted` can stamp a worker orphaned while the AGC was down | 2026-07-26 — Q435; 3 of 4 classes reclaimed unconditionally, the unstamped `Running` orphan only via a redelivered completion (proven across a real two-manager restart). Durable-deadline residual closed by [q438-worker-lifetime-deadline.md](archive/q438-worker-lifetime-deadline.md); whether GitHub retains that completion across a session gap became its own experiment, [q468-jobcompleted-retention.md](archive/q468-jobcompleted-retention.md) — answered 2026-07-29: it does, at a 13 h gap |
| [archive/q438-worker-lifetime-deadline.md](archive/q438-worker-lifetime-deadline.md) | Closes the Q435 residual: a worker orphaned while the AGC was down has no deadline and is indistinguishable in cluster state from one running a long job, so the cap has to be stamped at provision time. Establishes by measurement that the job's own `timeout-minutes` is not on the acquisition wire, which rules out deriving the deadline and forces the choice of a fixed default | 2026-07-27 — Q438; `maxWorkerLifetime`, default 12h (2× GitHub's own 360-minute default job timeout), stamped as the worker pod's `activeDeadlineSeconds` so the **kubelet** enforces it with no live AGC — the mechanism deliberately does not depend on the AGC, since in the motivating incident the AGC was down for all 16 hours. Overridable per RunnerGroup/RunnerSet, `0s` disables, an explicit `podTemplate` value wins. Live-retention follow-up [q468-jobcompleted-retention.md](archive/q468-jobcompleted-retention.md) closed 2026-07-29: GitHub still redelivers at a 13 h gap, so redelivery and this cap overlap rather than leaving a window between them |
| [archive/q468-jobcompleted-retention.md](archive/q468-jobcompleted-retention.md) | Turns the one unknown Q435 left open into an experiment: does GitHub redeliver an unacknowledged `JobCompleted` to a session created after a multi-hour gap during which the scale set had no session at all? Designs the three-phase probe (arm / check / cleanup) the measurement needs, and states what would make a result invalid | 2026-07-29 — Q468; **RETAINED at 13 h 3 m** on the live wire (`cmd/probe` Investigation F), after a first rung at 29 s the day before. Past Q438's 12 h `maxWorkerLifetime`, beyond which the kubelet has already killed the worker — so redelivery covers the whole window in which it could still reclaim anything, and the architecture/runbook redelivery sentences were upgraded from hedged to measured. A lower bound observed once, not a contract: `LOST` was never reached, so no timeout may be derived from it |
| [archive/q414-dind-tenant-fixture.md](archive/q414-dind-tenant-fixture.md) | One reusable v1 tenant fixture carrying a Docker-in-Docker worker shape, e2e coverage of the `gag-migrate` v1→v2 fan-out, and the defect the fixture found: a privileged tenant could not be migrated at all | 2026-07-26 — Q414; measured that `FanOut` emitted a namespaced `RunnerTemplate` the v2 webhook refuses, so `--apply` failed after creating the `EgressProxy` and left the namespace half-migrated. Privileged shapes now fan out to a namespace-qualified, provenance-labelled `ClusterRunnerTemplate` ([§H.11](../design/appendix-h-v2-api-decomposition.md#h11-migration-v2-tool-assisted)); four near-duplicate `ApplyActionsGatewayCR*` builders collapsed into one typed fixture. Unblocks [Q415](archive/q415-migrate-dogfood-validation.md) |
| [archive/q409-arc-to-v2-front-door.md](archive/q409-arc-to-v2-front-door.md) | The decision of record for which v2 version tenant-facing docs name (`v2beta1` for new tenants, `v2alpha1` only as the `gag-migrate` on-ramp), the partition of surfaces that deliberately keep naming `v2alpha1`, and why the ARC migration guide had to move off `v1alpha1` in the same change | 2026-07-25 — Q409; onboarding docs and examples aligned on `v2beta1` (dropping `maxListeners` and multi-label `runnerLabels`, which `v2beta1` rejects), and [migration-from-arc.md](../operations/migration-from-arc.md) rewritten onto v2 — it had been routing ARC *scale-set* users onto v1alpha1, which is Classic-only, i.e. off the single-acquirer protocol they already had |
| [archive/q365-shared-foundation-out-of-v1-files.md](archive/q365-shared-foundation-out-of-v1-files.md) | Audit finding F6: relocate the cross-version GMC foundation out of the v1-named `builder.go`/`actionsgateway_controller.go`/`cert.go`/`metrics_cert.go`, and the AGC's version-neutral `Metrics`/`AdmitFunc`/sink contracts out of the classic-only `listener` package — the measured version-neutral vs v1-specific classification, and why `JobHandlerFunc` did not move | 2026-07-25 — Q365; eight `shared_*.go` files plus `ipranges_v1.go` in the GMC package, a new `cmd/agc/internal/runnercore` leaf package in the AGC. Proven behaviour-free by an AST diff of every top-level declaration before and after. Turns the v1 removal ([Q273](../queue/Q273.md)) and the classic removal ([Q264](../queue/Q264.md)) into file deletions |
| [archive/versioned-docs.md](archive/versioned-docs.md) | Make the site a `mike`-managed versioned tree: default to the latest stable release, unreleased `main` behind an opt-in `dev` version — decision log (why mike, why Actions-served not branch-served, why `--alias-type=copy`, local validation, rollout seeding) | 2026-07-24 — Q238; `pages.yml` rewritten to deploy per-version via mike (stable=default, dev=opt-in), `extra.version.provider: mike` selector, root CNAME/robots re-asserted in the deploy job. Supersedes the Q387 tag-trigger fix. Durable how-to: [website.md § Versioned deploy](../development/website.md#versioned-deploy-mike) |
| [archive/q222-agc-sigterm-teardown-race.md](archive/q222-agc-sigterm-teardown-race.md) | `TestAGC_SIGTERM_DeletesAllSessions` left sessions un-DELETEd after manager-context cancel; two ceiling bumps (10→30→60s) failed to hold, so the third was ruled out in favour of a real teardown investigation | 2026-07-21 — Q222; two defects found. (1) Nothing waited for listener goroutines at shutdown — `mgr.Start` returned and the process exited mid-DELETE, leaking a GitHub-side session per in-flight listener on every rollout; fixed with a `listenerShutdown` manager Runnable that drains multiplexers concurrently. (2) The recycle/heal ownership handoff dropped the DELETE outright on cancellation (unit-reproduced red→green). A transient exit-DELETE failure was also a permanent leak; the delete is now detached and retried. Test ceiling back to 10s and no longer load-bearing; the original CI failure was not reproduced locally — see the doc's "What was not established" |
| [archive/claude-md-hook-rule-retirement.md](archive/claude-md-hook-rule-retirement.md) | Map of `CLAUDE.md` hook-workaround rules → the upstream guard-plugin fix that retires each (filed 2026-07-18), with verify-before-delete ground rules | 2026-07-20 — Q348; all five remaining rows retired in one pass once workspace-guard 1.7.x, branch-guard 1.4.0, prod-guard 2.x, and foreground-guard 0.2.0 were installed; each previously-prompting command re-exercised clean before its rule was deleted |
| [archive/unit-tests-speed.md](archive/unit-tests-speed.md) | Unit-test latency: four targeted cuts (fake-clock steps, injectable restart delay, shared RSA test keys, `t.Parallel()`) + the 2026-07-20 CI re-baseline that made `go test` a single multi-module invocation | 2026-07-20 — Q17; items 1–4 shipped in `c4660ea` (2026-05-23); the revival PR collapsed `scripts/go/go-test.sh`'s per-module-serial loop into one workspace-wide `go test` (CI `-race` gate 189s → 163s measured). Remaining CI-latency levers (lint critical path, coverage loop) parked as [Q361](../queue/Q361.md) |
| [archive/q264-scale-set-protocol-phases.md](archive/q264-scale-set-protocol-phases.md) | The shipped P0–P5 phase narrative of the scale-set (Option E) migration: protocol reverse-engineering (§2), the Investigation E/E2 live wire probes (§2a/§2b), the U6–U8 decisions of record (§5a), and the P1–P5 execution record — split out from the live doc so the ~71KB history isn't read whole; the open classic-removal residual stays in [q264-scale-set-protocol.md](q264-scale-set-protocol.md) | 2026-07-18 — Q337 (split); phases shipped through P5 (ScaleSet the default since v1.1.0, Classic deprecated). Structural fix for the Q224 fan-out distinct-delivery starvation classic cannot solve; live-validated clean-green 2026-07-05 |
| [archive/kata-on-gke.md](archive/kata-on-gke.md) | Kata Containers on GKE: spike (Q226 ✅ GO) + reference architecture for unprivileged kind-in-runner CI, then the Q286 rollout — GAG's own e2e through an unprivileged Kata runner and the secure-by-default variant flip | 2026-07-17 — Q286; AC#5 green (e2e-calico 56/0/2 at dind parity) after root-causing 7 live defects (workload-pool, helm label typing, kata-deploy tolerations, autoscale-from-zero labeling, busybox-blkid mkfs skip + dockerd startupProbe, guest resource split, nested IPC_LOCK); `E2E_VARIANT` default flipped to `kata`; untrusted-PR posture parked as appendix G.14 |
| [archive/q311-scaleset-monitoring-surface.md](archive/q311-scaleset-monitoring-surface.md) | Give the scale-set acquisition tier (the default protocol since Q264 P5) the four monitoring surfaces the classic tier has — a wedged scale-set-only deploy emitted no throughput alert because `active_sessions` is classic-only | 2026-07-17 — Q311; alerts `ActionsGatewayScaleSetProvisioningStalled`/`…ProvisionErrors` + SLO rule `scaleset_provision_success_rate:rate5m` in `prometheusrule.yaml` (mirrored in observability.md, runbook entries), a "Scale-set Acquisition Tier" tenant-dashboard row + platform cross-tenant panel (both `$namespace` vars unioned so a scale-set-only deploy populates), and synthetic `scaleset_*` preview series |
| [archive/release-friction-reduction.md](archive/release-friction-reduction.md) | Remove repeated manual friction from the release + pre-GA dogfood gate: make `publish.yml` own the Release (tag-derived prerelease flag + auto notes/digests), a one-command dogfood validate wrapper, the `setup.sh` `workerImage`-preserve footgun fix, and Calico path-filter narrowing | 2026-07-12 — Q293 (`publish.yml` owns the Release), Q294 (`scripts/dogfood/validate-release.sh` one-command gate), Q295 (`setup.sh` `workerImage`-preserve), Q296 (release.md gate corrections + narrowed Calico path filter) all shipped. Bucket 1 exercised on the next real `v*` tag; the wrapper's end-to-end dogfood run is a human pre-GA-gate step |
| [archive/q242-g1-proxy-destination-allowlist.md](archive/q242-g1-proxy-destination-allowlist.md) | G.1: admin-set destination allowlist (FQDN host suffixes + CIDRs) on the per-tenant egress proxy so CI jobs reach build dependencies (e.g. `proxy.golang.org`, internal/cloud-private IP ranges) without forfeiting per-tenant egress attribution | 2026-07-12 — Q242 G.1; CRD `destinationFQDNs`/`destinationCIDRs` (#460), proxy CONNECT allowlist + denied-counter (#461), GMC env/policy injection (#462), platform allowlist + validating webhook (#463), design/operator docs. Dogfood turn-on moot — Q224 closed via the ScaleSet default (Q264 P4); allowlist is a no-op on the direct-egress dogfood (mirror-first Athens, Q244). Per-tenant egress-IP attribution validation carried by [Q243](q243-egress-ip-reference-arch.md) |
| [archive/q284-podscheduling-surface.md](archive/q284-podscheduling-surface.md) | Add `topologySpreadConstraints` + `priorityClassName` to the `PodScheduling` block on `EgressProxy`/`ActionsGateway`; records the governance decision that `priorityClassName` needs a **separate infra allowlist** (reusing the worker one lets a tenant lift its workers to infra priority and preempt other tenants' proxies), that `topologySpreadConstraints` composes with rather than replaces the built-in anti-affinity, and that v2 `ActionsGateway` had no validating webhook | 2026-07-12 — Q284; both fields + infra `--allowed-infra-priority-classes` allowlist with boot-time disjointness check, extended `EgressProxy` webhook + new v2 `ActionsGateway` webhook, unit+envtest coverage, operator/design docs; §2 promoted into `docs/design/05-security.md` §5.2 |
| [archive/gke-dogfood-turnup-findings.md](archive/gke-dogfood-turnup-findings.md) | Turn-up + validation history for the [GKE dogfood runbook](gke-dogfood.md): the `v1.1.0-rc.6` turn-up, the eight classic-protocol fan-out re-routes that isolated the Q224 distinct-delivery starvation, the Q264 P4 ScaleSet clean-green close-out (7/7), and the Q246/Q247/Q254/Q259/Q260/Q265/Q266/Q267 root-cause write-ups | 2026-07-07 — Q336; chronological findings split out of the runbook (was ~112KB, now ~42KB) so the operational steps stay lean. All findings resolved; nothing open |
| [archive/q223-worker-scaleup-rate-limit.md](archive/q223-worker-scaleup-rate-limit.md) | Opt-in, default-off per-RunnerGroup token bucket (`spec.scaleUp.maxPerSecond`/`burst`) capping worker-pod *creation rate* (not count) to smooth cold-start stampedes on shared egress (NAT/firewall/VPN); gates pod creation in the provisioner, composes with the quota-retry wait; `actions_gateway_worker_scaleup_throttled_total` metric | 2026-07-06 — Q223 (G.11); v1 RunnerGroup + v2 RunnerSet field, `golang.org/x/time/rate` limiter, unit + provisioner-behaviour + envtest coverage, operator docs |
| [archive/worker-sidecar-reap-warning.md](archive/worker-sidecar-reap-warning.md) | Non-blocking warning + `PossibleReapBlockingSidecar` condition + `actions_gateway_reap_blocking_sidecar_templates` metric when a worker template has a regular (non-native) sidecar that can block pod reaping; name-list opt-out; steer to native sidecars (no reaper) | 2026-07-03 — Q249; detection helper in `api/v2alpha1`, GMC admission warning, AGC RunnerSet condition+gauge; unit + webhook + envtest coverage; operator docs |
| [archive/q237-docs-quality-audit.md](archive/q237-docs-quality-audit.md) | Six-goal quality audit of the published docset: 57 ranked findings (36 goal-1 docs-vs-code drift, 17 high) with remediation batches | 2026-07-01 — Q237 audit; remediation batches Q250 (A, goal-1 high), Q251 (B, goal-1 medium), Q252 (C, goal-5/6 usability & tone) all shipped; appendix-e v1/v2 straddle split to Q253 (resolved) |
| [archive/q246-release-asset-timeout-live-diagnosis.md](archive/q246-release-asset-timeout-live-diagnosis.md) | Live cold-run diagnosis of the dogfood release-asset download timeout: (a) Q61 cache race vs (b) Q247 CPU starve | 2026-07-01 — Q246: confirmed (a) the Q61 cold-start cache race (per-CR reconcile blanks the direct-egress allowlist from an empty cache; live-measured ~25s window on `gag-dogfood`). Fix: preserve an existing NP's egress while the cache warms. (b) CPU is only an amplifier. Findings folded into [gke-dogfood-turnup-findings.md](archive/gke-dogfood-turnup-findings.md) |
| [archive/q235-worker-wrapper-injection.md](archive/q235-worker-wrapper-injection.md) | Inject the `cmd/worker` wrapper into worker pods at runtime so the default install and any `actions/runner`-derived (ARC) image run jobs without a baked-in wrapper image | 2026-06-28 — Q235: OCI image volume (K8s ≥1.33) / initContainer fallback, GMC forwards `WRAPPER_IMAGE`; default-on; e2e-validated on kindnet + Calico (#437). Live GKE re-validate folds into Q224 |
| [archive/q187-air-gapped-install.md](archive/q187-air-gapped-install.md) | Air-gapped / private-registry install: chart image-pull-secret support + per-image registry overrides (digests preserved) + air-gapped install guide | 2026-06-26 — Q187: `imagePullSecrets` on the GMC pod; runtime AGC/proxy/worker covered by the SA-attach pattern; `docs/operations/air-gapped-install.md` |
| [archive/q205-label-metric-naming-audit.md](archive/q205-label-metric-naming-audit.md) | `app.kubernetes.io/*` recommended labels on all created objects + metric/span semconv alignment before the v2beta1 freeze | 2026-06-26 — Q205: shared `api/apilabels` helper, `renewjob_errors_total`→`renew_job_errors_total`, span attrs → `k8s.*`/`gateway.*`; envtest-asserted |
| [archive/milestone-2-tests.md](archive/milestone-2-tests.md) | M2 unit + envtest gaps (11 items) | 2026-05-29 — banner: "All 9 gaps shipped" |
| [archive/milestone-4-tests.md](archive/milestone-4-tests.md) | M4 builder + IPRange + webhook test gaps (8 items) | 2026-05-30 — `TestBuildNoProxy`, `TestBuildNetworkPolicy`, `TestHTTPFetcher*`, `TestBuildProxyServiceAddr`, `TestServer_ListenAndServe`, `TestIPRangeReconciler_Start` all present; `ValidateDelete` covered inline in webhook test |
| [archive/integration-tests-speed.md](archive/integration-tests-speed.md) | Five integration polling/sleep cuts | 2026-05-30 — superseded; GMC integration tests now use Gomega defaults (~10ms polling), faster than the 25ms target |
| [archive/rename-agc-to-controller.md](archive/rename-agc-to-controller.md) | Rename on-cluster `actions-gateway-agc` → `actions-gateway-controller` to match docs | 2026-05-30 — zero `"actions-gateway-agc"` literals remain in `cmd/`; M3 live-GitHub kind run validated the rename live |
| [archive/gaps.md](archive/gaps.md) | Three code-level fixes from the doc audit (CRD eviction fields, per-key `proxy.resources` merge, credential-rotation observability) | 2026-06-01 — all three fixes shipped |
| [archive/go-best-practices.md](archive/go-best-practices.md) | Go-idiom cleanups: module-version unification, async-channel fix, goleak coverage | Q38–Q41 all shipped |
| [archive/milestone-3-tests.md](archive/milestone-3-tests.md) | M3 metric/decryption/eviction test gaps | 2026-05-30 — H1–H5 + M1–M4 merged (`17a7f5c`); L items done/obsolete (Q9) |
| [archive/acquire-admission-control.md](archive/acquire-admission-control.md) | Gate worker-pod capacity before `acquirejob`; in-cluster queue rejected | Q59 — implemented |
| [archive/competitive-analysis.md](archive/competitive-analysis.md) | GAG vs ARC per-benefit working notes; fed the comparison content | Q60 — superseded. The "verified" this row used to assert never happened: the Q60-closing commit added Kueue/Exostellar prose and no ARC per-claim check, and 11 unverified cells shipped. Actually verified 2026-08-06 in [competitive-analysis-2026-08](competitive-analysis-2026-08.md) |
| [archive/platform-owned-quota.md](archive/platform-owned-quota.md) | Remove tenant `spec.namespaceQuota`; platform owns Namespace + `ResourceQuota` + `LimitRange` | 2026-06-14 — Q130, breaking CRD change pre-1.0 |
| [archive/logging-audit.md](archive/logging-audit.md) | Cross-module log-call-site audit: format fragmentation, credential-leak surface, hot-path spam, correlation, per-tenant log level | Q86–Q89 — all themes shipped (Theme A was the 1.0-gating JSON unification) |
| [archive/k8s-best-practices.md](archive/k8s-best-practices.md) | Project-wide Kubernetes best-practices audit (RBAC, pod security, controller correctness, CRD polish, manifests, observability, supply chain) | 2026-07-06 — Q30–Q36 all shipped; D7 conversion-webhook scaffolding deferred to v2beta1 graduation (Q74), H2 live-e2e companion folded into Q396. Durable A6 (`CreateOrPatch` vs SSA) + HTTP/2 Rapid-Reset facts promoted to [02-architecture.md](../design/02-architecture.md) + [05-security.md](../design/05-security.md) |
| [archive/q12-helm-chart.md](archive/q12-helm-chart.md) | Production Helm chart `charts/actions-gateway/` — GMC CRDs, RBAC, validating webhook, and admission policy installable via `helm install`, every default at the secure posture from the `cmd/gmc/config/` kustomize bases | 2026-06-28 — Q12; chart shipped, live-validated end-to-end (Q219), the last 1.0 packaging gate |
| [archive/q51-metrics-reconcile.md](archive/q51-metrics-reconcile.md) | Reconcile operator-facing metrics docs with the metrics the code registers — the six-layer audit (Layer 3) found six documented-but-unregistered names; per-metric implement-or-drop | 2026-06-16 — Q51 |
| [archive/q56-namespace-patch-vap.md](archive/q56-namespace-patch-vap.md) | Gate the GMC's cluster-wide `namespaces: patch` so a compromised GMC can't relabel a non-tenant namespace (PSA escalation) — `namespace-psa-guard` VAP + marker-label contract | 2026-07-06 — Q56 (k8s audit §B B2) |
| [archive/q64-pod-informer-waiter.md](archive/q64-pod-informer-waiter.md) | Replace the AGC provisioner's per-session Pod poll with a single shared Pod informer waiter (event-driven completion detection) | 2026-07-06 — Q64 (k8s audit §A4) |
| [archive/q69-authenticated-metrics.md](archive/q69-authenticated-metrics.md) | Authenticated secure-serving (uniform mTLS) for the proxy + AGC `/metrics` on `:8443` | 2026-06-16 — Q69 |
| [archive/q72-metrics-scrape-wiring.md](archive/q72-metrics-scrape-wiring.md) | Per-tenant metrics scrape wiring — Services + ServiceMonitors so Prometheus can scrape the mTLS `:8443` endpoints Q69 shipped | 2026-06-17 — Q72 |
| [archive/q74-v2beta1-conversion-webhook.md](archive/q74-v2beta1-conversion-webhook.md) | The v2alpha1→v2beta1 graduation cut in mechanical detail: conversion webhook, served/storage/hub wiring, protocol-field-in-annotations strip | 2026-07-06 — Q74; graduation shipped (PR #557) |
| [archive/quota-pressure-conditions.md](archive/quota-pressure-conditions.md) | Surface namespace `ResourceQuota` pressure on the proxy pool + worker pods as Kubernetes status conditions and controller-exported Prometheus metrics | 2026-06-20 — Q82 |
| [archive/q95-worker-pod-lifecycle.md](archive/q95-worker-pod-lifecycle.md) | Worker-pod lifecycle: bounded-retention deletion of completed pods, deadline reaping of stuck-Pending pods, OwnerReference GC on tenant/CR deletion | 2026-06-11 — Q95 |
| [archive/q97-multiarch-images.md](archive/q97-multiarch-images.md) | Every published first-party image (`gmc`/`agc`/`proxy`/`worker`) a multi-arch OCI index (linux/amd64+arm64) with signing/SBOM intact | 2026-06-11 — Q97 |
| [archive/q114-jit-agent-selfheal.md](archive/q114-jit-agent-selfheal.md) | AGC self-healing for single-use JIT agents — recover when GitHub deletes a completed JIT runner record out from under a live session (M4 §12 bug 2) | 2026-06-13 — Q114 |
| [archive/q121-q122-q125-gmc-confinement.md](archive/q121-q122-q125-gmc-confinement.md) | Three `1.0-gate` findings from security-audit-2026-06 on the GMC RBAC + reconcile/teardown surface: name-scoped Secret RBAC, fail-closed teardown, confinement — implemented together | 2026-06-25 — Q121/Q122/Q125 |
| [archive/q132-priorityclass-allowlist.md](archive/q132-priorityclass-allowlist.md) | Platform-owned PriorityClass allowlist for runner `priorityTiers` — GMC webhook rejects any tenant `priorityClassName` not on the admin-configured allowlist | 2026-06-26 — Q132 |
| [archive/q134-session-registration-flake.md](archive/q134-session-registration-flake.md) | Root-cause + fix for the intermittent e2e "no session registered" flake (AGC never registers a broker session with the shared in-cluster fakegithub) | 2026-06-15 — Q134 |
| [archive/q138-bounded-http-clients.md](archive/q138-bounded-http-clients.md) | Bounded-by-default HTTP clients — eliminate `http.DefaultClient` no-timeout fallbacks (the Q108/Q134 wedge class) via an `httpx` helper + a linter gate | 2026-06-16 — Q138 |
| [archive/drop-kustomize.md](archive/drop-kustomize.md) | Make the Helm chart the single cluster-install path; eliminate the duplicate hand-maintained `cmd/*/config` kustomize manifests | 2026-06-18 — Q142 |
| [archive/q143-single-source-webhook-roles.md](archive/q143-single-source-webhook-roles.md) | Extend the Q142 single-source + drift-gate pattern to the chart's validating webhook config and agc-tenant-role (kill the remaining hand-copies) | 2026-06-18 — Q143 |
| [archive/q145-apiserver-cidr-allowlist.md](archive/q145-apiserver-cidr-allowlist.md) | Optional opt-in apiserver-CIDR allowlist scoping the AGC NetworkPolicy's 443/6443 egress (closes the 05-security §5.2 residual; unset preserves any-destination) | 2026-06-18 — Q145 |
| [archive/q156-failure-conditions.md](archive/q156-failure-conditions.md) | Surface provisioning + credential failures in CRD `.status.conditions`; promote condition type/reason strings to exported API consts | 2026-06-20 — Q156 |
| [archive/v2-api-m2-q163.md](archive/v2-api-m2-q163.md) | v2 API M2 — the data-kind ("noun") reconcilers: EgressProxy reconciler + RunnerTemplate webhook | 2026-06-21 — Q163 (v2-api.md M2) |
| [archive/q164-m3a-build.md](archive/q164-m3a-build.md) | v2 API M3a core build (single-gateway parity): the two reconcilers + the provisioner owner-ref seam | 2026-06-25 — Q164 (v2-api.md M3a) |
| [archive/v2-api-m5-migration.md](archive/v2-api-m5-migration.md) | v2 API M5 — one-shot `gag-migrate` tool fanning a v1alpha1 tenant out to v2alpha1 + the v1/v2 cutover | 2026-06-25 — Q165 (v2-api.md M5) |
| [archive/v2-api-m4-cross-namespace-sharing.md](archive/v2-api-m4-cross-namespace-sharing.md) | v2 API M4 — cross-namespace `EgressProxy` sharing: the provider-consent check, ConfigMap CA distribution, dual-side NetworkPolicy, and the two design holes the build surfaced (a cross-namespace ref was not expressible; the AGC cannot read a remote proxy) | 2026-08-08, Q166 (v2-api.md M4) |
| [archive/q168-direct-egress.md](archive/q168-direct-egress.md) | Make the v2 egress proxy genuinely optional — no `proxyRef` ⇒ NetworkPolicy-restricted direct egress, not `Degraded`/`ProxyNotFound` | 2026-06-23 — Q168 |
| [archive/q170-job-lifecycle-events.md](archive/q170-job-lifecycle-events.md) | Emit Kubernetes Events on the four key AGC job-lifecycle transitions (event-based incident visibility alongside metrics/conditions) | 2026-06-22 — Q170 |
| [archive/q172-optional-templateref.md](archive/q172-optional-templateref.md) | Relax v2 `RunnerSet.spec.templateRef` from required to optional (non-breaking) — unset resolves a worker shape via a fallback chain incl. a default ClusterRunnerTemplate | 2026-06-22 — Q172 |
| [archive/q182-audit-policy-install.md](archive/q182-audit-policy-install.md) | Ship the API-server audit policy as an applyable/installable asset (auto-install where the project controls the apiserver config) instead of a hand-copied static file | 2026-06-25 — Q182 |
| [archive/q197-workload-identity-signer.md](archive/q197-workload-identity-signer.md) | The `spec.credentials.workloadIdentity` union member — authenticate to GitHub via an external App-JWT signer, no GitHub App private key in-cluster | 2026-06-24 — Q197 |
| [archive/q201-workload-identity-provisioning.md](archive/q201-workload-identity-provisioning.md) | Complete the no-PEM delegation path end-to-end (GMC signer-config env + kubelet-projected token; Vault kind e2e) so a WorkloadIdentity gateway provisions + authenticates | 2026-06-25 — Q201 |
| [archive/q208-fqdn-egress.md](archive/q208-fqdn-egress.md) | CNI-native FQDN egress opt-in — express the proxy pool's GitHub allowlist as Cilium `toFQDNs` / Calico `domains` instead of the 24h CIDR reconcile (additive, default unchanged) | 2026-06-27 — Q208 |
| [archive/q220-service-mesh-validation.md](archive/q220-service-mesh-validation.md) | Live-cluster validation of the service-mesh coexistence guidance (Istio + Linkerd sidecar mode on kind); corrected the operator guide | 2026-07-07 — Q220 (evidence record; broadened by Q280) |
| [archive/q260-fanout-completion-reconciliation.md](archive/q260-fanout-completion-reconciliation.md) | Reconcile GitHub's per-delivery fan-out with AGC's one-runner-per-session model — Option A winner-driven fan-out completion (`AGC_FANOUT_COMPLETION`, default on) | 2026-07-06 — Q260 |
| [archive/q260-planid-dedup-refix.md](archive/q260-planid-dedup-refix.md) | Q260 re-fix — re-key job-acquisition dedup on `planID` to stop the burst-start Secret-collision pool collapse | 2026-07-04 — Q260 |
| [archive/required-status-checks.md](archive/required-status-checks.md) | Make the CI quality gates merge-blocking: an always-running `gate` summary job per workflow (safe to require despite path-gating) + the ruleset edit that marks them required | 2026-07-13 — Q297 |

## Conventions

When adding a new plan:

- Put it at the top of the file: a one-paragraph "what and why," then a **Status at a glance** table if there are 3+ discrete work items with mixed state.
  The table is the index a returning reader scans first.
- Cite code with file:line links.
  They go stale, but stale links are easier to fix than missing ones.
- Mark deferred or accepted items explicitly (⚠️ Partial — *what was accepted and why*).
  Silent omissions become land mines.
- Once everything in a plan ships, leave the plan in place with the status table updated to ✅ Done.
  Don't delete it — the rationale is more valuable than the diff.

When writing a plan's Status cell:

- Link a backlog item that is still open as `[QNNN](../queue/QNNN.md)`, and write a closed one bare.
  Bare is the closed form, so the link is what makes the claim expire with the row: closing it kills the anchor, and `make plan-index-check` then reports the cell that summarizes work which has finished.
  The rollup only changes when a plan's last row closes, and the worker closing that row cannot tell it was the last one, which is how three cells went stale across 1.4 and 1.5 (Q800).
- Column 3 only.
  A bare ID in the Scope column says what the plan was about and stays true after the row closes.
- A `release-X.Y.md` row cannot read as open (❌, 🔲, 🚧) once that release is published.
  The tag is a fact, so `make plan-index-check` fails the cell instead (Q812): mark it ✅, or ⚠️ if a Queue row genuinely remains.
  Nothing else could see this, and it is not hypothetical: the 1.3 row read `❌ Open` on `main` for the nine days after `v1.3.0` shipped, with every other rule green.

When a plan fully closes:

- If `docs/STATUS.md` still references it (Progress table or any Queue row), leave it under `docs/plan/`.
- Once STATUS.md no longer references it, `git mv` it to `docs/plan/archive/` and move its row in this README to the Archive section.
  Update any other in-repo links to the new path **and the moved doc's own relative links** (dropping into `archive/` adds one `../` level).
  The doc stays available; the working directory just gets less noisy.
  See the full protocol in [`docs/development/maintaining-backlog.md`](../development/maintaining-backlog.md#archiving-completed-plan-docs).
- **Do this on close, not in a later audit** — in the same change that drops the plan's last STATUS reference.
  `make plan-index-check` (part of `make check`) fails when an active, non-`ⓘ` plan here is no longer referenced by STATUS.md, so a forgotten archival can't ship silently.

Add a row to this README when creating, completing, or archiving a plan.

{{- /*
Generated from cmd/gmc/config/webhook/manifests.yaml by 'make chart-webhook' — DO NOT EDIT.
The webhook body (rules, failurePolicy, path, sideEffects, admissionReviewVersions) is the
controller-gen output of the +kubebuilder:webhook marker; this template re-injects the chart's
Helm wiring (name prefix, labels, namespace, cert-manager CA inject / caBundle). Re-run
'make chart-webhook' after any webhook-marker change. Drift is gated by 'make chart-webhook-check'.
*/ -}}
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingWebhookConfiguration
metadata:
  name: {{ include "actions-gateway.namePrefix" . }}-validating-webhook-configuration
  labels:
    {{- include "actions-gateway.labels" . | nindent 4 }}
  {{- if .Values.certManager.enabled }}
  annotations:
    # cert-manager's ca-injector copies the Issuer's CA into the caBundle below.
    cert-manager.io/inject-ca-from: {{ printf "%s/serving-cert" .Release.Namespace }}
  {{- end }}
webhooks:
- admissionReviewVersions:
  - v1
  clientConfig:
    service:
      name: webhook-service
      namespace: {{ .Release.Namespace }}
      path: /validate-actions-gateway-github-com-v1alpha1-actionsgateway
    {{- if not .Values.certManager.enabled }}
    {{- include "actions-gateway.webhookCerts" . }}
    caBundle: {{ b64enc $.agWebhookCerts.caCert }}
    {{- end }}
  failurePolicy: Fail
  name: vactionsgateway-v1alpha1.kb.io
  rules:
  - apiGroups:
    - actions-gateway.github.com
    apiVersions:
    - v1alpha1
    operations:
    - CREATE
    - UPDATE
    resources:
    - actionsgateways
  sideEffects: None
- admissionReviewVersions:
  - v1
  clientConfig:
    service:
      name: webhook-service
      namespace: {{ .Release.Namespace }}
      path: /validate-actions-gateway-com-v2alpha1-clusterrunnertemplate
    {{- if not .Values.certManager.enabled }}
    {{- include "actions-gateway.webhookCerts" . }}
    caBundle: {{ b64enc $.agWebhookCerts.caCert }}
    {{- end }}
  failurePolicy: Fail
  name: vclusterrunnertemplate-v2alpha1.kb.io
  rules:
  - apiGroups:
    - actions-gateway.com
    apiVersions:
    - v2alpha1
    operations:
    - CREATE
    - UPDATE
    resources:
    - clusterrunnertemplates
  sideEffects: None
- admissionReviewVersions:
  - v1
  clientConfig:
    service:
      name: webhook-service
      namespace: {{ .Release.Namespace }}
      path: /validate-actions-gateway-com-v2alpha1-runnertemplate
    {{- if not .Values.certManager.enabled }}
    {{- include "actions-gateway.webhookCerts" . }}
    caBundle: {{ b64enc $.agWebhookCerts.caCert }}
    {{- end }}
  failurePolicy: Fail
  name: vrunnertemplate-v2alpha1.kb.io
  rules:
  - apiGroups:
    - actions-gateway.com
    apiVersions:
    - v2alpha1
    operations:
    - CREATE
    - UPDATE
    resources:
    - runnertemplates
  sideEffects: None

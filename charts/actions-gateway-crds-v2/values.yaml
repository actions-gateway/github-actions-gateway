# This chart ships only CustomResourceDefinitions for the v2alpha1
# (actions-gateway.com) API; it has no configurable values. The CRD templates
# under templates/crds/ are generated from the controller-gen sources by
# scripts/sync-chart-crds.sh — do not hand-edit them.

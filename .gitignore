.context
# Claude local state — ephemeral, machine-local (keep settings.json / commands / agents / skills / hooks tracked)
.claude/worktrees/
.claude/settings.local.json
.claude/.credentials.json
.claude/scheduled_tasks.lock
.claude/shell-snapshots/
.claude/history.jsonl
.claude/todos/
.claude/tasks/
.claude/projects/
.claude/sessions/
.claude/session-env/
.claude/cache/
.claude/telemetry/
.claude/backups/
.claude/downloads/
CLAUDE.local.md
# binary build output
/.build/
# Local-build of the fakegithub helper (`make` at repo root drops it here);
# root-anchored so it doesn't shadow the test/fakegithub/ source directory.
/fakegithub
# stray binaries — always build via make build-* into .build/ instead
/cmd/agc/agc
/cmd/gmc/gmc
/cmd/probe/probe
/cmd/proxy/proxy
/cmd/worker/worker

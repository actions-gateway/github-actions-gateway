apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Library entry `privileged-dind` — see ../README.md. Each entry is its own base
# so an operator adopts exactly one; there is deliberately no aggregate
# kustomization that applies all three.
#
# PATCH THIS BASE WITH JSON 6902, NOT A STRATEGIC MERGE, wherever the patch
# reaches into a list — see the same note in ../kata-dind/kustomization.yaml for
# the mechanism, the measurement, and the two ways round it.
resources:
  - template.yaml
